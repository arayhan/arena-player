# PROGRESS archive — planning phase

Entries from the planning phase, before Phase 1a scaffolding began. Moved out of
`../PROGRESS.md` verbatim to keep the live log short, because every agent is instructed
to read it before working and it had reached 44 KB.

Consult this only when tracing _why_ an older decision was made. What is currently
**true** lives in PRD.md, architecture.md, DESIGN.md, and PRODUCT.md — not here.

---

[2026-07-31] [user] Project re-planned from scratch after prior build got messy (agents/hooks/skills built before planning, DB pivoted mid-build Supabase->Neon+R2->revert). DB re-locked as Neon+R2, final.
[2026-07-31] [setup] Scaffolding order this time: PRD -> architecture -> design-system/database -> CLAUDE.md -> settings/hooks -> MCP -> skills -> agents (last). See archive/neon-r2-pre-wipe git tag for the fuller pre-reset build if old code needs referencing.
[2026-07-31] [setup] Agent team + skills built. Ready for Phase 1 execution via docs/tasks/ (currently empty, populated when build starts).
[2026-08-07] [user] /impeccable shape produced a confirmed design brief for the ORDER SECTION, persisted to .impeccable/surfaces/app-page-tsx.md via the skill's own writer (so context.mjs auto-loads it on future runs instead of it living only in a plan file). Three decisions, all resolving PRODUCT.md principles that had no design expression: (1) DATE ROW â€” today preselected, "Hari ini"/"Besok" prominent, days 3-14 behind a "Pilih tanggal" control, so the scarcest horizontal space at 375px stops going to the least-used dates (principle 5). (2) ELAPSED HOURS â€” collapse into an expandable "Sudah lewat (N)" row at the top, so the list opens on what is bookable and the day never reads as sold out. (3) FILL AFFORDANCE â€” scarcity counter "sisa N slot", shown ONLY when 1-3 remain, because "sisa 8 slot" is not scarcity, it advertises an empty venue and works against the client's own goal (principle 6). BIGGEST FINDING: the elapsed-slot problem was recorded in PRODUCT.md as needing a distinct `past` status in an API contract marked FIRM â€” a Phase 4 blocker. It is not one. The client already knows the current time and the canonical slot starts from lib/slots.ts + lib/dates.ts, so it computes elapsed locally and simply stops DISPLAYING those hours as "Terisi". The API returning `booked` is harmless once the client owns the label. It was a display problem wearing a backend costume. PRODUCT.md corrected. Brief also enumerates two states the design never had: ALL-ELAPSED (distinct from "penuh" â€” the day was not full, it is over; after 24.00 it is the only possible state) and ERROR (never an empty grid, which would read as fully booked). Motion deliberately left unspecified per the project's ask-before-animating rule.
[2026-08-07] [user] .impeccable/ is NOT blanket-gitignored â€” the two things inside it differ in kind. design.json STAYS TRACKED: 17.8KB of real design authority not duplicated elsewhere (tonal ramps for navy/blue, the semantic-layer map, per-component HTML+CSS, 11 named rules with dos/donts). A blanket .impeccable/ rule would have started ignoring a file git already tracks â€” the confusing state where a tracked file matches an ignore pattern. critique/ IS ignored: snapshots are graded self-assessments that go stale the moment they are acted on, and the current one says the artifact "cannot do the job the checkpoint is buying", scores 22/40, and records that fabricated contrast figures shipped. Ignoring does not delete â€” /impeccable polish still reads them locally. live/ pre-emptively ignored as machine-local config (dev command, port) though it does not exist yet. NEW OPEN DECISION in PRODUCT.md: does the git repo transfer to the client at handover? Not abstract â€” it decides whether PROGRESS.md's candid notes, the plan docs' rejected approaches, and critique snapshots are client-visible. Undecided, so the conservative default applies. Sits beside the open Neon/R2 ownership question. ALSO: the contrast correction was INCOMPLETE â€” DESIGN.md and DESIGN.html were fixed to 6.37/6.80 but .impeccable/design.json still carried 7.4/7.9 in two note fields, and since that file is tracked the wrong figures were the committed truth. Third location, found only by inspecting the folder for an unrelated question. CORRECTION TO MY OWN EARLIER CLAIM: I reported mojibake in design.json's narrative block. False. Decoded as UTF-8 the file has 4 real em-dashes (U+2014) and ZERO replacement chars â€” PowerShell 5.1 was rendering UTF-8 as CP1252 in its display output. The corruption was in my reading, not the file. Did not "fix" it; rewriting correct em-dashes to hyphens would have damaged a clean tracked file based on a tooling artifact.
[2026-08-07] [user] /impeccable init round 2 (update branch â€” PRODUCT.md existed, so asked what was stale rather than redoing the interview). THREE ANSWERS, all consequential. (1) NEW GAP FOUND before asking: nothing anywhere stated booking LEAD TIME, yet the 14-day window drives the date row that is the first thing in the order section. Answer: MOSTLY SAME-DAY OR NEXT-DAY. So fourteen equal-weight pills spend the scarcest horizontal space at 375px on the least-used dates. Recorded as principle 5: today is the default, not one of fourteen. (2) SUCCESS = FILL EMPTY SLOTS (revenue, not admin convenience â€” less WA back-and-forth is a side effect, not the goal). Consequence recorded as principle 6: all nine slots are currently presented as equals, and a product measured on filling dead hours needs some affordance making a quiet slot more attractive than a blank row â€” which cannot imply a discount since no pricing is confirmed. That affordance exists nowhere in the plan. (3) TRAFFIC = MIX, none dominant, so no channel-specific optimisation may be prioritised; OG image and metadata both matter, neither earns disproportionate effort. CONFLICT SURFACED by combining (1) and (2): GET /api/availability returns `booked` for today's ELAPSED slots and the design renders them identically to genuinely-taken ones. That was fine when the demand pattern was unknown. With same-day confirmed as the primary journey, someone opening at 19.00 sees 06.00-18.00 all labelled "Terisi" â€” the day looks SOLD OUT when six of those hours were simply never available to them. A page that makes today look full is the worst possible outcome for a product measured on filling empty slots. Fixing it needs a distinct `past` status in an API contract currently marked FIRM â€” recorded in PRODUCT.md as product truth, decision belongs to the Phase 4 backend discussion.
[2026-08-07] [user] PRODUCT.md lives at docs/PRODUCT.md, not the repo root. Initially written to root because impeccable's reference/init.md says "New files go at PROJECT_ROOT/PRODUCT.md" â€” but that is the skill's default, not a constraint: context.mjs resolves from the active project root AND from .agents/context/ then docs/. Root also clashed with this repo's own convention where every other doc lives in docs/. Moved and verified by re-running context.mjs, which now reports productPath: docs\PRODUCT.md with no NO_PRODUCT_MD error. Added to the architecture.md folder tree (ordered before PRD.md â€” product truth is upstream of product spec) and to CLAUDE.md's docs table as the first row, since that table is explicitly "read in this order".
[2026-08-07] [user] PRODUCT.md written via /impeccable init (product truth only â€” init never writes DESIGN.md or visual direction). CONFIRMED: SINGLE FIELD â€” this was never stated anywhere before and is load-bearing; the bookings schema keys on (booking_date, time_slot) with no field identifier, so uniq_active_slot is correct as written, but a second field would change schema + API contract + mock + order section. Worth having asked. Primary user CONFIRMED as team captain / group organiser booking for 8-12 people, usually mid-conversation in another chat â€” which retroactively justifies two existing decisions (order section within 1-2 scrolls, unavailable slots visible not hidden). TWO OPEN DECISIONS deliberately left unmarked rather than inferred, at the user's instruction: (1) primary traffic channel â€” IG bio vs WhatsApp shares vs organic search, and it is not cosmetic, WhatsApp sharing would make the OG image load-bearing and search would promote SEO from a DoD checkbox to a real requirement; (2) how the client defines success â€” less admin back-and-forth vs filling off-peak slots vs beating competitors on credibility, and each optimises for something different (filling off-peak would justify surfacing quiet slots differently, an affordance that does not exist). Both are the client's to answer, not the developer's. Also recorded: positioning is the anti-double-booking guarantee (a neighbouring field can copy the visuals but not live availability backed by a DB constraint), and the six unsupplied TODO(content) items are listed under Evidence with an explicit must-not-fabricate note.
[2026-08-07] [user] design-system.md enhanced to a real three-layer token system (primitive -> semantic -> component, per ui-ux-pro-max:design-system) + docs/design-system.html renders it. Was 3 colours and 2 font families with no type scale, spacing, radius, elevation, motion tokens, or component specs. FOUR DECISIONS taken via AskUserQuestion: mixed radius (2px cards/buttons, 4px cards, pill for date rows only â€” near-sharp carries the Orbitron athletic feel while round pills signal the row scrolls); slot grid is ONE COLUMN full-width rows, driven by a hard constraint â€” "Menunggu Konfirmasi" is 20 chars and a 3-col grid at 375px gives ~110px per cell so the label cannot fit at all, 2-col forces truncation; fluid type 1.25@375px -> 1.5@1440px via clamp() so oversized display type lands where there is room without breaching the 1-2 scroll rule on mobile; 4px spacing base with section rhythm restricted to 48/64/96/128. ACCESSIBILITY FIX: the old spec said "Pending: yellow" â€” yellow text on white is ~1.4:1 and unreadable, and "Menunggu Konfirmasi" is information not decoration. Status colours are now surface+border+text TRIPLES: pending amber-100/300/800 at 7.4:1, booked red-100/300/800 at 7.9:1. Also documented: past slots render identically to booked because GET /api/availability returns `booked` for elapsed slots so the client genuinely cannot distinguish them â€” any design that separates them needs an API change first. HTML carries an explicit warning that it is a reference artifact, NOT production: fonts load from CDN (no build step) where production uses next/font for zero-CLS self-hosting, and motion is plain CSS where production must route through lib/motion.ts â€” copying either into the app breaks hard rules 6 and 7. SEQUENCING NOTED: this is Phase 1b task 4 built before task 1 (art direction) and task 2 (hero copy), so the HTML is tokens+components only and lists what remains undecided rather than silently inventing direction.
[2026-08-07] [review] architecture.md reviewed, 9 fixes. THREE CONTRADICTIONS with the PRD: (1) the booking-flow line still said "open wa.me AND route to /booking" â€” drift I introduced two turns ago when the flow changed to WhatsApp-only; the doc was contradicting the spec it implements. (2) API contract had no 429 despite the PRD adding rate limiting â€” this section is what MSW implements and agents are told to read instead of inventing shapes, so a code missing here is one the UI never handles; now documented with the explicit warning that 409 and 429 are NOT interchangeable (showing 409 copy to a rate-limited user tells them their slot was taken when it was not). (3) POST flow had no rate-limit step at all; now step 1, BEFORE multipart parse, because parsing a 2MB body before rejecting an attacker is most of the cost the limit exists to avoid. Whole flow restated as cheapest-rejection-first. FOLDER STRUCTURE was stale in four ways: public/ was entirely missing though logo/favicon/OG live there AND mockServiceWorker.js lands there â€” the exact file the Phase 4 MSW check looks for; lib/store/ (zustand) existed in CLAUDE.md but not here, and this doc is the source of truth; hooks list showed only notify.ps1 (missing inject-gotchas.ps1 and check-claudemd.ps1); two stale "Phase 1" refs plus docs/tasks/step-01 naming left over from the old 8-step plan. Added lib/proof.test.ts â€” proof.ts holds MIME and size constraints, which is exactly the non-trivial logic hard rule 8 requires tested. PACKAGE VERSIONS unpinned: next ^15.5.22, react ^19.2.8, @aws-sdk/client-s3 ^3.1098.0 and pnpm@11.17.0 were never verified against the registry, and false precision reads as "someone checked" when nobody did â€” a wrong pin fails install on day one of 1a. All now "resolve at install", to be recorded back into the table at the same moment the performance budget gets replaced with measured figures.
[2026-08-07] [user] Context7 MCP added to .mcp.json (local npx, no API key â€” matches the existing neon server's stdio+npx shape; package resolves, @upstash/context7-mcp 3.2.5). Reason it earns its place on THIS project: several locked libraries have breaking changes that training data reliably gets wrong â€” MSW v2 (http.get/HttpResponse.json vs v1's rest.get/res(ctx.json())), TanStack Query v5 (isPending, object-form args), zod v3 vs v4, @gsap/react's useGSAP, and the Neon serverless driver whose OID parser override database.md calls blocker-class. Phase 1a task 6 builds the MSW mock layer, which is the first place stale syntax would bite. CLAUDE.md gained one line â€” borderline against its own "when to update" criteria, but it changes agent behaviour (look up current API rather than recall), which is a cross-cutting convention and therefore on the update list.
[2026-08-07] [user] TanStack Start evaluated as a Next.js replacement, REJECTED. Recorded in architecture.md as a FINAL decision alongside the DB one so it does not get re-derived. Not because it is worse â€” it has one genuine fit here, TanStack Router's validateSearch mapping onto the four /booking param states with zod already chosen â€” but it loses on this project's constraints: (1) HANDOVER, paid project with a 14-day warranty then maintained by someone else, and Next.js developers are abundant while TanStack Start developers are scarce; (2) next/font and next/image are LOAD-BEARING not conveniences â€” design-system.md uses them for zero-CLS webfonts and reserved image space, feeding hard rule 6 (no CLS) and 7 (LCP <2.5s with hero TEXT as the LCP element), so switching means hand-rolling those guarantees; (3) maturity during a warranty on a tight budget; (4) rewrite across 3 docs + 4 agent files. Corollary now written down: next/font and next/image are not freely swappable â€” replacing either requires proposing a replacement for the CLS/LCP guarantees, not just a different import. What would have justified revisiting was a deploy target unable to run Next.js â€” now ruled out. SUMOPOD CONFIRMED Node-capable, closing the biggest open risk in the deploy plan (PRD had said "fallback is Vercel if Sumopod cannot run Next.js (to be confirmed)" since it was written). next build + output:'standalone' deploys directly; Vercel demoted from live fallback to contingency footnote. Still open, deliberately: subdomain config on Sumopod â€” only Node capability was confirmed, and subdomains matter for admin.arena-player.com in the other repo. CLAUDE.md deliberately NOT updated â€” checked against its own criteria: tech stack unchanged (Next stays), no hard rule moved, no phase or folder change, and deploy-target detail is not something an agent needs before touching code.
[2026-08-07] [review] Product Spec reviewed, 9 findings applied. FLOW CHANGED: slot select now opens wa.me ONLY â€” the site no longer navigates to /booking simultaneously. Old spec sent the user to two destinations from one click, which on mobile means a WhatsApp app deep-link racing a same-tab navigation, the exact combination in-app webviews and popup blockers handle inconsistently, on the primary traffic source. Consequence accepted and documented: /booking has NO automated entry until the bot ships, so the ADMIN IS THE BOT in the interim â€” they receive the WA message and paste back /booking?date=&time=. Same URL either way. Knock-ons handled: /booking entry is always a pasted link so malformed/stale params are the normal case not the edge â€” spec'd all four states (valid / missing / expired / unavailable), and "unavailable" deliberately still lets the user proceed because pre-checking would be a check-then-insert race. Phase 3 journey check rewritten as TWO LEGS since WhatsApp is a deliberate break in the chain. ABUSE: POST /api/bookings is public+unauthenticated with only a honeypot; a trivial script could hold all 126 slots (9x14) in pending for 24h until lazy expiry, and burn unbounded R2 storage. Spec'd per-IP rate limit + server-side size/MIME rejection BEFORE the R2 write + 429 distinct from 409; built in Phase 4. SCHEMA: added time_slot_canonical CHECK â€” the race guard is a unique index on TEXT, so '06.00 - 08.00' vs '06.00-08.00' were different rows booking the same slot and the guard silently did nothing; lib/slots.ts canonicalised in app code but nothing enforced it in the DB. Renamed proof_url -> proof_key (stored a key, name said URL, someone would have written img src). phone now normalised to 628xxx on write (wa.me and WA API format, so the later bot matches by direct lookup instead of fuzzy). notes capped at 500 chars. Also documented: cancellation is manual via WA (Ketentuan rule 2 grants it but no cancel route is scoped), the 30s public cache deliberately increases 409s, and availability is one-date-per-request so 1b must not design a per-pill availability indicator without a bulk endpoint.
[2026-08-07] [user] Library choices settled. Question was "is picking libraries Phase 1a?" â€” yes: 1a task 1 covers state strategy, and eng-lead enforces "no new dependencies without user approval". PICKED: zustand (client state), zod (validation), react-hook-form (/booking). DEFERRED to 1a task 1: date handling (native Intl in a tested lib/dates.ts vs a library) and icon library. My recommendation on state was NONE â€” server state is TanStack Query's, the two selections are useState-sized, and cross-page state already travels via /booking?date=&time= query params, so there is no shared or cross-page client state; user chose zustand anyway, so CLAUDE.md rule 9 and frontend.md now carry explicit scope discipline (a store duplicating server data or URL state has outgrown its purpose). BUDGET CONSEQUENCE, flagged not litigated: subtotal moved ~151 -> ~174KB, headroom ~50 -> ~26KB, with dates+icons still unbudgeted at ~5-8KB. That leaves ~18-21KB for ALL component code across a 5-section landing page plus a form, which is plausibly not enough. Every figure is an estimate; the first real pnpm build in 1a settles it. Pre-planned fix recorded in architecture.md and frontend.md: next/dynamic so / never loads react-hook-form or zod, which only /booking needs â€” costs nothing to plan now. Alternatives if that is insufficient: raise the 200KB ceiling with evidence, or drop a library. CLAUDE.md updated because tech stack is on its own "when to update" list â€” the criteria written earlier this session working as intended.
[2026-08-07] [user] CLAUDE.md upkeep: criteria written + Stop hook backstop. THRESHOLD now explicit in CLAUDE.md's "When to update this file" section, because "major" was undefined and wrong in either direction is costly â€” update too eagerly and CLAUDE.md becomes a second PRD (95 lines gets read, 300 gets skimmed, and a CLAUDE.md nobody reads is worse than a short one); too rarely and its phase table, hard rules, and folder structure start lying to every agent that loads it. UPDATE for: phase structure, hard rules, tech stack, folder structure, cross-cutting conventions, install/run commands, repo scope. DO NOT for: task detail, DoD checkboxes, rationale prose, anything lookup-able in the PRD. Threshold calibrated against this session â€” the phase re-cut, GSAP swap, TODO(content) rename, and repo-scope change all belonged in CLAUDE.md and went in; the wa.me caveat, journey-check wording, and DoD checkboxes correctly did not. HOOK: .claude/hooks/check-claudemd.ps1 fires on Stop when docs/PRD.md, docs/architecture.md, .claude/skills/** or .claude/agents/** changed and CLAUDE.md did not. It NUDGES, never gates â€” it cannot judge whether the change crossed the threshold, so "deliberate, no update needed" is a valid answer. Anti-loop is the whole design risk: a Stop hook must exit 2 for its message to reach Claude, and exit 2 blocks the turn, so the script bails immediately when the harness passes stop_hook_active=true on the re-fire. Exactly one nudge per turn. Registered synchronously (no async) since the exit code IS the signal â€” unlike notify.ps1 which stays fire-and-forget. Any error path exits 0; a broken hook that wedges the session is worse than a missed nudge. Verified in a throwaway git repo across four cases: trigger (exit 2 + correct file named), anti-loop (exit 0 silent under identical dirty tree), clean tree (0), both-changed (0).
[2026-08-07] [user] Review loop partially automated. Key constraint: hooks run SHELL COMMANDS, they cannot invoke reasoning â€” so only the mechanical half of review is hookable. Of round 3's six findings, four were pure greps (stale marker, category-count mismatch, bare "Phase 1", a replace_all corrupting a sentence) and two needed judgment (skill out of sync with PRD decisions, a rationale that had gone stale). Split accordingly. DONE NOW: SessionStart hook (.claude/hooks/inject-gotchas.ps1) injects the trap list every session, so arena-gotchas' "every agent must load this once per session" is guaranteed instead of honor-system â€” that rule was previously unenforceable and was in fact only honoured this session because the user asked. Script is ASCII-only: PowerShell 5.1 read the UTF-8 em dashes as ANSI and injected mojibake into context on first test. Exits 0 unconditionally so a broken hook never blocks a session. DEFERRED to Phase 1a task 3 (where the DX harness already lives, avoiding a tooling detour before any product code exists): check:docs script asserting no TODO(phase2) survives, TODO(content) finds exactly six categories, no bare "Phase 1", overview table matches detail sections â€” wired to a Stop hook exiting 2 so failures loop back, guarded on stop_hook_active or it recurses forever. NOT automated, by design: whether a skill still matches the PRD, whether a rationale is still true.
[2026-08-07] [review] Round 3, self-review against arena-gotchas. MARKER RENAMED: TODO(phase2) -> TODO(content) everywhere. The re-cut made "Phase 2" mean the landing page, so TODO(phase2) pointed at the wrong phase â€” an agent working Phase 2 would grep it, find the WA number and bank account, and reasonably think they were theirs to resolve. They actually swap in the Real content phase, AFTER Phase 4. Renamed now because zero code exists, so nothing had to be re-grepped; naming the work instead of a phase number survives future renumbering. Category list was also incomplete: declared 4 (WA/bank/address/photos) while logo and hero copy were also TODO items, so an audit against the list would flag two legitimate placeholders as stray. Now six, stated identically in CLAUDE.md rule 3, PRD, and arena-gotchas. arena-gotchas synced with rounds 1-2, which it had missed entirely despite being the skill every agent loads before writing code: backend was still lumped as "deferred to a later discussion" alongside the optional WA bot (now Phase 4 MANDATORY), no MSW retirement trap, no hero-copy ownership, no client checkpoints. Also fixed: PRD said axios was a "Phase 3 decision" when the instance and QueryClientProvider are built in 1a task 6 and Phase 3 only consumes them. Self-inflicted during this round and caught: a replace_all rewrote the sentence explaining the rename so it read "it was once TODO(content)".
[2026-08-07] [review] Phase plan review round 2, 7 more fixes. (1) NO CLIENT CHECKPOINT existed anywhere â€” all gates were internal. Added two: after 1b (client sees design system HTML) and after Phase 2 (client sees landing on a real phone), both in the DoD blocks not just prose, since a checkpoint outside the DoD is a suggestion. The 1b one is the high-leverage one: rejecting a direction there costs one HTML page, the same rejection in Phase 2 costs five rebuilt sections. Flagged as a BLOCKING input â€” a slow client is schedule risk to raise, not a reason to build on an unapproved direction. Phase 3 stays internal. (2) MSW WAS NEVER RETIRED â€” genuine deployment hazard, not tidiness: it registers a service worker, so a stray mockServiceWorker.js in production intercepts real requests and serves fake availability, failing silently as a working-looking site with wrong data. Phase 4 now requires NODE_ENV gating, confirming the file is absent from built output, handling unregister for browsers that already loaded dev, and verifying real network calls in the panel. Mirrored into architecture.md beside the API contract. (3) HERO COPY was unassigned â€” only the Ketentuan was ever specified, so the Indonesian headline/subheadline/meta would have blocked Phase 2 or been invented at build time. Now 1b task 2, drafted in-house and user-approved via AskUserQuestion, TODO(phase2) if the client later wants their own wording. Placed in 1b because copy and type scale are decided together. (4-7) Drift from round 1: overview table 1b row omitted art direction while its own detail section called it the phase's key task; Product spec claimed to cover "Phases 1a-3" while containing the Phase 4 data model and API routes; Phase 3 journey check included wa.me without noting the number is a placeholder (would produce a false failure report); tech stack still said "Developer's own accounts for Phase 1".
[2026-08-07] [user] Every question to the user now goes through AskUserQuestion, never plain text â€” they want to click, not type. Added to global ~/.claude/CLAUDE.md (applies to all projects) and this repo's CLAUDE.md. Note: .claude/settings.json cannot express this â€” it only handles permissions/hooks/env, not behaviour. Exceptions: ExitPlanMode for plan approval (harness requirement) and pure free-form values (phone number, API key) which have nothing to enumerate.
[2026-08-07] [review] Phase plan reviewed, 7 fixes applied. (1) ART DIRECTION had no home in any phase â€” tokens were decided but layout language/type scale/section rhythm were not, so Phase 2 would have run /impeccable per section and produced five improvisations with no through-line. Now 1b task 1, written once, executed by Phase 2. (2) Phase 3 had no design skill and no gate while Phase 2 had both â€” a polished landing handing off to a plain form at the conversion point. Now has /impeccable + the same gate. (3) ACCESSIBILITY appeared in zero phases; now in 1a dev rules (baseline) and Phase 3 DoD (label association, aria-describedby, inputMode=tel, focus-on-submit, keyboard-operable upload). (4) check:setup connects to Neon+R2 which do not exist until the backend â€” removed from 1a, moved to Phase 4. (5) "Later" lumped mandatory backend with optional WA bot; backend promoted to Phase 4 with an explicit note that 1a-3 produce a site that looks finished but takes zero real bookings. (6) Added end-to-end journey check (landing -> wa.me -> /booking -> success) as Phase 3 task 6 via /qa. (7) /remotion-create was mislabeled as Phase 2's animation skill â€” GSAP does animation, Remotion only produces video assets if the 1b gate passed.
[2026-08-07] [user] Motion effects are the user's choice, not the agent's: every animation and micro-interaction (scroll reveals, hover/focus, parallax, magnetic CTAs, slot-cell transitions, marquee, page transitions) gets presented via AskUserQuestion BEFORE the code is written â€” implementing then asking turns a tweak into a rewrite. Guardrail so it does not become noise: batch by SECTION not per element (one call covering that section's reveal + hover + transition), use the preview field to show motion concretely, name effects precisely (animation-vocabulary skill does reverse-lookup), state each option's performance cost against the per-section Lighthouse gate. Explicitly NOT asked: prefers-reduced-motion fallbacks, which are mandatory. Written into design-system.md, arena-design skill, frontend.md.
[2026-08-07] [user] docs/references/ is now gitignored scratch, never pushed, and reference files get deleted once the work they informed is done (file stays available during the build, deleted after, with an explicit note naming what was removed). Consequence handled: since the file is never committed AND then deleted, findings must be written into docs/design-system.md BEFORE deletion or the reasoning dies with the image â€” a future session or the client's maintainer would otherwise inherit "invert the dark neon benchmark" with no benchmark. Agents told explicitly that an empty docs/references/ means references were consumed, not that none existed, so they read design-system.md as the durable record instead of concluding no benchmark exists. .gitignore keeps only the README tracked.
[2026-08-07] [user] Asset workflow set: when a design needs an image, write the prompt (target path, dimensions/format, ready-to-paste prompt, purpose) and hand it to the user, who generates it externally and commits the file. Never silently substitute a gradient or grey box to avoid asking. Icons are the exception â€” icon library, not AI generation, since generated icons drift in stroke weight/optical grid; library choice is a Phase 1a dependency decision. AI imagery restricted to abstract/decorative use only: a generated photo read as "the actual field" would mislead customers booking a venue they have not seen, so real venue photos stay TODO(phase2) pending the client. Written into design-system.md, arena-design skill, frontend.md.
[2026-08-07] [user] Motion stack swapped: GSAP + ScrollTrigger + @gsap/react REPLACES Framer Motion entirely (not alongside â€” two runtimes for one job is ~35KB redundant). Reason: pinned/scrubbed scroll timelines. Cost of the swap, now written into architecture.md gotchas: GSAP has no built-in prefers-reduced-motion, so ALL animation must route through a gsap.matchMedia() wrapper in lib/motion.ts and direct gsap.to() in components is banned; useGSAP() from @gsap/react required or animations leak on remount under React 19 Strict Mode; ScrollTrigger registers client-only and needs refresh on / <-> /booking navigation; GSAP license must be verified at install since this is a paid client project. ONE capped WebGL moment now permitted (hero only, dynamic import after order section interactive, static fallback, disabled under reduced-motion + saveData, <=40KB gzip, deletable in one commit) â€” amends hard rule 6. Performance budget written into architecture.md as the single source: <=200KB gzip initial JS, <=40KB lazy WebGL chunk, LCP <2.5s, Lighthouse mobile >=85. The 40KB cap is what excludes three.js (~150KB) and pixi.js (~140KB) â€” by arithmetic, not by name; use a GLSL fragment shader (~3-5KB) or OGL (~10KB) instead. anime.js rejected: no capability Framer/GSAP lacks. Hero video via /remotion-create DEFERRED to a Phase 1b gate with written pass criteria, decided BEFORE production so a rejection does not waste the render; fail case = /remotion-create makes off-site social assets only. /impeccable unchanged (taste, not a runtime).
[2026-08-07] [setup] Project skills were missing from the working tree; restored from 196f8dd via git checkout. NOT restored as-is â€” arena-gotchas still said "Phase 1 ONLY... Phases 2-3 are context, not tasks", which the frontend-first re-cut made false, and every agent is told to load that skill before touching source files. Rewrote its scope traps, added the /booking pricing OPEN DECISION pointer and the mock-layer rule. arena-design updated for GSAP + the WebGL exception + order-section wording. arena-database checked, no stale content.
[2026-08-07] [user] Phase plan refined after review. P1 split into 1a (engineering foundation) + 1b (design foundation) â€” different disciplines were hiding behind one done-checkbox. 1a gains two tasks: lock the API contract in architecture.md, and build an MSW mock layer + QueryClientProvider + axios instance, which is what stops P3's "integrate with API" from dead-ending. 1b: design system HTML IS the prototype, one artifact not two, so the UI is not paid for twice. P2 build order now layout -> order -> hero -> content -> footer (riskiest first; render order unchanged). P2 design skill narrowed to /impeccable alone â€” three direction-setting skills produced three directions. Per-section Lighthouse + reduced-motion gate instead of end-of-phase batch. Landing anchor #booking -> #order (stops shadowing the /booking route; English id per the code-is-English rule). axios wrapped by TanStack Query. Backend still deferred, but its agenda is now written in PRD (schema/datatypes, migration strategy, layered architecture, validation placement, presigned-URL upload). POST /api/bookings marked PROVISIONAL everywhere because presigned URLs will reshape it; GET /api/availability is firm.
[2026-08-07] [user] Phases re-cut frontend-first in PRD. P1 = frontend planning (architecture, scaffolding, DX, dev rules, design system analysis + HTML, prototype; skills /plan-eng-review /plan-devex-review /devex-review). P2 = landing page (layout, hero, content, order, footer; skills /impeccable /high-end-visual-design /design-taste-frontend, /remotion-create for animation). P3 = booking form at route /booking (layout, UI, validation, submission, axios). Backend + WA bot + deploy demoted to "Later phases â€” to be discussed". Form route renamed /form -> /booking. axios added to stack. OPEN: whether a rupiah amount renders on /booking (needs client rate card; would narrow the no-prices hard rule to "/ only").
[2026-08-07] [user] design-system.md/.html renamed to DESIGN.md/DESIGN.html and rewritten to the DESIGN.md format spec (stitch.withgoogle.com/docs/design-md) â€” the same spec impeccable's reference/document.md already implements, so no format conflict. Spec fetched and verified via /browse rather than assumed. SPLIT WAS THE REAL DECISION: the spec's 8 sections (Overview, Colors, Typography, Layout, Elevation & Depth, Shapes, Components, Do's and Don'ts) are strictly visual, and ~40% of the old file was process â€” motion-approval protocol, image-sourcing workflow, AI-imagery limits, asset locations, consulting order, landing content map. A straight conversion would have silently dropped rules that frontend.md and the arena-design skill quote verbatim. Process content moved to docs/design-process.md; DESIGN.md stays spec-pure so Stitch's linter, impeccable's live panel, and any other DESIGN.md-aware agent parse it. THIRD FILE: .impeccable/design.json sidecar (schemaVersion 2) carries what the spec's schema cannot hold â€” motion tokens, shadows, breakpoints, borders, the semantic layer, tonal ramps, 7 drop-in component HTML/CSS snippets, and the narrative (rules, dos, donts). NORTH STAR DELIBERATELY LEFT UNSET: document.md's Step 3 wants a Creative North Star, but art direction is Phase 1b task 1 and inventing one here would pre-empt the task the phase exists to run. Overview states what IS confirmed (inverse of the dark benchmark, inherited navy, Orbitron/Inter tension, low density for the mid-conversation organiser) and marks the north star pending; PRD task 3 now says task 1's direction fills it. SEMANTIC LAYER COMPROMISE: DESIGN.md frontmatter carries primitives only, because the spec forbids a token referencing another token in the same group â€” the semantic layer survives as a prose table in Colors plus extensions.semanticLayer in the sidecar, so it is not lost, just not machine-readable. 11 Named Rules extracted (Three-Layer, Status-Is-Information, Fluid-Not-Stepped, Fixed-Small, Tight-Display, Two-Scroll, Horizontal-Containment, Tinted-Shadow, One-Round-Shape, Visible-Unavailable, Focus-Is-Required) since Stitch's own outputs lean on them and they are stickier for AI consumers than bullet lists. DETECTOR RUN on DESIGN.html found and fixed a real one: `border-left:3px solid` on three callout cards â€” the side-tab accent is a top AI-UI tell and it contradicted the tonal-layering depth model the doc itself states; callouts now drop their shadow and fill grey-50, and DESIGN.md records the ban. Two detector findings ACCEPTED NOT FIXED: Inter flagged as overused (it is a locked PRODUCT.md brand commitment and impeccable's own rule is that the brief beats a saturation warning), and system-ui in fallback stacks (a fallback, not the design face). 9 files re-pointed: CLAUDE.md (now two rows, DESIGN.md + design-process.md), PRD.md, architecture.md (folder tree + the GSAP gotcha, which pointed at DESIGN.md for a rule that now lives in design-process.md), references/README.md, frontend.md, arena-design SKILL.md, arena-gotchas SKILL.md, inject-gotchas.ps1, DESIGN.html internal refs. PROGRESS.md history left with the old filenames on purpose â€” it records what was true when written.
[2026-08-07] [review] /impeccable polish on docs/DESIGN.html. Driven by the stored critique snapshot (22/40) read via critique-storage.mjs â€” worth noting the mechanism, because .impeccable/critique/ is gitignored and the previous session rewrote these same files without ever seeing it. TWO SCOPE CALLS BY THE USER. (1) EYEBROWS: craft-floor.md bans a kicker above a heading outright ('no brief earns it back'), while DESIGN.md carried an `eyebrow` typography token â€” but that token entered DESIGN.md via my own format-conversion pass at 0c7804d, not from the client, and craft-floor says the committed world overrides it while your own habit does not. Resolved by folding the label INTO the heading where it carried real information ('Layer 1 â€” Primitives' keeps the layer number, which is the page's whole thesis) and deleting it where it was decoration ('TYPE', 'FORMS'). 8 eyebrow elements -> 0; token removed from DESIGN.md frontmatter, the Hierarchy list, and sidecar typographyMeta; a Don't added in all three so it does not return. (2) SCOPE: defects plus bands, TOC/anchors, and the peak-end restructure. DEFECTS FIXED: .input--error synced to the red-800 border + red-100 fill decided in 3ce12fd (the render still had the 1.90:1 version); the past-slots callout, which still claimed elapsed slots need an API change â€” DESIGN.md now says the opposite, so it was factually wrong copy on a client-facing page; <html lang=en> with half the copy Indonesian, now 35 lang=id strings; .pill:disabled replaced with aria-disabled so unavailable dates stay in the tab order per the Visible-Unavailable Rule and the surface brief's explicit 'must not be copied'; slot selection moved from class-only into aria-pressed (9 controls) and made REVERSIBLE â€” clicking a selected slot now deselects instead of forcing another choice, which the critique flagged as User Control 2/10; 20 component rules bypassing the semantic layer now route through a full Layer 3 token block, measured 20 -> 0; dead tokens --space-1/--space-32/--shadow-md put to work and --dur-slow/--ease-in-out labelled production-only rather than left silently unused. STRUCTURE: --color-bg-subtle now actually renders alternating full-bleed bands, the one structural idea the critique said was declared and never executed; TOC with working section anchors; 'Butuh keputusan Anda' moved to the TOP as questions and an Indonesian lead paragraph replaces the developer disclaimer, so the page no longer opens on a caveat or closes on 'Not decided yet'. VERIFIED, not asserted: batched round at 1440x900 and 375x812, no console errors, no horizontal overflow (375/375), slot row 343x56px. THE LOAD-BEARING CLAIM RE-MEASURED: 'Menunggu Konfirmasi' renders 146px inside a 343px row, untruncated â€” time 120px + label 146 + gap + padding = 310, leaving 33px slack, so Orbitron's wide figures are affordable and the one-column argument holds. Detector: same 3 categories as before, no fourth, so no regression. Em-dashes rose 48 -> 83 but the split is 12 in CSS comments and 29 in body copy, most of those title separators ('Layer 1 â€” Primitives', 'Pending â€” 6.37:1') rather than prose interruptions; left alone since polish preserves incumbent copy. OUT OF SCOPE ON PURPOSE: the Alur journey frames (critique P0) belong to the exec tab's shape run â€” polish.md forbids smuggling a redesign in under a refinement command â€” and slot-surface scannability (amber-100 is 1.11:1 on white) is a token change the critique assigns to /impeccable colorize.
[2026-08-07] [review] TWO SESSIONS WERE EDITING THIS REPO CONCURRENTLY ALL DAY and it produced real defects â€” logged so the working rule that follows is not lost. Sessions 9ef1af43 (doc review, Vitest decision, DESIGN.md format conversion) and 67d9c864 (PRD/architecture rewrites, PRODUCT.md, /impeccable runs). Proof rather than suspicion: the /impeccable critique run started 08:29Z and finished 08:56Z, the DESIGN.html rename landed ~08:50Z, and the critique header records catching it â€” 'Assessment A read the pre-rename file, B the post-rename file'. ROOT CAUSE: .impeccable/critique/ is gitignored, so a 137-line graded assessment of these exact files was invisible to git log and to the session rewriting them. Two defects reached commit 0c7804d as a result. (1) CONTRAST RATIOS PRINTED AS USER-FACING LABELS WERE WRONG â€” 7.4:1 and 7.9:1 carried forward unverified from the old file; computed values are 6.37:1 and 6.80:1, overstated ~16%. Both still pass AA so no design decision changed, but this artifact's whole credibility strategy is printing falsifiable numbers, and two unsourced figures make every other number on it suspect. Corrected in cd7decb; all values recomputed rather than adjusted. (2) A WCAG 1.4.11 FAILURE WAS WRITTEN INTO THE DESIGN AUTHORITY: .input--error used a red-300 border as its ONLY visual signal at 1.90:1 against a 3:1 floor, the field stayed white, and the DESIGN.md rewrite promoted it from an HTML detail to a spec line plus sidecar component CSS â€” so the defect would have shipped into Phase 3's /booking form at the conversion point. Fixed: red-800 border (8.31:1) plus red-100 fill, so the state depends on neither a single property nor hue alone. Recorded as the Visible-Boundary Rule in DESIGN.md and the sidecar. THIRD FIX, from the /impeccable shape brief that landed mid-commit: DESIGN.md said elapsed slots could not be distinguished from booked ones without a `past` status in the API contract, and instructed 'do not design around it before the contract changes'. WRONG â€” the client already knows the current time and TIME_SLOTS, so it derives elapsed itself. GET /api/availability needs no `past` status and stays FIRM. The brief chooses a collapsed 'Sudah lewat (N)' group over nine rows labelled 'Terisi'; DESIGN.md now points at it. NOTE FOR WHOEVER TOUCHES THEM NEXT: PRODUCT.md still carries this as an unresolved 'Conflict to resolve' and architecture.md still frames the past-slot override as final â€” both are now stale, left alone deliberately because session 67d9c864 owns those files and was live. WORKING RULES ADOPTED: (a) read .impeccable/critique/ before editing any design artifact â€” being gitignored is exactly why it gets missed; (b) never carry a printed measurement forward without recomputing it; (c) one repo, one writing session at a time, or commit before handing off.
[2026-08-07] [review] architecture.md reviewed, 5 gaps fixed. (1) API CONTRACT ONLY LOCKED RESPONSES â€” the POST request body was never written despite task 5 promising "exact request/response JSON", and step 2 of the flow referenced a honeypot whose field name existed nowhere. Three consumers (MSW handler, Phase 3 form, Phase 4 route) would each have guessed independently. Request table now written: date, slot, teamName, phone, notes, proof, website(honeypot); the 400 `fields` keys are these same names; honeypot responds fake 201 rather than 400 so a bot learns nothing. (2) LAZY EXPIRY IS A WRITE INSIDE A CDN-CACHED GET â€” flagged UNRESOLVED, not fixed, because the fix is a Phase 4 design call. A cache hit never reaches origin, so the only mechanism freeing abandoned slots is starved exactly when traffic is low; a pending booking due to expire at 03:00 on a quiet night holds the slot until someone next misses the cache. No error, no log, no failing test â€” it just costs the client bookable hours. Also breaks GET-is-safe (prefetch, link scanners, back-nav). Three candidate fixes written (cron / on-POST / drop s-maxage); PRD's "lazy expiry acceptable, no cron required" and "already locked, carried in unchanged" both amended since they contradicted the new open question. (3) STATUS MAPPING MISSING â€” DB has 4 states (pending|confirmed|rejected|expired), API has 3 (available|pending|booked), mapping written nowhere. rejected/expired -> available is the half that gets guessed as `booked`, which blocks genuinely-open slots with nothing erroring. Table added, tied to uniq_active_slot's WHERE clause which defines the same two active states. (4) ORPHANED R2 PROOFS â€” upload precedes insert, so a crash between them leaves a file no row points at; the 409 path's deleteProof() cannot run in a process that died. Nothing in the system can detect it and the bucket is handed to the client. Fix is an R2 lifecycle rule on proofs/ (dashboard, not repo â€” added to the Phase 4 agenda so it is not lost at handover). (5) BUDGET TABLE CONTRADICTED ITS OWN PROSA â€” table said headroom 26 while the paragraph below said 18-21; 174+26+5..8 = 205-208, already over the 200KB ceiling before any component. Table reordered so dates+icons sit above the subtotal; headroom now reads 18-21. Also added: check:budget must ship with the numbers in task 8, since a budget nothing measures is a wish. Removed the struck-through `@/` gotcha (retired rule cluttering a live gotcha list; history is in this file) and replaced it with the missing `server-only` entry â€” it is the mechanism that makes hard rule 4 fail the BUILD instead of relying on memory.
[2026-08-07] [user] Verification harness switched from hand-rolled Node scripts to Vitest. Old plan: scripts/check-lib.ts + scripts/check-setup.ts run via `node --experimental-strip-types`, chosen for zero dependencies. Hidden cost that killed it: plain Node cannot resolve the `@/` alias, so EVERY lib/ module was forced to import siblings by relative path with explicit .ts extensions, plus allowImportingTsExtensions in two tsconfigs â€” production import style bent to suit a test harness. Vitest resolves `@/` via tsconfig paths, so that constraint is retired entirely; also gains per-test isolation (one failing assertion no longer aborts the run), failure diffs, and watch mode. check:setup ALSO goes through Vitest rather than staying a plain script â€” otherwise the `@/` restriction survives in lib/db/ and lib/storage/ and the whole trade buys nothing. Kept as separate globs: `check:lib` = `vitest run lib` (no credentials, ever), `check:setup` = `vitest run scripts` (needs .env.local, Phase 4). Tests colocated as lib/*.test.ts. Hard rule 8 reworded. Updated: architecture.md (verification section, lib/ import convention, `@/` gotcha struck through not deleted, folder structure, versions table), PRD.md, CLAUDE.md, backend.md, eng-lead.md, arena-database skill.
[2026-08-07] [user] lib/slots.ts + lib/dates.ts moved from Phase 2 into Phase 1a (now task 6, mock layer becomes task 7). Two reasons: the 1a MSW handlers must return realistic availability, and a mock hardcoding its own slot strings is a second source of truth that drifts from TIME_SLOTS silently â€” drift only surfacing when the real backend lands. Second, it stops check:lib shipping in 1a as an empty harness that proves the runner works but asserts nothing. Phase 2's order section then consumes already-tested primitives. 1a DoD gained a checkbox requiring the tests actually assert, and requiring mock data be derived from TIME_SLOTS rather than hardcoded.
[2026-08-07] [user] Admin app removed from this repo's scope entirely. arena-player-web = public site only (landing, /form, availability API). Admin goes to a separate repo (arena-player-admin) sharing the same Neon DB + R2 bucket. Phase 3 here is now deploy + handover only. No auth, no admin routes, no monorepo. Updated: PRD.md, CLAUDE.md, architecture.md, eng-lead.md.
