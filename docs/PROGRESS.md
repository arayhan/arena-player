# PROGRESS — shared agent log

Cross-agent communication file. Every agent reads this before working, appends after.
Format (caveman-compact): `[YYYY-MM-DD] [agent] [what] [reason]`

Agents: pm | lead | engineer | reviewer
Older entries use the retired six-agent tags (instructor, eng-lead, senior, fe, be) — left as written.

**This file holds the current phase only.** Earlier entries live in
[progress-archive/](progress-archive/) — read those only when tracing _why_ an older
decision was made. What is currently **true** lives in [PRD.md](PRD.md),
[architecture.md](architecture.md), [DESIGN.md](DESIGN.md), and [PRODUCT.md](PRODUCT.md);
this log records how it got that way, not what the rules are.

Archive when a phase closes, so this file never becomes a tax on every session.

---

## Phase 1b — Design foundation

[2026-08-08] [user] CLIENT DIRECTIVE recorded: "minimalist UI, but modern — with many animations, transitions, and micro-interactions." Written into DESIGN.md Overview as an INPUT to Phase 1b task 1, explicitly NOT as a north star — the Overview still says the north star is unset, and promoting a client sentence into one would pre-empt the task the phase exists to run. TENSION RESOLVED as minimal FORM, rich BEHAVIOUR: few elements each responding precisely, on the reasoning that restraint is what makes dense motion legible — the same effects on a busy layout read as noise. "Minimalist" confirmed to mean few elements + whitespace, NOT reduced colour; the monochrome reading would have collided with the status triples, which are an accessibility requirement (pending 6.37:1, booked 6.80:1) rather than decoration. SCOPE SPLIT, recorded as a rule not a preference: expressive motion in hero / content / section transitions; the ORDER SECTION gets functional feedback only (state change, selection, elapsed collapse). Exempt for a stated reason — it is where the booking happens, where the per-section Lighthouse gate bites hardest, and where Product Principle 4 applies most sharply, since the organiser is deciding fast with 8-12 people waiting. Motion that delays that decision fights the outcome the client is paying for, so withholding it there is the directive being honoured rather than ignored. EXPLICITLY UNCHANGED and stated in the doc, because this is the half that gets forgotten: every effect is still user-chosen via AskUserQuestion before code (the directive raises the ceiling, pre-approves nothing), everything still routes through lib/motion.ts (more motion = more reduced-motion surface, not less), and no-CLS plus the performance budget are untouched — on a mid-range Android in an in-app webview the binding cost is CPU per frame, so an effect can pass the KB budget and still fail the Lighthouse gate. Also added to PRODUCT.md Brand Commitments as stated-by-client with a pointer to DESIGN.md for the resolution (not restated, to avoid a duplicated-rationale drift surface), and as question 5 on the 1b client gate so the deliberate absence gets confirmed rather than read as a shortfall.

[2026-08-07] [review] Client checkpoint 1b prepared — the DoD item that had never happened. Two artifacts. (1) ALUR section added to DESIGN.html, closing the critique's outstanding P0: the page was a component inventory when PRD 1b defines tasks 4+5 as one artifact whose handlers prove the landing -> order -> form journey. Three frames at the real 375px width: order section (today preselected, scarcity line, collapsed "Sudah lewat (6)", bookable slots), the literal WhatsApp message the CTA produces, and the form outcome INCLUDING THE 409 — which was rendered nowhere despite being the highest-stakes moment the product has and the entire reason uniq_active_slot exists. Built to the confirmed brief in .impeccable/surfaces/app-page-tsx.md. No motion, per the ask-before-animating rule and because static frames are correct for a walkthrough. (2) docs/checkpoint-1b.md — the record that makes it a checkpoint rather than a conversation, since delivery is a live screen share where nothing otherwise survives the meeting. Pre-written questions ordered by what they block: WhatsApp-only handoff (BLOCKS PHASE 2, and the client has never been asked despite the whole flow being redesigned around it), pricing (BLOCKS PHASE 3), the six missing content items, traffic channel, single-field confirmation. Plus a sign-off line. TWO SELF-INFLICTED FINDINGS caught by re-running the detector: I hardcoded #E7FFDB for a WhatsApp-green bubble — raw hex outside the palette on the one page whose own rule forbids it, and a brand colour that is not Arena Player's; replaced with --color-bg-subtle. The border-left I used instead then tripped the side-tab slop rule; dropped. Detector back to the 3 known false positives, no new findings.
[2026-08-07] [setup] PROGRESS.md split. All 38 planning-phase entries moved verbatim to progress-archive/00-planning.md; nothing rewritten, only relocated. Reason: the file had reached 44 KB and arena-gotchas plus all six agent files instruct agents to read it before working — roughly 11k tokens of context tax per agent per session, growing unboundedly. Deliberately did NOT distil "standing decisions" into this file: those already live in PRD/architecture/DESIGN/PRODUCT, and restating them would create a fourth drift surface, which this project has already paid for three times.
[2026-08-07] [review] /impeccable redesign on docs/DESIGN.html. Concept-seed roll (key 7a4384e8, mode read) dealt index 4, 'Papan Jadwal' — an Indonesian enamel signage world. USER PINNED THE CHALLENGER instead: brick build-instruction book. new-work.md permits this explicitly ('a user- or brief-pinned direction beats the roll, always'); the roll's job was to stop the run converging on my own top-ranked pick, and it did. Two challengers disposed of on record: Ikeda datamatics cannot honour a locked commitment (pure black-and-white contradicts navy #011A43 AND the client-named light/blue-white anti-reference — a factual failure, not taste), and the hand-drawn zine went to the re-roll pool (cream-plus-marker is the rendition default new-work.md warns about, and hand-drawn undercuts a document whose credibility rests on printing verifiable numbers). THE FUSION: every colour keeps both its instruction-book role and its product meaning. Ground = blue-50, keyline ink = the client's logo navy, inventory tags = amber-300. The source world's 'brick red for the new part' maps to Signal Blue because RED ALREADY MEANS BOOKED here — taking it literally would have collided with a state meaning, and clarity wins conflicts. STRUCTURE follows the form natively: parts inventory first, nine numbered assembly steps, the Alur journey as the finished model. The four open decisions sit BEFORE step 00, which the reviewer called the best structural decision on the page. BLOCKED ON A COLLISION FIRST: the other session had 152 uncommitted lines of Alur work plus a brand-new checkpoint-1b.md whose walkthrough script names this file section by section, and whose sign-off options end with 'Redirect — direction rejected, return to Phase 1b task 1'. A redesign IS a Redirect, and the checkpoint exists to let the CLIENT trigger one. Raised before touching anything; user chose to proceed and carry Alur forward. Their work committed verbatim at ff8f2a7 first so a wholesale rewrite could not destroy it. TOKEN CHANGE THAT ESCAPED THE DOCUMENT: grey-500 computes 4.44:1 on the blue ground and fails AA, so --color-fg-muted moved to a hue-tinted navy-400 (6.94:1 white, 6.38:1 blue). Strictly better on white too. FINISH REVIEW ran fresh with no inherited context and returned REWORK: the page argued a three-layer indirection its own stylesheet did not practise — 17 Layer 3 tokens skipped Layer 2 (there were NO semantic state tokens at all) and the worked example printed on the page was a two-hop chain described as three. Also: clicking a slot in Alur cleared the step 04 demo (toggleGroup fell back to document); frames claimed 375px and rendered 335px; selected pill day label 4.20:1; the guide arrow drew in a 27ms flash inside an authored 420ms because the box was 1068px holding 202px of centred ink; the checkmark was a Unicode glyph that fell out of Archivo into the platform sans; and nothing in the system had a loading, submitting, empty or failed state. Fixed all nine, plus fired --new-part which had a token, a comment justifying it, and zero applications — the reviewer's judgment was that its absence, not the hue remap, is what drained the world. SECOND VERDICT: conditional, with TWO REGRESSIONS FROM MY OWN FIX BATCH. The frames grid I 'fixed' put 1189px of content in a 1068px column, scrolling the page horizontally at 1240-1327px and clipping the 409 panel — the one thing checkpoint-1b.md calls the most important on the page. And .btn[aria-disabled=true] had no CSS rule at all, so the submitting state was byte-identical to idle, directly beneath a paragraph explaining why aria-disabled is correct. Both fixed and verified by objective measurement (no horizontal scroll at 375/1280/1300/1440; submitting computes grey-200 vs navy idle) but NOT re-scored by the reviewer. STILL OPEN by agreement: FIRST VIEWPORT (inventory on the second scroll), native disabled on Kirim, unstyled ::placeholder at 2.35:1, 'Sudah lewat (6)' expanding to 2 rows, and the expanded elapsed rows rendering in the booked red triple while labelled 'Sudah lewat' — which contradicts step 04's own 'elapsed hours are not booked hours' and is pre-existing, not from this build. DISCLOSED: this install ships neither impeccable-finish-reviewer nor impeccable-documenter nor a degraded/ reference. The review ran in a general-purpose agent with no inherited context (the property that matters); the documenter pass ran in-thread.

## Phase 1a — Engineering foundation

[2026-08-08] [build] Step 02 scaffold. Next 16.3.0 + React 19.2.8 + Tailwind v4 + TS 5.9.3, 25 packages pinned exactly, `pnpm dev` serves 200 with `<html lang="id">`, typecheck and lint clean. THE BUDGET IS BREACHED AND THE ESTIMATES WERE 30% LOW. Measured `/` on a modern browser (excluding the 38.7KB `noModule` polyfill legacy browsers only): framework 126.5KB against an estimated 90, GSAP+ScrollTrigger 43.6 vs 35, axios 17.5 vs 13, TanStack Query 10.0 vs 13, date-fns+tz 8.1 vs 4-6, react-icons 2.2 vs 1-2, zustand 0.7 vs 1. Subtotal 208.6KB against a 200 ceiling WITH ZERO COMPONENT CODE, where the plan assumed ~18-21KB of headroom. TWO THINGS WENT RIGHT: react-icons, flagged as a barrel-tree-shaking gamble, came in at 2.2KB for six icons so the extract-the-glyphs fallback was never needed; and React Compiler, which Next 16 enables by default, costs 0KB and stays on. USER CHOSE the 61KB fix: GSAP moves behind a dynamic import in `lib/motion.ts` (-43.6, a one-file change because hard rule 6 already funnels every animation through it, at the cost that a hero ENTRANCE must be CSS since nothing can animate before the chunk lands), and axios becomes a `/booking`-only dependency (-17.5, since `/` makes one GET that native fetch handles in 0KB, while `/booking` keeps axios for `onUploadProgress` on the 2MB proof upload which fetch cannot report). `/` now sits at ~147.5KB with ~52KB of real headroom. axios joins zod and react-hook-form under the same route-split rule.

[2026-08-08] [build] Step 02 also surfaced FIVE TRAPS worth not re-learning. (1) Next 16 REMOVED the per-route First Load JS table that Next 15 printed, and Turbopack emits no app-build-manifest.json, so step 08's planned `check:budget` had no output left to parse; `--experimental-analyze` prints a route count and nothing sized. Replaced with `scripts/measure-bundle.mjs`, which gzips `.next/static` and splits shared from route chunks using build-manifest.json. More honest anyway — it counts what ships. (2) `pnpm view <pkg> version` returns the `latest` DIST-TAG, and sorting the versions array by publish order returns something else entirely: it reported react 19.0.8, which is the `backport` tag, not 19.2.8. That same read caught next@15.5.23 sitting on `backport` — Next 15 is in maintenance — which is why this project is on 16. (3) `latest` is not always SUPPORTED: TypeScript's latest is 7.0.2 and ESLint's is 10.8.1, but create-next-app@16.3.0 pins ^5 and ^9; the peer ranges permit the newer majors and permitting is not testing. Took Vercel's combination. (4) pnpm's `minimumReleaseAge` policy rejected react-hook-form@7.85.0, published hours earlier — fixed by taking 7.84.0 rather than relaxing the policy, and editing package.json was NOT enough because the lockfile held the old resolution until `pnpm clean --lockfile`. (5) The icon probe route was first written as `app/_probe/` and SILENTLY MEASURED NOTHING — App Router treats a leading underscore as a private folder and excludes it from routing. The build succeeded, the route table simply had no probe in it, and the totals were identical. Caught by reading the route table rather than trusting the number.

[2026-08-08] [build] Step 01 (fc28921). Both open library choices closed: date-fns v4 + @date-fns/tz, and react-icons — chosen because no mainstream set still ships a WhatsApp brand mark and six glyphs are needed. Route split given a MECHANISM instead of an intention: structure (form libraries only under app/booking, app/api and lib/validation.ts, where route handlers are server-side so zod costs the client nothing), an ESLint no-restricted-imports zone rule that fails at author time with a readable message, and per-route check:budget as the backstop for transitive imports lint cannot see. CONSEQUENCE NAMED RATHER THAN DISCOVERED: choosing a date library over native Intl means lib/shared/dates.ts imports date-fns, and lib/shared/ is copied byte-identical into the admin repo — so that repo is now obliged to install it. check-shared.mjs therefore diffs the shared peer dependency's version range as well as the file bytes, because date-fns v3 and v4 differ in exactly the timezone API this relies on and two repos on different majors would produce a byte-identical dates.ts computing different dates.

[2026-08-08] [review] Pre-flight (commit 7a7dbeb). FOUR CONTRADICTIONS found in documents marked locked, all of which would have been discovered with code already written against the losing side. `notes` said 280 in the API contract and 500 in both the PRD field list and the notes_length SQL constraint — 500 won, since two sources agreed and one of them was the database. The folder tree still listed dates.ts/slots.ts/validation.ts at lib/ top level, contradicting the lib/shared/ contract added ONE COMMIT EARLIER: that commit's grep used the pattern `lib/dates`, which never matches a tree line written `│   ├── dates.ts`. The shared-contract table gave validation.ts the proof upload constraints while two other files said lib/proof.ts — resolved toward lib/proof.ts, because the admin reads proofs and never uploads them. Task numbers drifted three ways, including architecture.md calling the budget "task 7" and "task 8" three paragraphs apart. The Phase 1a DoD also had NO checkbox for task 8 at all, so the measured budget, check:budget and lib/motion.ts could have been skipped with every box ticked. Board completed to 8 step files; step 05 marked verify-don't-rewrite because the API contract was already written during planning.

[2026-08-09] [build] Step 02b — RESTRUCTURE to src/ + feature-driven modules, inserted between steps 02 and 03 at the user's request because the flat layout was unfamiliar to work in. Done now because it was the cheapest it would ever be: app/ held three files, components/ lib/ mocks/ were empty directories, and the admin repo still has zero code. It had to precede step 03, which authors the ESLint route-split zones and check:docs — both hard-code paths, and writing them against the old layout then rewriting them is the mechanical-edit defect class this repo has lost time to three times. NEW SHAPE: src/{app,modules,domain,server,services,components,lib,utils,mocks}. Modules are named after SURFACES — home renders /, booking-form renders /booking — and NEVER import each other; src/lib/ is narrowed to polish for installed libraries (cn, motion, query-client) and src/utils/ holds web-only helpers (error, formatter). db/ stays outside src/ because it is SQL run by hand, not source anything imports.

[2026-08-09] [decision] The frozen shared layer is now `src/domain/`, four files instead of three: slots.ts, dates.ts, status.ts, phone.ts. Took four rounds of naming to land. `shared/` was rejected because once src/modules/ exists, "shared" reads as _shared between modules_ and attracts the first cross-module button component into a folder that can never accept one. `constants/` was rejected because two-thirds of the contents are functions. `utils/` and `helpers/` were rejected because those are the names you reflexively want LATER for web-only helpers — spending them on a frozen folder sends the reflex to the one place that cannot accept it. The proposal to move it into src/modules/booking/domain/ was rejected on three counts: / needs TIME_SLOTS and isPastSlot for the order-section grid so home would cross-import booking; the ESLint zod zone stops being expressible as a clean folder boundary the moment / imports from the booking module; and the admin repo would have to invent a modules/booking/ folder for a surface it does not have, with its natural module being `bookings` plural anyway. Modules are named after surfaces, the domain after the business — different axes, which is why the module was renamed booking-FORM: / is also about booking, and one word meaning two things is what caused the confusion.

[2026-08-09] [decision] validation.ts SPLIT into status.ts + phone.ts, because half of it was vocabulary rather than validation — the same name-vs-contents mismatch that disqualified `constants/`. status.ts carries the two status vocabularies, ACTIVE_STATUSES mirroring uniq_active_slot's WHERE clause, and toSlotStatus() encoding the 4→3 mapping that architecture.md calls the half that gets guessed wrong. THREE OF THE FOUR DOMAIN FILES HAVE NO DEPENDENCIES and must stay that way: only dates.ts imports date-fns, so importing TIME_SLOTS costs no date library — and every package added there is one the admin repo is obliged to install. Merging slots into dates was considered and rejected for exactly that reason. src/domain/ also imports its own siblings RELATIVELY (./slots, never @/domain/slots), the one documented exception to @/-everywhere, so a byte-identical copy resolves the same in both repos regardless of either tsconfig.

[2026-08-09] [build] cn() added with clsx 2.1.1 + tailwind-merge 3.6.0, both resolved by dist-tags rather than by sorting versions — the trap that already produced two wrong pins here. MEASURED BEFORE APPROVING, three builds with each probe isolated against a control route so the "use client" boundary is subtracted rather than blamed on the library: control 0.5KB, clsx alone 0.2KB, clsx + tailwind-merge 8.2KB, so tailwind-merge accounts for 8.0 of it. User chose to keep both; the clsx-only fallback is measured and is a one-file swap since cn()'s signature does not change. Budget subtotal on / is now 155.7KB against the 240 ceiling, headroom ~84KB. Probe routes deleted in the same session.

[2026-08-09] [gotcha] `next build` REGENERATED .next/dev/types/validator.ts still pointing at ../../../app/page.js after the move, so typecheck failed on a file no longer in the tree while the build itself reported "Compiled successfully". Deleting just .next/dev was not enough — the next build recreated it from cached state. Clearing all of .next fixed it. Worth knowing before concluding a move broke the code: the compile succeeded the whole time.

[2026-08-09] [gotcha] The scripted path sweep introduced a defect of its own: lib/db/ and lib/storage/ both map to src/server/, so code-reviewer.md ended up reading "confirm it is imported in `src/server/` and `src/server/`". Caught by re-grepping rather than by trusting the sweep's own success output. Every replacement pattern carried a (?<!src/) lookbehind to prevent src/src/ double-prefixing, and a repo-wide grep for src/src/ confirmed none — but the lookbehind guards the wrong-path failure, not the collapsed-distinction one. Two files were also missed by the first sweep list entirely (PRODUCT.md, and one line in the database skill), found by a second grep pass.

[2026-08-09] [build] Step 03 — DX harness (d19b72f, 38932a9). prettier 3.9.6 + eslint-config-prettier 10.1.8 (dist-tags, pinned, dev-only, 0KB), vitest.config.ts carrying the SAME @/ -> ./src alias as tsconfig because a drift there makes tests assert against a file the app does not ship, check:lib = `vitest run src`, and check:docs with seven checks. EVERY CHECK PROVEN TO FAIL THEN REVERTED: check:docs on a planted TODO(phase2), a planted module barrel, a planted cross-module import, and an undeclared TODO(content) category; eslint on @/server from a module, axios on the landing module, the @/ alias inside src/domain/, and MSW v1 rest.get — plus the negative case, zod inside src/server/ staying CLEAN, which proves the allow-list works rather than the rule being off. check:lib verified with .env.local moved aside.

[2026-08-09] [gotcha] THE STOP HOOK FAILED ITS OWN FIRST TEST — exit 0 on a real violation, the exact never-fires defect this repo already shipped once, caught only because the test existed. Cause: check-docs.mjs wrote findings to stderr, and PowerShell 5.1 wraps every stderr line from a native command in a NativeCommandError; under ErrorActionPreference=Stop that is terminating, the hook's own catch swallowed it, and it exited 0. Fixed at the source rather than the symptom — findings go to STDOUT, the exit code carries pass/fail, and no 2>&1 is needed anywhere. Re-tested all three paths: clean exits 0 silently, a violation exits 2 with a readable message, stop_hook_active exits 0 without recursing.

[2026-08-09] [decision] check:docs asserts NO ORPHANS on TODO(content), not all-six-present. Only one of the six declared categories has code today; the other five mark content that arrives in Phase 2+, so all-six-present would have shipped red or been quietly deleted. It also distinguishes a marker from prose ABOUT a marker: docs write `TODO(phase2)` in backticks and "Phase 1" in quotes when naming what they forbid, so prose is scanned with code spans, fenced blocks and quoted spans stripped, and marker checks only read files that can carry a real marker. The first run reported ~40 findings, nearly all of them the checker matching its own patterns. NO --passWithNoTests: check:lib was red with zero tests, and a check that passes when it ran nothing is the antipattern this project keeps warning about — cn() is non-trivial shipping logic so per hard rule 8 it earned a real test instead.

[2026-08-09] [gotcha] TWO THINGS THE NEW CHECKS CAUGHT ON THEIR FIRST REAL RUN. database.md said "once the Phase 1 build starts" — bare Phase 1, fixed to 1a. And src/lib/cn.test.ts failed on MY OWN wrong assumption: tailwind-merge treats flex and block as one display conflict group, so cn("flex","block") is "block", not "flex block". Pinned as a test since it is the non-obvious half of what the 8.0KB buys. Separately, `git add -A` swept in files prettier had reformatted that are not ours to format — all 398 lines of .impeccable/design.json, which /impeccable document regenerates, so the diff would flip-flop forever. Restored from 7f58728 and added .impeccable/ plus docs/progress-archive/ to .prettierignore. PROGRESS.md deliberately stays formatted: it is appended every step.

[2026-08-09] [build] Step 04 — docs/dev-rules.md. No code. Named lowercase-kebab to match architecture.md, database.md and design-process.md: the UPPERCASE names in docs/ are externally fixed (PRD is a standard acronym, DESIGN.md is mandated by the Stitch format spec) and everything the project named for itself is kebab. 14 references across both repos already said dev-rules.md, and after four missed sweeps this session, casing is not worth a fifteenth. Content is deliberately the layer BELOW CLAUDE.md — conventions whose violation costs a review comment, not a phase — and it restates nothing: where another doc is the authority it is linked. The four ambiguous folder pairs each get a one-question test, the sharpest being "does this file exist because of a package in package.json?" for lib/ versus utils/. Accessibility baseline is the part with teeth because nothing fails when it is skipped: the 409 focus move is called out specifically, since without it a screen-reader user never learns their slot was taken. Added to CLAUDE.md's docs table (121 lines now, still under the ~95-gets-read/300-gets-skimmed threshold it names).

[2026-08-09] [gotcha] Step 04's own acceptance grep was stale in the same way step 03's was: it searched for "never imports from", which is pre-02b phrasing from when the rule read `lib/ never imports from app/`. Replaced with three greps covering all three current import rules. That is the fifth instance this session of a check or doc still describing the layout that existed before the restructure — every one found by reading, not by a command, which is precisely why check:docs now exists.

[2026-08-09] [reviewer] Checkpoint over d19b72f/38932a9/4ff1f4d before step 06 writes src/domain/. Verdict FIX-FIRST, and the timing was the point: four files were about to be written against guards that did not hold. FOUR REAL GAPS, each confirmed by probe rather than by reading. (1) The src/domain/ ban was ALIAS-ONLY — `@/lib/cn` errored, `../server/db` linted clean, in the one folder whose documented house style IS relative imports, so the wrong import is a single keystroke from the right one; and the failure is invisible twice over, because the admin repo has no src/server/ or src/utils/, meaning such a file stays byte-identical (check:shared passes) and simply does not build over there. (2) Import rule 1 — nothing under src/ imports from src/app/ — was enforced NOWHERE, while CLAUDE.md and dev-rules.md both said "three import rules, all lint-enforced". src/domain/ got it incidentally through the @/* ban; modules, lib, utils, components and services got nothing. A doc overstating a guard is the failure class this repo has already paid for. (3) Relative cross-module imports passed BOTH named guards: eslint matches `@/modules/*` and check-docs check 5 was a literal `line.includes("@/modules/" + other)`, so `import { schema } from "../booking-form/booking-form.schema"` inside src/modules/home/ was clean on lint AND check:docs — precisely the import CROSS_MODULE's own message says ships zod to /. (4) The booking-form block hardcoded `@/modules/home` instead of the CROSS_MODULE wildcard, so it alone would exempt a third module the day one exists. Also dismissed one suspicion on the record so nobody "fixes" it later: the domain block's bare `["@/*"]` looks like it should miss two-segment `@/domain/slots`, but eslint's pattern matching crosses `/` and it errors at both depths — the two-pattern spellings elsewhere are belt-and-braces, not a template.

[2026-08-09] [build] Guard fixes from the checkpoint, every ban re-probed with `eslint --stdin --stdin-filename` on both sides. ESLINT: new APP group bans `@/app`+`@/app/*` repo-wide, with src/app/** overriding it to nothing (app composes itself — a layout importing its page is the boundary working) and src/server/** keeping it, since server sits BELOW the extraction boundary while a route handler does not; the old combined `src/app/api/** + src/server/**` block is split for that reason. src/domain/ gains a `["../*"]` group — `./slots` unaffected, any `..` rejected. booking-form takes CROSS_MODULE instead of the home literal. Six must-error cases fire and four must-be-clean cases stay clean, including api importing @/server + zod and booking-form importing zod + axios. CHECK:DOCS now 10 checks: check 5 resolves relative specifiers with posix.normalize and reports cross-module-import, app-boundary and domain-escape, which is the half no glob can express — banning `../*` outright would break `home/components/x.tsx` importing `../home.service`, which is correct and routine. Plus unbalanced-fence, because the fence toggle FAILS OPEN and a stray ``` silently disabled checks 1-3 for the rest of a file. SHIPS gains .html so docs/DESIGN.html is scanned for markers — it was in neither SHIPS nor isProse, and hero copy is one of the six TODO(content) categories. Check 7 now scans .claude/agents/ and .claude/hooks/ as its own comment always claimed, and matches integer ratios (3:1 is the WCAG 1.4.11 bar and was invisible to a decimals-only pattern). vitest include widened to .test.{ts,tsx} so a Phase 2 component test cannot be silently uncollected while check:lib reports green.

[2026-08-09] [gotcha] TWO OF MY OWN FIXES WERE WRONG ON FIRST RUN, both caught by probing rather than by reading. unbalanced-fence reported three times per file because checks 1, 2 and 3 each walk the same file through scannable(); deduped with a module-level Set. And the ratio comparison was `flatDesign.includes("3:1")`, which is TRUE for any doc containing "14.53:1" — substring matching quietly accepts every integer ratio that shares digits with a decimal one. Replaced with set membership over ratios parsed from DESIGN.md by the same regex. The first probe of that check passed against a value DESIGN.md does not contain, which is exactly the never-fires shape this repo keeps re-learning; re-probed with 9.99:1 and 7:1 and both are now reported.

[2026-08-09] [decision] The "where does a thing go" table in dev-rules.md is now ORDERED, because unordered it mis-answered one of the four files step 06 was about to write. `dates.ts` exists because of date-fns AND the admin repo needs it, so the lib-vs-utils question — the one the step 03 commit called the sharpest — sends a frozen, byte-identical file to src/lib/ when asked first. Three of the four domain files hit the same contradiction more mildly. The admin question now comes first and stops the walk, on the reasoning that it is the only one whose wrong answer is a contract in two repos rather than a move. Also corrected eight folders to nine (src/mocks/ was never accounted for) and added check:shared to the verification table before it goes stale one step from now.

[2026-08-09] [build] db/migrations/20260809_create_bookings.sql — the folder had been empty since the repo started while database.md said the file lived there. Copied byte-identical from the SQL block in database.md and asserted so with a round-trip comparison, not by eye. Nothing runs it; it is still pasted by hand into the Neon SQL editor. It exists so ACTIVE_STATUSES has a real artifact to be checked against rather than a paragraph.

[2026-08-09] [build] Step 06 — src/domain/ is no longer a .gitkeep. slots.ts (TIME_SLOTS, canonicalSlot, slotStartHour), dates.ts (todayInJakarta, bookingWindow, isBookingDateString, isWithinBookingWindow, isPastSlot), status.ts (the two vocabularies, ACTIVE_STATUSES, toSlotStatus) and phone.ts (normalisePhone, isValidIndonesianMobile), 40 tests passing. THREE OF FOUR STAY DEPENDENCY-FREE as required; only dates.ts imports date-fns + @date-fns/tz, and it imports its sibling RELATIVELY (./slots) so the copy resolves in both repos. THE TZDateMini QUESTION IS MOOT: with the hoisted `{ in: JAKARTA }` context, no TZDate or TZDateMini is ever constructed at all — plain Date objects flow through, so the toISOString ban is enforced by the method being out of reach rather than by review, and the class costs nothing because it is never imported. Bundle impact is re-measured in step 08, where / actually imports this; a probe route today would measure a page nothing renders. canonicalSlot returns NULL rather than the cleaned-up input for an unrecognised slot — the tolerant version would let '07.00 - 09.00' through as a real slot nobody sells.

[2026-08-09] [build] check:shared + scripts/check-shared.mjs, wired as `check:lib = node scripts/check-shared.mjs && vitest run src` so it cannot be skipped. Diffs BOTH DIRECTIONS (a one-way walk cannot see a file present on the far side and absent here), diffs TESTS as well as sources on the user's decision — the admin repo inherits the proof, not just the code, which obliges it to carry vitest alongside date-fns and @date-fns/tz — and diffs the declared range of both shared peer dependencies, because v3 and v4 differ in exactly the timezone API dates.ts uses and two repos on different majors produce a byte-identical file computing different dates. Sibling resolved from ../arena-player-admin, overridable with ARENA_ADMIN_PATH, documented in .env.local.example as a path rather than a secret. PROVEN TO FAIL against a throwaway copy on all three drift shapes: one character of separator drift (reported with the exact line and both sides), a file missing on the far side, and a peer-dep major mismatch with byte-identical sources. Today it SKIPS LOUDLY — the admin repo has no src/ — naming how many files are unguarded, because a check reporting success having compared nothing is worse than no check.

[2026-08-09] [gotcha] THE TZ PIN IS WHAT MAKES THE DATE TESTS MEAN ANYTHING, and this was demonstrated rather than argued. Removed `{ in: JAKARTA }` from todayInJakarta: under vitest's pinned TZ=UTC three tests went red, and the same broken helper run under this machine's native Asia/Jakarta returned 2026-08-10 — exactly what the test expects. So without the pin the suite would have been green against a helper with no timezone handling at all, on the developer's machine and nowhere else.

[2026-08-09] [gotcha] TWO OF MY OWN TEST ASSERTIONS WERE WRONG, both caught on first run. (1) isPastSlot at exactly 12:00 was expected to leave the 12.00-14.00 slot bookable; the code says it is past, and the code is right — a slot starting this second cannot be sold to a team that has to travel. The boundary is now inclusive by decision and pinned by a named test. (2) normalisePhone was expected to accept 08123456789012, which is a 13-digit national number and not a real Indonesian mobile; the range is 8 followed by 8-11 more digits, so the true boundaries are 0812345678 and 0812345678901 and both are now asserted.

[2026-08-09] [gotcha] THE NEW slot-canonical-drift CHECK FELL FOR ITS OWN FAILURE MODE. The nine canonical strings now live in two places — TIME_SLOTS and the time_slot_canonical constraint — which is the copied-value shape this repo has lost time to three ways, so check:docs gained an eleventh check to compare them. The first version matched every quoted slot-shaped string in each file and therefore compared THREE PROSE MENTIONS from slots.ts' own doc comments against the nine in the SQL. It was a checker taken in by exactly the confusion it exists to catch. Both sides are now scoped to their declaration first — the TIME_SLOTS array literal and the constraint body — and extraction happens inside that slice. Proven to fail from both directions: a separator drift planted in the SQL, and a wrong end-hour planted in slots.ts.

[2026-08-10] [build] RENAMED the two check scripts to match what they do, on the user's ask. `check:lib` was named when a top-level lib/ existed and now runs `vitest run src` — nothing named lib is involved, and it also secretly ran the domain diff first, so the name was wrong twice. `check:shared` was named when the frozen folder was lib/shared/; step 02b renamed the folder and the script never followed. Now `check:unit`, `check:domain`, and `scripts/check-domain.mjs`. Sixth instance this session of a name describing a layout nobody has any more, and the first one nothing was going to catch: it broke nothing, it only misled.

[2026-08-10] [decision] The bundling is gone, replaced by `pnpm check` — lint, typecheck, format:check, check:domain, check:docs, check:unit, cheapest first. check:domain was folded inside check:lib so it could not be skipped; that bought the guarantee at the cost of a command that lied about what it ran. The umbrella is the honest version and covers six checks instead of two, though it is opt-in where the bundle was automatic — mitigated by every doc that said "run check:lib" now saying "run pnpm check", and by the Stop hook running check:docs independently. Verified all three ways: check:unit runs 40 tests with NO domain output (the actual behavioural change, and the one thing a rename could silently get wrong by leaving the && in place), `pnpm check` exits 0 across all six, and a planted prettier violation makes it exit 1 from the MIDDLE of the chain rather than only the first link.

[2026-08-10] [build] Sweep covered 26 references in web and 14 files in admin. HISTORY DELIBERATELY NOT SWEPT — 6 mentions in web's PROGRESS.md, 2 in the archive, 2 in admin's — because those record what was true when the work ran. Anyone "fixing" them later is introducing the defect, not removing one. Also ticked five Phase 1a DoD boxes that were provably done and had stayed unticked, exactly the failure docs/tasks/README.md warns about; the API-contract box was left UNTICKED because step 05 is verify-don't-rewrite and that verification has never formally run — ticking it would be the inverse defect.

[2026-08-10] [build] Admin repo swept in the same pass, backup re-taken first because it still has no git. Three statements there were made false by web's step 06 and are now corrected: architecture.md and PRD.md both said "web's src/domain/ does not exist yet" and specified a skip message accordingly — it exists, and the repo that is now missing from the comparison is admin itself. Its code-reviewer said a skip means web is absent; it now means admin is. And database.md called source-vs-constraint "the part no check in either repo currently does" — web's check:docs does it now as slot-canonical-drift, so that passage was rewritten to name THREE distinct surfaces rather than deleting admin's check:schema: check:domain is source vs source, web's slot-canonical-drift is source vs the SQL FILE, and check:schema is source vs the LIVE DATABASE. Only the last proves the migration was actually run, and run unedited. Admin also gains the same `pnpm check` umbrella, minus the two credentialed scripts — an umbrella that cannot run on a fresh clone is one people stop running.

[2026-08-10] [decision] docs/progress-archive/ KEPT. Its context cost is already zero, which is the entire reason it was split out: excluded in check-docs HISTORY, excluded in .prettierignore, and no agent reads it routinely. It holds the only record of WHY — why the project was replanned from scratch, why the DB was re-locked to Neon+R2 after the Supabase pivot and revert. PRD, architecture, DESIGN and PRODUCT hold what is TRUE, not why.

[2026-08-10] [build] src/hooks/ added on the user's ask — cross-module React hooks, `use-<thing>.ts`, tenth folder under src/. Boundary written down rather than left to be guessed: it takes the SAME one-consumer question as src/components/, with one exception that matters more than the rule. A data-fetching hook is NEVER promoted however many surfaces call it, because the query key, cache lifetime and retry policy are decisions about a surface, not reusable machinery — `home.queries.ts` stays in home. The test that settles it: if a hook's name would have to mention a route to be honest, it is a module hook. Also stated plainly that hooks/ and components/ are organised by KIND in a repo otherwise organised by FEATURE, and that the inconsistency is tolerated only because the one-consumer rule keeps them small — a src/hooks/ past a handful of files means the rule stopped being applied, not that it needs subfolders.

[2026-08-10] [decision] The new folder came with a new import rule, because adding it unguarded means the first cross-module hook can import a module and nothing complains. src/components/ and src/hooks/ sit BELOW modules: a shared hook reaching into @/modules/home is not shared, it is a home hook in the wrong folder, and it drags whatever that module imports onto every surface using it. Enforced the same two ways as the other three rules — a new ESLint zone for both folders (DEFAULT_PATTERNS + CROSS_MODULE) and a twelfth check:docs check, `shared-layer-inversion`, for the relative spelling no glob can express. Six probes: hook->module, component->module, hook->axios and hook->@/app all error; hook->domain+lib and module->hook (the correct direction) stay clean. Relative form `../modules/home/home.service` from src/hooks/ proven to exit 1.

[2026-08-10] [gotcha] The scaffold repo MOVED and the earlier work in it is gone. It is now at raygent/raygent-scaffolds/client-project-scaffold and has been rewritten as a Handlebars project GENERATOR — bin/create.mjs plus templates/ — so the CLAUDE.md and docs/architecture.template.md written for it earlier this session no longer exist. The structure now lives in templates/docs/_folder-structure.partial.hbs, which has five conditional trees. src/hooks/ added to the five FRONTEND branches only; the two backend branches deliberately do not get it, since there is no React there. Worth knowing before anyone looks for the old files: they were not deleted by us, the repo was replaced.

[2026-08-10] [build] Step 07 — mock layer and data plumbing. src/mocks/{availability,handlers,browser,server}.ts, src/lib/query-client.ts, src/services/api-client.ts, src/modules/home/{home.types,home.service,home.queries}.ts, src/app/providers.tsx wired into the layout. 58 tests, up from 40. MSW v2 confirmed against the INSTALLED package's own exports rather than Context7, which is not connected in this session — `require("msw")` lists http/HttpResponse and msw/browser lists setupWorker, which is more authoritative for 2.15.0 than any doc would be.

[2026-08-10] [gotcha] THE STEP'S OWN ACCEPTANCE COULD NEVER PASS. It said `curl localhost:3000/api/availability` — but MSW is a SERVICE WORKER: it intercepts browser fetches and nothing else, so curl reaches Next's router and 404s no matter how correct the handlers are. The paths in that block were pre-src/ as well. Raised before writing anything and the user chose browser-worker + msw/node for tests, which keeps src/app/api/ EMPTY until Phase 4 — that phase then ADDS a route file rather than replacing the body of one that already contains a dev-only branch. Acceptance rewritten around `pnpm check:unit` and two greps of .next/static/.

[2026-08-10] [build] THE PRODUCTION TRAP IS CLOSED AND PROVEN, not deferred to Phase 4's checklist. providers.tsx compares process.env.NODE_ENV, which the bundler INLINES, so in a production build the branch is a literal false and the dynamic import("@/mocks/browser") is never emitted — msw is not in any page's module graph rather than merely unreached. A runtime flag would have shipped the mock and then trusted a value. Verified on a real `pnpm build`: `grep -rl "mockServiceWorker\|setupWorker\|onUnhandledRequest" .next/static/` returns nothing, and so does the same grep for TEST409 and availabilityFor. onUnhandledRequest is "bypass" so an un-mocked call fails like a real one instead of being swallowed.

[2026-08-10] [decision] Error triggers are RESERVED TEAM NAMES — TEST409, TEST429, TEST400 — chosen by the user over a URL flag and over "one slot is always full". The URL flag needs a special path through api-client.ts that outlives the mock; the always-full slot can only ever produce 409, leaving the 400 and 429 screens built blind. A team name needs no devtools, and a reviewer can reproduce a 409 mid-walkthrough by typing. Written into architecture.md's API contract section as a table, next to the 409-vs-429 rule they exist to exercise.

[2026-08-10] [gotcha] MSW HANDLERS REGISTERED WITH RELATIVE PATHS MATCH NOTHING UNDER msw/node. `http.get("/api/availability")` resolves against location.origin in a browser; in Node there is no origin, so every test failed with "Cannot bypass a request when using the error strategy". Fixed with the origin wildcard `*/api/availability`, which matches both contexts — the browser request is absolute by the time MSW sees it anyway.

[2026-08-10] [gotcha] MY HANDLER WAS WRONG AND THE TEST WAS RIGHT: it validated `slot` with canonicalSlot, which REPAIRS a near miss by design, so '18.00-20.00' was accepted as valid. The contract says slot is an EXACT member of TIME_SLOTS. Switched to isTimeSlot. The distinction is the whole point of having both: canonicalise where user or URL text ENTERS the client, reject at the API — an API that silently repairs means a caller sending the wrong format never learns, and the Phase 4 route becomes the only place the format is enforced. This handler is the rehearsal for that route, so it has to be as strict as the route must be.

[2026-08-10] [build] Budget re-measured now that / actually imports something: 126.5 framework + 10.7 route chunk = 137.2 KB on a real build, excluding the 38.7 noModule polyfill. TanStack Query's table row moved 10.0 -> 10.7 because 10.0 came from an isolated probe route and 10.7 is what ships — library plus providers.tsx plus the query defaults. The projected subtotal is now 156.4. The gap between 137.2 and 156.4 is NOT headroom: date-fns, cn(), react-icons and zustand are written but not yet reached by any page, so it is the bill for code that already exists.

[2026-08-10] [decision] Query defaults are correctness decisions, not tuning. staleTime 30s matches the contract's cache line — availability that goes stale slowly shows a slot as free after someone took it, and the visitor learns at the 409, after filling the form and uploading a receipt. retry 1 not 3, because four round trips on stadium wifi means staring at an empty grid instead of an error you can act on. mutations retry 0, ALWAYS: a retried POST is a second insert attempt, and the only thing between that and a double booking is a unique index doing its job twice. Also: validateStatus < 500 on the axios instance, because 409/429/400 are expected answers with different UI and turning them into exceptions loses the body holding `fields`.

[2026-08-10] [gotcha] First draft of providers.tsx started the worker from a useState INITIALISER — a side effect during render, which React 19 strict mode double-invokes. Rewritten with useEffect and a cancelled flag. Caught by reading it back before running anything, not by a check; nothing in the harness looks for side effects in render.

[2026-08-10] [build] Step 08 — scripts/check-budget.mjs, src/lib/motion.ts, and hard rule 6 turned from a sentence into two mechanisms. check:budget is wired as its own script plus `pnpm check:ship` (= check && build && check:budget) rather than into `pnpm check`, on the user's decision: the budget needs a fresh build, and putting one inside the ten-second command turns it into a forty-second command people stop running. Same reasoning that unbundled check:domain from the test run.

[2026-08-10] [gotcha] THE INPUT STEP 08 WAS PLANNED AROUND DOES NOT EXIST. It said "reads Next's per-route First Load JS from the build output" — Next 16 removed that table, Turbopack emits no top-level app-build-manifest.json, and the per-route .next/server/app/<route>/build-manifest.json it DOES emit lists only the polyfill and the shared root files. The route's own chunk is missing from it, which is precisely the number the check exists to watch. The PRERENDERED HTML is the honest source: it is literally the list of scripts the browser is told to fetch for that route, and index.html references all six chunks including the 10.7KB one the manifest omits. That also makes the "per route, not just a total" requirement achievable, which it was not from any manifest.

[2026-08-10] [decision] check:budget READS THE CEILING OUT OF architecture.md instead of holding its own copy. That file calls itself "the single source — reference it, never copy the numbers", and this repo has lost time three separate ways to a value copied with nothing checking the copy; a budget script restating 240 would have been the fourth. If the row cannot be parsed the check EXITS NON-ZERO rather than defaulting — a budget check guessing its own budget is worse than no check. Editing the number in the table now changes what the check enforces, which is what "single source" was supposed to mean all along.

[2026-08-10] [build] PROVEN TO FAIL THREE WAYS, and the second is the one that matters. (1) --limit 50: all three routes BREACH, exit 1. (2) THE LEAK: a client component in src/modules/home/components/ calling useAvailability, rendered from page.tsx — every link in that chain is legal, ESLINT REPORTED NOTHING, and / went 137.0 -> 151.8 KB while /_not-found stayed at 137.8. Set --limit 145 and only / breaches. That is the whole argument for per-route rather than one total, demonstrated rather than asserted. (3) with .next/server/app moved aside it fails loudly instead of reporting success over nothing. Probe reverted; final clean build has / at 137.0 KB against the 240 ceiling, 103 KB headroom.

[2026-08-10] [gotcha] THE STEP FILE TOLD ME TO USE useGSAP AND IT CANNOT BE USED. @gsap/react's source opens `import gsap from "gsap"`, and useGSAP is a hook, so importing it means importing it STATICALLY — which puts the full 43.6KB back on first load and undoes the lazy decision this same step exists to enforce. Verified by reading node_modules/@gsap/react/src/index.js, not by recall. useMotion does directly what useGSAP wraps: gsap.context() for scoped cleanup, driven from a useEffect around a dynamic import("gsap").

[2026-08-10] [decision] HARD RULE 6 IS NOW MECHANICAL, using a property worth recording: eslint's no-restricted-imports matches STATIC imports and does NOT match import() — probed both directions before relying on it. So `gsap`, `gsap/*` and `@gsap/react` are banned repo-wide in every zone (all six override blocks restate it, the replace-not-merge trap again), leaving the dynamic form as the one remaining way in, and check:docs gained a 13th check asserting that form appears in src/lib/motion.ts alone. A second dynamic import elsewhere would both bypass the reduced-motion branch and split the 43.6KB across another chunk. Seven eslint probes and one check:docs probe, all fired.

[2026-08-10] [decision] MotionSpec.settle is a REQUIRED field, not a convention. You cannot describe an animation through useMotion without also saying what a reduced-motion visitor sees — and "nothing" is the wrong answer, because an entrance animation starts at opacity 0, so skipping the tween leaves the element invisible forever. The reduced branch applies the END STATE. GSAP has no built-in prefers-reduced-motion handling at all, which is the entire reason a bare gsap.to() in a component is banned.

[2026-08-10] [gotcha] TWO REACT RULES CAUGHT MY FIRST DRAFT of useMotion, neither by a test. The hook depended on `spec`, a fresh object literal every render, so it rebuilt the animation on every re-render; moved to a ref. Then React 19's lint rejected writing that ref during render — correctly, since it makes output depend on how many times a component happened to render — so the ref is updated in its own effect declared BEFORE the animation effect, with useRef(spec) seeding the first value. The remaining eslint-disable is one line, for the deliberate `...deps` spread, and says why.

[2026-08-10] [build] Final Phase 1a build verified on both lazy boundaries: `grep -rl "gsap\|ScrollTrigger" .next/static/` returns nothing and so does the msw grep from step 07. Both 43.6KB and the mock are genuinely absent rather than merely unreached.

[2026-08-10] [build] Step 05 verified rather than rewritten, as the step file demanded, and running its acceptance for the first time is what found the rest of this entry. CONTRACT IS INTACT: notes is 500 in all four copies with no stray 280 anywhere it means the field, FIRM and PROVISIONAL both present twice, and every response code specified. DoD box ticked because it was checked, not assumed — it had survived two earlier ticking passes precisely because this had never run.

[2026-08-10] [gotcha] I INTRODUCED A GAP IN GATE 1 AND DID NOT NOTICE FOR TWO STEPS. Writing db/migrations/20260809_create_bookings.sql made a THIRD copy of the schema alongside docs/database.md and docs/PRD.md, and slot-canonical-drift only compared src/domain/slots.ts against db/migrations/. The other two carry the same nine slot strings, the same four statuses, and the same uniq_active_slot WHERE clause, watched by nothing. That is the copied-value failure this repo has paid for three times, committed by the person who had just built the check against it.

[2026-08-10] [build] slot-canonical-drift REPLACED by schema-value-drift, same slot in the check list, wider reach. Asserts across db/migrations/*.sql, docs/database.md and docs/PRD.md that time_slot_canonical matches TIME_SLOTS, status_valid matches BOOKING_STATUSES, uniq_active_slot's WHERE matches ACTIVE_STATUSES, and notes_length is 500 — the four value families whose drift is dangerous rather than the text around them. Plus one byte-identity assertion: docs/database.md's block must stay identical to the migration, which line 54 already claims. docs/PRD.md is deliberately EXEMPT from that one — its block is a 28-line comment-free summary against the migration's 45, which is correct for a spec and means a whole-block diff is the wrong tool there. PROVEN TO FAIL FIVE WAYS, one per source and one per value family; the ACTIVE_STATUSES probe fired from all three copies at once, which is exactly the coverage that was missing.

[2026-08-10] [gotcha] TWO OF STEP 05'S OWN ACCEPTANCE GREPS WERE WRONG AND NEITHER DOCUMENT WAS. `^// (201|400|409|429)` returned 0 while every code sat at lines 45 and 97-103, and a bare `grep -rn "280"` returned six hits that are --dur-base:280ms in DESIGN.html plus the PROGRESS entries recording this very contradiction. Both fixed rather than deleted: the assertions are worth keeping, and an acceptance that fails on correct content trains people to ignore it, which is worse than not having one. Also dropped the field-name grep entirely — it compared architecture.md against "the PRD form spec", but the PRD lists UI LABELS (Nama Tim, Nomor WhatsApp) and never wire names. That is correct single-sourcing, and a grep implying a gap invites someone to duplicate the field table into the PRD.

[2026-08-10] [decision] Recorded in the API contract, because it is deliberate and looks like a mistake: THE QUERY PARAM IS `time` AND THE POST FIELD IS `slot`. /booking?date=…&time=… is a link a human admin types into WhatsApp and `time` is the word they would guess; `slot` is the wire name matching TIME_SLOTS and the time_slot column. Renaming the param breaks every link already pasted into a chat — the one place in this system with no deploy and no rollback. Consistent across all four places it appears. Phase 3 reads `time` and submits `slot`.

[2026-08-11] [milestone] PHASE 1a CLOSED, 9/9. Last DoD box ticked: /plan-eng-review, /plan-devex-review and /devex-review have all run, all six P1 findings are closed and pushed at a4fdd0b. Twelve P2s and three P3s stay open in ~/.gstack/projects/arayhan-arena-player/ and none of them gates Phase 1b — P1 was the ship-blocking bar and nothing below it touches the design foundation. Final state: pnpm check exit 0, 13 doc checks, 74 unit tests, / at 137.0KB gzip with 103.0KB headroom under the 240KB ceiling. The heaviest remaining debt is T7 (an eslint zone() helper — the replace-not-merge trap in flat config has now bitten three separate times in this repo) and T8 (fixture tripwires proving the 17 check-docs assertions can actually fail). Both are P2 by agreement because they harden tooling that currently works, not code that currently ships.

[2026-08-11] [gotcha] PHASE 1b WAS BUILT BACKWARDS AND THE DOCS DID NOT NOTICE. PRD tasks 4 and 5 — the design system page and its clickable journey — already exist as docs/DESIGN.html: 1116 lines, eleven sections, ending on the Alur frames. Tasks 1, 2 and 3 never ran. So the artifact commits to an entire visual world (brick build-instruction book, user-pinned over the rolled Papan Jadwal challenger) while docs/DESIGN.md line 139 still reads "Creative North Star: not yet established. Art direction is Phase 1b task 1 and has not been decided." That line is now false, and it is the line an agent starting Phase 2 would read first. The direction survives only in PROGRESS prose and in the HTML itself, neither of which is where DESIGN.md's own format spec says a north star lives. Task 1 is therefore mostly transcription of a decision already taken — the exception being docs/references/benchmark-bataskotapoint.png, 6.8MB, gitignored, and never read by anyone, which is both a 1b DoD item and the only way to answer what "surpass the benchmark, inverted to light blue-white" means concretely.

[2026-08-11] [build] PHASE 1b TASKS 1 AND 3. Benchmark finally read — docs/references/benchmark-bataskotapoint.png, 6.8MB, a full-page desktop capture at 1920x7888 that had sat unread since it was dropped in. Findings written into DESIGN.md as a table plus four falsifiable claims, because the file is gitignored and deleted and anything not written down goes with it. THE MOST USEFUL FINDING WAS A CONVERGENCE, NOT A CONTRAST: the benchmark also renders booked slots in a red triple with a lock glyph and also uses filled pills for the selected date. Arena Player arrived at both independently, and that is a reason to keep them rather than differentiate — a booking grid is the wrong place to be original, and this is the convention the local audience already reads. What it does badly is the useful part: the booking section sits roughly five scrolls down behind hero, video, specs and gallery, which is the evidence behind our two-scroll rule; and six sections carry six identically-treated centred two-tone headings, so a visitor has no sense of place. It also shows prices, which we cannot.

[2026-08-11] [decision] ART DIRECTION WRITTEN DOWN — "the build-instruction book". DESIGN.md line 139 had said "Creative North Star: not yet established" while DESIGN.html shipped a fully committed world, so the document an agent reads first was contradicting the artifact it points at. Mostly transcription; the one genuinely undecided part was the SECTION-TRANSITION LANGUAGE, chosen by the user from three options: numbered assembly steps, an oversized Orbitron numeral in grey-200 bleeding off the left edge with a navy keyline between steps. Picked over keyline dimension rules and over bands-and-hairlines because it costs zero kilobytes, needs no motion, works at 375px, and fixes the exact defect both the benchmark and our own first draft were graded on — the critique's phrase was "one compositional idea repeated eight times". Also recorded the fusion rule that makes the world cohere: every colour carries both an instruction-book role and a product meaning, and where they conflict the product wins — which is why "the new part" is Signal Blue and not the source world's brick red, since red already means booked.

[2026-08-11] [decision] TWO SETTLED, BOTH ON THE RECOMMENDATION. Inter STAYS as the body face: the detector's flag was correct (it is the face every AI-generated interface converges on) but it is a stated client commitment in PRODUCT.md, "invisible" is the right brief for body type a captain reads at speed, and every printed number in the system — the 60-68ch measure, the 14px/12px fixed sizes, the state-label ratios — was computed against Inter's metrics, so a swap invalidates them all at once. The reason is now written down, which is the actual fix; the failure mode was inheriting it without noticing. HERO-VIDEO GATE DECIDED AND FAILED, before anything was produced, as the PRD requires. It failed on condition 2, which was always the binding one: iOS Low Power Mode and in-app webviews block autoplay outright and the in-app browser is the PRIMARY device here, so a real share of visitors would only ever see the poster. Conditions 1 and 3 were never reached. LCP element is the Orbitron headline plus logo; the "no autoplaying video" guardrail in CLAUDE.md stays unamended because it only needed an exception clause if the gate passed; /remotion-create is reserved for off-site social assets.

[2026-08-11] [fix] THE FIVE ITEMS LEFT "OPEN BY AGREEMENT" ARE CLOSED, and three of them were accuracy defects rather than taste. (1) ELAPSED IS NOW A FOURTH SLOT STATE. It was rendering in the danger triple while labelled "Sudah lewat" — contradicting the paragraph printed directly above it in step 04, and overstating how busy the day was on a product measured by filling empty hours. Now grey-50 fill, navy-400 text, and a 3px navy-400 left rule at 6.64:1: the separation is STRUCTURAL, not chromatic, because both states are unavailable and a hue swap alone leaves them reading as the same family — only one of the two is somebody else's booking. Added to the step 04 state inventory, which otherwise omitted a state the system ships. (2) "Sudah lewat (6)" rendered TWO rows. Now six, which with the three below makes the full nine-slot day — a small lie, but on the one page whose entire argument is that its numbers are checkable. (3) The DIRECTION CONTRACT comment still claimed the first viewport was the parts inventory; the page has opened on the client's decisions since the peak-end fix, which the reviewer called its best structural decision. Comment corrected rather than the page reverted. (4) Kirim used native `disabled`, the only control on the page breaking the rule the page itself prints twice — a keyboard user reaches the last field and finds nothing. Now aria-disabled, with the CSS comment corrected too: it claimed pointer-events:none stops the press, which covers the MOUSE only and leaves keyboard Enter live, so Phase 3 owes a handler guard. Half a fix described as a whole one is exactly the pattern the P0 punished. (5) The system had NO ::placeholder rule at all, so every Phase 3 field would have inherited the UA default at roughly 2.35:1; added --input-placeholder-fg at navy-400, 6.94:1 on white, opacity:1 explicit because browsers stack their own alpha on top. Gate green throughout: 13 doc checks, 74 tests.

[2026-08-11] [decision] HERO COPY CHOSEN — "Pilih Jam. Kirim. Main." Three registers were drafted and the user picked the three-step imperative over an answer-before-asked framing ("Jam Kosong, Terlihat Langsung.") and a same-day energy one ("Malam Ini, Lapangan Menunggu."). It wins on two grounds. The headline IS the assembly, so it says what the art direction says in the visitor's own words rather than in ours. And length is a hard constraint here, not a stylistic one: at Orbitron 900 on a 375px screen the LONGEST WORD is what breaks the layout, and this headline's longest word is five characters against nine for both rejects. The same-day option was also factually risky — a hero cannot know what time of day it is being read without client JS, so "malam ini" is simply wrong to anyone arriving at 9am. Subheadline "Jadwal Arena Player tampil langsung. Pilih jam kosong, lanjut lewat WhatsApp." Meta wired into src/app/layout.tsx. It stays TODO(content) deliberately: it is drafted in-house, so a client rewrite swaps it in three places at once — unlike the Ketentuan, which is verbatim client content and must never be reworded.

[2026-08-11] [gotcha] "ALL THREE SLOT STATES" APPEARED IN THREE PLACES AND IS NOW FOUR. Splitting elapsed out of booked changed a count that the PRD stated in the Phase 1b task table, in the client-checkpoint paragraph, and in the Definition of Done. None of the 13 doc checks could catch it — check:docs asserts value families like TIME_SLOTS and BOOKING_STATUSES against their SQL, but a prose numeral describing a design artifact has no machine-readable counterpart to drift against. Fixed by hand in all three. Worth recording because it is the shape of defect this project keeps producing: a change that is correct in the artifact and stale in every document that counted it.

[2026-08-11] [gotcha] I ADDED THE SIDE-TAB ACCENT BORDER AGAIN. The detector caught a 3px navy-400 left rule on the new elapsed slot state — the same rule this project already added and removed once, during the checkpoint build, for the same reason. Worse, I had written a paragraph in both DESIGN.md and DESIGN.html arguing it was "structural, not chromatic" and therefore fine, which is precisely how a slop pattern survives review: with a rationale attached. Two things made it feel justified. The critique DID prescribe a 3px left rule — but for the available/pending/booked triple, whose fills computed 1.11:1 and 1.22:1 and genuinely carried no signal; elapsed versus booked is neutral-family against danger-family, which is not the same problem. And the elapsed rows sit behind a collapsed opt-in disclosure, so the scannability argument that justified the rule elsewhere does not reach them. Removed. Three signals already separate the two states: colour FAMILY not hue, the resting border, and the label itself, which is what satisfies WCAG 1.4.1 — a fourth that costs the page its credibility is not a trade worth making on an artifact whose whole argument is that it is checkable. The lesson generalises: a recommendation from a critique is scoped to the component it was written about, and porting it to a neighbouring component is a new decision, not a continuation.

[2026-08-11] [decision] Inter's overused-font finding persisted as a shared ignore-value in .impeccable/config.json, with the full reasoning in the ignore record rather than only in DESIGN.md. The detector is right and the flag stays true; suppressing it is correct only because the user settled the question deliberately this session. Recorded as shared, not local, so the next session sees the decision instead of re-litigating it — which is the same failure mode as the gitignored critique folder in hard rule 10.

[2026-08-11] [fix] THE CLIENT CHECKPOINT WAS SHOWING THE CLIENT A STALE LIST OF ITS OWN QUESTIONS. DESIGN.html's "Butuh keputusan Anda" block — the first thing the client reads, moved there deliberately by the peak-end fix — still listed art direction and hero copy as open, both decided today, and DID NOT LIST THE WHATSAPP-ONLY QUESTION AT ALL. That is the one that blocks Phase 2 and the one the client has never been asked, so the block was simultaneously wrong about what is settled and silent about what matters most. Rewritten to the three that are actually open: the WhatsApp-only flow, pricing, and the six missing content items. Also rewritten IN INDONESIAN, unlike the rest of the page: the critique's second-lowest heuristic score was English jargon-dense chrome on an artifact whose checkpoint audience is an Indonesian field owner, and this is the one section that audience must act on rather than be walked through.

[2026-08-11] [decision] THE TWO BLOCKING ANSWERS ARE DECOUPLED FROM SCHEDULING A CALL. The checkpoint doc had one format — a live screen share — and every question inside it, which makes the client's calendar the gate on Phases 2 and 3. Scheduling IS the schedule risk the document itself keeps warning about. Added a copy-paste Indonesian WhatsApp message carrying only questions 1 and 2 plus the content list, to send now; the walkthrough still happens and still confirms the direction, but if it slips, the two answers that actually gate work are already moving. Deliberately NOT all six questions: the other four change what gets prioritised or what fills a placeholder and none of them stops work, and a six-question message gets two answered. Also corrected three things the day's work made stale in the walkthrough script — the slot list is four states not three, the decisions block is three items not four, and the headline is now DRAFTED and needs approval rather than supply, which moves it out of the same "waiting on client" bucket as the bank details.

[2026-08-11] [review] /design-review ON docs/DESIGN.html — FOUR FINDINGS FIXED, ONE ACCEPTED, ALL FIVE ARE NUMBERS THAT DISAGREED WITH A SENTENCE. Ran DEGRADED and the DoD says so rather than hiding it: Codex was not installed on this machine so the outside-voice source audit never ran, and no subagent was dispatched because this repo's standing instruction is not to spawn one unasked. Single-reviewer, stated plainly. (1) ELAPSED CARRIED NO SURFACE SIGNAL AT ALL — measured, its grey-50 fill against an available cell's white computes 1.05:1, so after splitting elapsed out of booked earlier today I had left it distinguished by border HUE and text alone, which breaks this system's own Shapes rule that no state depends on colour alone. Fill and border both to grey-200, making elapsed the only borderless cell — structural, not an accent stripe, and the same grey-200 the disabled button already uses. navy-400 on grey-200 is 5.61:1. With six rows expanded at 375px the day now reads six grey, one amber, two available, which is the honest picture and the entire reason the state was split. (2) The states badge said 5; the cell has six. Every other badge on the page is an exact count. (3) The Alur journey opened a 320px dead gutter between 900 and 1299px — space-between on two 375px frames in a 1068px column — on the one section whose job is showing that the screens happen IN ORDER; 1280 is the most common laptop viewport there is. (4) THE FRAMES CLAIMED "the real device width" AND WERE FALSE BY 36px: each draws its own border and padding inside the page column, so rows got 307px where the product gets 343. This document has already taken a P0 for exactly that class of error. Padding recovered 8px; the claim now states the gap instead of denying it.

[2026-08-11] [gotcha] I ALMOST SHIPPED A NO-OP FIX WITH A COMMENT SAYING IT WORKED. Slot times vary with their digits — "18.00 - 20.00" measures 127px, "10.00 - 12.00" measures 120px — so whether a row wraps depends on which numbers it contains. The obvious fix is font-variant-numeric:tabular-nums, DESIGN.md's own typography checklist already asks for it on number columns, and I wrote it in with a confident comment. MEASURED AFTER: 127 and 120, unchanged. ORBITRON SHIPS NO tnum FEATURE, so the property does nothing on this face. Reverted rather than left in place, because a no-op with a comment claiming it solved the problem is worse than the problem — it retires the finding while leaving it live. The CSS now records the measurement and flags the line to revisit first if the display face ever changes. Accepted rather than fixed: in the PRODUCT a 343px row leaves 26px of slack, so this only surfaces inside the narrower Alur frames. Second time in one session that verifying a fix I had already written up contradicted the write-up.

[2026-08-11] [milestone] PHASE 1b IS 7/8. Benchmark deleted after its findings outlived it, which is the condition docs/references/README.md sets. The only box left is the CLIENT APPROVAL, which no amount of work here can tick — it needs the client. The two blocking questions are drafted and ready to send as a WhatsApp message; sending them is the single highest-value action available on this project right now, and it costs one message.

[2026-08-11] [build] DESIGN.HTML PUBLISHED AS A SHAREABLE LINK FOR THE CLIENT CHECKPOINT. A .html attachment is close to useless to a field owner on a phone — WhatsApp blocks the type on some clients, and on Android opening one means Files, Downloads, then pick a browser. A URL opens in one tap. THE TRAP THAT MADE THIS NON-TRIVIAL: the artifact host applies a strict CSP that blocks every external request, and DESIGN.html loads Archivo, Orbitron and Inter from a Google Fonts CDN <link>. Published as-is, all three fall back to system sans — and the typography is a large part of what the client is being asked to approve, so it would have shown them the wrong page while looking like it worked. Fixed by fetching the LATIN subsets only (Google serves ~36 woff2 files across vietnamese, latin-ext, cyrillic and greek; the page is Indonesian and English) and embedding all 10 as base64 data URIs. 312KB of fonts, 0.46MB total, verified after publishing that document.fonts.size is 10 and h1/body/slot-time resolve to Archivo/Inter/Orbitron rather than fallbacks. Build script kept in the scratchpad, not the repo — a 0.46MB base64 file does not belong in git, and the repo copy stays 72.8KB with its CDN link intact for local dev.

[2026-08-11] [decision] THE PUBLISHED COPY HAS ITS COMMENTS STRIPPED; THE REPO COPY KEEPS EVERY ONE. 15.9KB of HTML and CSS comments removed from the shareable build only. The reasoning in those comments is exactly what a future developer needs, so it stays in git — but the published page gets a URL the client can forward, view-source is one tap away, and the comments carry candid internal notes ("a small lie", "WHICH WAS FALSE BY 36px"), the /impeccable seed key and roll index, and internal repo paths. PRODUCT.md records "does the git repository transfer to the client at handover?" as an OPEN DECISION whose conservative default is that internal material stays internal; this applies that default rather than pre-empting it. Verified after stripping: seed key, roll index, impeccable, and the candid phrases all return 0 hits. Two internal paths survive deliberately — docs/DESIGN.md and src/lib/motion.ts appear in VISIBLE body copy, where a design system legitimately names its own source of truth.

[2026-08-11] [fix] TWO STALE CLAIMS CAUGHT BY READING THE WHOLE FILE BEFORE PUBLISHING, WHICH IS THE ONLY REASON THEY WERE FOUND. Step 03 still told the reader "Whether the product keeps them is Phase 1b task 1, still open" — task 1 ran today and settled Orbitron and Inter deliberately, so the page was telling the client a decision was open that had been closed hours earlier. And step 04 called elapsed "a fourth state" directly under a badge reading "+ 6 states"; both numbers are right under different definitions (four come from the data, six are what the cell renders including hover and selection) but printing them four paragraphs apart with no reconciliation is the kind of thing that makes a reader stop trusting the other numbers. Both fixed. Neither was reachable by any check in the repo — check:docs asserts value families against SQL, and no assertion can know that a sentence describing a phase went stale when the phase advanced.

[2026-08-11] [milestone] CLIENT ANSWERED. Alur WhatsApp-saja DIKONFIRMASI — admin standby membalas dengan link formulir, dan klien sendiri yang mengangkat bot WhatsApp sebagai pengganti nanti, yang sudah jadi item pasca-Phase-4 di PRD tapi sekarang dengan dukungan klien, bukan asumsi kita. PHASE 2 TERBUKA. Harga: TIDAK di halaman utama, TAPI DITAMPILKAN di formulir booking — ini menutup OPEN DECISION yang menahan hard rule 2 sejak PRD ditulis, dan mempersempitnya dari "no prices anywhere" jadi "no prices on /". Klien menjawab DI MANA harga muncul tanpa menjawab BERAPA, jadi daftar tarif jadi kategori TODO(content) KETUJUH — perubahan pada hard rule 3 dan pada scripts/check-docs.mjs. Harga adalah satu-satunya placeholder yang TIDAK BOLEH diberi angka sementara: enam lainnya kalau lolos ke produksi cuma terlihat belum diisi, sedangkan harga karangan terlihat persis seperti harga asli, ada orang yang mentransfer berdasarkan itu, dan itu jadi angka developer yang menempel pada bisnis klien.

[2026-08-11] [decision] APPROVED WITH CHANGES, ENAM PERMINTAAN, TIGA BERTABRAKAN DENGAN ATURAN YANG SUDAH DITULIS. Dunia visualnya bertahan — terang, biru-putih, build-instruction book — jadi ini bukan Redirect. (1) THREE.JS DITOLAK OLEH ARITMATIKA, BUKAN OLEH SELERA. three.js ~150KB gzip; / terukur 137.0KB dari plafon 240KB, jadi three.js LEBIH BESAR dari seluruh sisa budget, dan 3.75x cap 40KB yang jadi syarat izin WebGL. anime.js = runtime animasi kedua yang tidak menambah kemampuan apa pun di atas GSAP. architecture.md baris 330 sudah menulis jawabannya jauh sebelum permintaan ini datang: "Reach for the shader, not the engine." Efeknya tetap dapat — shader GLSL ~3-5KB atau OGL ~10KB. (2) SLOT GRID: batasan 146px untuk "Menunggu Konfirmasi" itu nyata tapi HANYA MENGIKAT DI 375px. Jadi HP tetap satu kolom, >=768px jadi 2 kolom, >=1024px jadi 3, dan labelnya pindah ke bawah jam supaya 20 karakternya utuh di berapa pun kolomnya. Bukan kompromi — itu pembacaan yang benar atas batasannya. (3) ROUNDED: 2px/4px jadi 10px/14px, tapi date pill TETAP 999px. Jarak antara "jelas membulat" dan "bulat penuh" itulah yang masih membawa sinyal "baris ini bisa digeser"; kalau semuanya jadi 24px, pill cuma jadi yang paling bulat, bukan satu-satunya yang bulat, dan baris tanggal butuh panah atau fade untuk mengatakan apa yang bentuknya sudah katakan gratis.

[2026-08-11] [gotcha] TOKEN RADIUS DIGANTI NAMANYA BERSAMA NILAINYA, DAN ITU DISENGAJA. --radius-sharp:2px jadi --radius-control:10px, --radius-card:4px jadi --radius-panel:14px. Token bernama "sharp" yang isinya 10px persis jebakan nama-basi yang proyek ini terus-terusan tangkap; "control" dan "panel" menyebut token itu UNTUK APA, bukan dulu kelihatan seperti apa. Sekalian: lede step 04 masih berbunyi "One column, full-width rows" tepat di sebelah grid tiga kolom yang baru saja dirender — ditangkap karena screenshot diambil setelah perubahan, bukan karena ada check yang bisa melihatnya. Tidak ada assertion di repo ini yang bisa tahu sebuah kalimat jadi salah ketika CSS di bawahnya berubah.

[2026-08-11] [milestone] PHASE 1b CLOSED 8/8, PHASE 2 STARTED. Kotak terakhir dicentang sebagai APPROVED WITH CHANGES: empat dari enam permintaan klien yang masuk lingkup 1b sudah dilipat masuk (slot grid, sudut membulat, harga, alur WhatsApp); dua sisanya — shader header dan motion scroll/hover — memang eksekusi Phase 2 karena hero-nya belum ada, dan keduanya sudah dispesifikasikan di DESIGN.md supaya Phase 2 tinggal mengeksekusi, bukan memutuskan ulang. PRD memang mengizinkan ini: "or their changes are folded in before Phase 2 starts".

[2026-08-11] [build] PHASE 2 TASK 1 — LAYOUT. Tiga hal yang sebelumnya sengaja ditahan sampai 1b selesai, sekarang ditulis. (1) globals.css akhirnya dapat blok @theme-nya, ditranskrip dari frontmatter DESIGN.md yang normatif, tiga lapis: primitives (nilai mentah), semantics (untuk apa), components (di mana). Phase 1a sengaja mengosongkannya — menyalin token sebelum arah desain diputuskan akan membuat sumber kebenaran kedua untuk nilai yang masih bergerak. (2) next/font/google untuk Orbitron + Inter, self-hosted; architecture.md mencatat next/font sebagai load-bearing untuk jaminan no-CLS dan LCP, jadi CDN <link> bukan pilihan. (3) src/modules/home/ — komponen Section yang membawa perangkat utama arah desain: ordinal Orbitron raksasa di grey-200 di samping heading, keyline navy antar langkah. Hero tidak diberi nomor: dia membuka halaman, bukan melanjutkan urutan, dan menomorinya 00 cuma menghitung demi menghitung. BIAYA BUNDLE NOL — / tetap 137.0KB dari plafon 240KB, sisa 103.0KB.

[2026-08-11] [gotcha] PORT 3000 ADALAH PROYEK ORANG LAIN, LAGI. curl ke localhost:3000 mengembalikan 200 dan saya hampir memakainya sebagai bukti. Screenshot-nya ternyata "SMILE — Sistem Monitoring Inventaris Logistik Kesehatan", aplikasi yang sama sekali berbeda. Arena Player ada di 3002; `pnpm dev` saya sendiri gagal start karena sudah ada dev server lain berjalan, dan pesan itu tenggelam di log. Proyek ini SUDAH pernah mencatat jebakan ini persis (dulu port 3000 dipakai "Food Allergy Certified", Arena Player di 3001) dan tetap kena lagi. Pelajarannya bukan "cek portnya" — melainkan STATUS 200 TIDAK MEMBUKTIKAN SIAPA YANG MENJAWAB. Yang menangkapnya adalah screenshot, bukan status code.

[2026-08-11] [fix] DRAF PERTAMA Section MEMBUAT BADAN TEKS TIDAK SEJAJAR DENGAN JUDULNYA. Numeral dan judul dipasang sebagai flex row, lalu children digantung di bawahnya selebar penuh — jadi heading ter-indent sejauh lebar numeral sementara badan teksnya tidak, dan di 375px itu terbaca sebagai kesalahan, bukan sebagai perangkat desain. Diganti jadi grid dua kolom: numeral memiliki kolom 1, semua yang dikatakan section itu ada di kolom 2. Terukur setelah perbaikan: h2 di x=91, paragraf di x=91. Ditemukan dari screenshot, bukan dari check mana pun — tidak ada assertion di repo ini yang bisa melihat dua elemen yang seharusnya sejajar ternyata tidak.

[2026-08-11] [decision] ATURAN MOTION ORDER SECTION DIBUKA. DESIGN.md sebelumnya menyatakan dengan tegas bahwa order section hanya boleh functional feedback — perubahan state, seleksi, collapse — dengan alasan yang diukur: di situ booking terjadi, di situ gate Lighthouse per-section paling menggigit, dan di situ organiser memutuskan paling cepat dengan 8-12 orang menunggu. USER MEMBUKA ATURAN ITU: klien minta banyak animasi dan tidak mengecualikan bagian tempat produknya sebenarnya terjadi, jadi order section tidak jadi satu-satunya ruangan diam di halaman yang selebihnya bergerak. Dicatat sebagai keputusan bertanggal, bukan dilonggarkan diam-diam, karena aturan lama ditulis dengan tegas dan sesi berikutnya akan membaca keduanya sebagai kontradiksi kalau tidak dijelaskan. SATU HAL BERTAHAN, DAN ITU SOAL LATENSI BUKAN SELERA: feedback seleksi tetap seketika. Bukan pembatasan seberapa banyak section itu bergerak — entrance, reveal, date row, collapse, grid semuanya boleh — melainkan aturan tentang satu momen di mana animasi DAN feedback adalah kejadian yang sama, dan penundaan di situ bukan hiasan melainkan jawaban yang lebih lambat atas "jam 8 kosong tidak".

[2026-08-11] [decision] TIGA EFEK ORDER SECTION DIPILIH USER SEBELUM ADA KODENYA, sesuai aturan yang masih berlaku. (1) MASUK LAYAR: heading fade + naik 12px, date pills geser dari kiri, lalu baris slot menetas berurutan 40ms berjarak, total ~400ms. Stagger-nya adalah gagasan instruction-book yang dibuat harfiah — bagian-bagian datang satu per satu lalu duduk di tempatnya, yang persis argumen seluruh halaman ini tentang apa itu booking. (2) TAP SLOT: fill biru mendarat di 0ms, ring melebar dari titik tap lalu memudar selama 300ms. DIPISAH DUA LAPIS DENGAN SENGAJA — fill adalah JAWABAN atas "slot ini jadi milik saya sekarang tidak", jadi mendarat seketika; ring itu hiasan dan jalan setelahnya. Satu transisi 300ms tunggal akan membuat jawabannya datang 300ms terlambat, yaitu balasan lebih lambat yang menyamar sebagai kehalusan. (3) COLLAPSE: caret berputar 90deg, tinggi kontainer membuka 280ms, enam baris elapsed menetas 30ms berjarak; GSAP mengukur tinggi aslinya sendiri alih-alih menganimasikan ke auto, dan itulah yang menjaganya keluar dari budget CLS. Semuanya transform + opacity saja, semuanya lewat src/lib/motion.ts.

[2026-08-11] [build] PHASE 2 TASK 2 — ORDER SECTION. Date pills 14 hari, slot grid responsif 1/2/3 kolom, empat state melawan MSW mock, collapse "Sudah lewat", skeleton, error state, dan CTA. Logika pemisahan elapsed ditaruh di order.utils.ts sebagai fungsi murni dengan `now` yang diinjeksi, bukan dibaca dari jam internal — fungsi yang memanggil new Date() sendiri lulus jam 09.00 dan gagal jam 21.00, di mesin orang lain. 16 tes baru, total suite 90 (naik dari 74). BUNDLE: / naik dari 137.0KB ke 164.0KB, sisa 76.0KB dari plafon 240KB. Kenaikan 27KB itu TanStack Query plus komponen section; GSAP belum terhitung karena masih lazy dan baru dimuat saat efeknya jalan.

[2026-08-11] [gotcha] TES SAYA SALAH, KODENYA BENAR — UNTUK KETIGA KALINYA DI REPO INI. Saya assert 6 elapsed / 3 live pada jam 19.00 Jakarta, menalar dari mock di DESIGN.html alih-alih dari aturannya. isPastSlot membandingkan `currentHour >= slotStartHour`, jadi jam 19.00 slot 18.00-20.00 SUDAH LEWAT — dan itu benar: tidak ada yang bisa memesan jam yang sudah berjalan. Jawabannya 7/2. Tesnya diperbaiki dan satu tes tambahan ditulis khusus untuk mengunci perilaku batas itu, supaya asumsi yang sama tidak diulang sesi depan. Pola yang sama sudah muncul di isPastSlot dan di normalisePhone sebelumnya: yang salah ekspektasi saya, bukan implementasinya.

[2026-08-11] [gotcha] OVERFLOW 41px DI 375px, TIGA PENYEBAB BERLAPIS, DAN DUA PERBAIKAN PERTAMA SAYA TIDAK MENGUBAH APA PUN. (1) `min-w-0` pada grid item — tidak berpengaruh, karena `1fr` itu `minmax(auto,1fr)` dan minimum otomatisnya ada di TRACK, bukan di item; perbaikannya `minmax(0,1fr)`. (2) Root OrderSection juga `grid`, dan track implisitnya `auto`, jadi date row menyusun ukuran track dari kontennya sendiri dan `overflow-x-auto` tidak pernah aktif; diganti flex column. (3) YANG SEBENARNYA PALING PENTING: numeral section memakan 59px + gap 16px, menyisakan 268px untuk konten — sementara DESIGN.md sudah MENGUKUR bahwa slot cell butuh 343px karena "Menunggu Konfirmasi" 20 karakter dan tidak boleh dipotong. Terukur: cell butuh 323px, dapat 268px. Jadi children dibuat selebar penuh di HP dan baru ter-indent dari md ke atas. Indentasi itu selera, 343px itu batasan produk yang terukur — dan ketika keduanya bertabrakan, kejelasan menang, aturan yang sama yang menaruh Signal Blue di tempat dunia sumbernya mau merah bata. Tidak ada satu pun check di repo ini yang bisa melihat ini; yang menangkapnya screenshot dan getBoundingClientRect.
