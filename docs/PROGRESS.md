# PROGRESS — shared agent log

Cross-agent communication file. Every agent reads this before working, appends after.
Format (caveman-compact): `[YYYY-MM-DD] [agent] [what] [reason]`

Agents: pm | lead | designer | engineer | reviewer
Older entries use the retired six-agent tags (instructor, eng-lead, senior, fe, be) — left as written.

**This file holds the current phase only.** Earlier entries live in
[progress-archive/](progress-archive/) — read those only when tracing _why_ an older
decision was made. What is currently **true** lives in [PRD.md](PRD.md),
[architecture.md](architecture.md), [DESIGN.md](DESIGN.md), and [PRODUCT.md](PRODUCT.md);
this log records how it got that way, not what the rules are.

Archive when a phase closes, so this file never becomes a tax on every session.

---

## Phase 2 — Landing page

[2026-08-13] [designer] KETENTUAN REDESIGNED AS "PELAT ENAMEL" — ten rows become ONE PLATE. Two files: KetentuanRows.tsx and HomePage's `#ketentuan` block only. The list used to be rows floating on the band with a hairline above and below each, and a heading hovering over them with nothing joining the two; it is now a single object drawn entirely with rules — 2px `--color-interactive-on-band` painted edges top and bottom, the client's own document title as a head field, ten hairline-divided fields under it. No radius, no shadow, no fill, no four-sided frame: edges only, because a box here is a card and the Band Rule exists to forbid exactly that. Section.tsx was NOT edited.

THE 41px OVERFLOW IS NOT IN THIS COMPONENT AND NEVER WAS — THE PLATE CONTRIBUTES ZERO. Located by Range-rect measurement rather than by reading the file: the entire overflow is the `<h2>`. "KETENTUAN" is one unbreakable word measuring **278.6px** in Panchang at the h2 clamp's 28.75px, and Section's numeral track leaves the heading column **221.6px**, so the word overhangs the viewport by exactly the 41px the section reports. `#ketentuan`'s own overflow is now 42/41/21/0/0/0 at 320/375/414/768/1280/1440 and every escaping element at each width is either that `<h2>` or Section's own `02` ordinal; the PLATE's `scrollWidth - clientWidth` is **0 at all six**. NO VALUE OF `--text-h2` ABOVE 22.9px FITS THAT COLUMN, so raising the Sub-360 Floor does not fix it either — at the floor's own 24px the word is still 232.6px against 221.6px. DESIGN.md line 612 already named the right answer before anyone hit this: "if a second exception is ever proposed, the honest conclusion is that the numeral track — not the type — is the thing that needs to change." IT IS A PAGE-WIDE Section DEFECT, NOT A KETENTUAN ONE: the `1.7` track constant was derived against Orbitron and re-justified against Saira, and under Panchang the ordinal ink overflows its own track by 4px on `01` and **34px on both `02` and `03`** at 375px, growing to 73px at 1280px and 77px at 1440px — the ghost numeral is drawn straight through the heading beside it, which is visible in the committed 1280px screenshot. Page-level overflow is 45/41/22/131/57 at 320/375/414/768/1280, BYTE-IDENTICAL to the pre-change baseline, with `#lokasi` still the page's own worst offender.

EVERY GEOMETRY NUMBER RE-DERIVED AGAINST PANCHANG, AND THE OLD ONES WERE ALL FROM A RETIRED FACE. The `84px` numeral track carried **24.68px of dead space — a quarter of the track** — taken straight out of the reading column on the narrowest device. Measured live with the face loaded, Panchang 900 at the 24px the numeral actually runs at, all ten pairs: 01 42.16 / 02 53.72 / 03 53.91 / 04 55.95 / 05 53.75 / 06 55.19 / 07 54.16 / 08 55.77 / 09 55.19 / 10 42.16. Widest is "04" at 55.95px, narrowest "01" and "10" at 42.16px — a **13.79px spread**, because the face's digits are deliberately unequal ("1" advances 56.00px where "0" advances 119.61px at 100px/900). NO `tabular-nums` ADDED: Panchang ships no `tnum`, already measured and recorded against this face, so the class would be a no-op claiming to be a fix — the third time this repo would have shipped it. Track is now `calc(var(--text-rule-numeral)*2.75)` = **66px** against 60px of worst-case skewed ink, **6px of slack**, expressed against the token so it follows the outline floor rather than being pinned to one viewport. The skew overhangs RIGHT by 5.4px into the 16px gutter, leaving **10.6px of clearance to the text at every width** — verified, not assumed, since a left overhang would have been harmless and a right one could have collided. Reading column goes 243px -> **261px at 375px** and 188px -> 206px at 320px. Row padding 22px -> `py-6`, because 22px is not a step this system's 4px interior scale has.

CONTRAST COMPUTED AGAINST BOTH BACKGROUNDS, WHICH IS THE ROW THAT GETS MISSED — every text node here sits on the band AND on the hover fill: rule text blue-50 on navy-900 **15.69:1** and on navy-700 **11.94:1**; head label navy-200 on navy-900 **7.91:1**; numeral stroke blue-400 on navy-900 **6.71:1** and on navy-700 **5.11:1**. All five hand-computed first, then read back from `getComputedStyle` on the live hovered row, and they agree to the hundredth. The outline was confirmed to be genuinely rendering as transparent-fill + 1px stroke rather than falling back. Hairlines stay navy-700 at 1.31:1 and that is correct — they are structure, and 1.4.11's 3:1 bar binds a boundary that CARRIES a state, of which this section has none.

THE HOVER SLIDE IS GONE, AND IT IS A DELIBERATE DEPARTURE FROM DESIGN.md, which specifies the row tinting AND sliding 14px. Measured: at 375px the row is 343px starting at x=16, so the slide puts its right edge at 373px — 2px from the viewport and past the plate's own painted edge, the one line on this object that is supposed to be fixed. Structural: the order plate answered the identical question by replacing wash-and-glow with a flat fill, because a field painted on a sign does not slide. The fill also stays quiet on purpose — nothing in this section is clickable, and a loud hover promises an affordance that is not there. Side benefit that matters more than the look: the component now animates `background-color` and NOTHING ELSE, so the reduced-motion guarantee is structural rather than argued. The old 14px translate was not an animation the CSS block could remove — `transition-duration: 0.01ms` makes a transform instant, not absent, so a reduced-motion visitor got the full 14px jump.

THE HEAD FIELD IS WHY THE TITLE STOPPED BEING A KICKER. "PERATURAN SEWA LAPANGAN" is the client's own heading for this document — verbatim from the same PRD.md block as the ten rules, not ours to shorten — but a small uppercase line floating above "KETENTUAN ARENA" is dimensionally identical to the kicker-above-heading pattern DESIGN.md bans, and it read as one. Inside the plate, above the first rule and divided by the same hairline as every other field, it is a label ON the object instead of a second heading before it. Same words, different structure, and the structure is what the ban is about. Heading order stays h2 -> h3.

STAYS A SERVER COMPONENT, ZERO JS. No scroll reveal was added: the section is pure reading, the ProgressBar already animates against it, and the landing page's measured framework baseline is 126.5KB. Offered as a question rather than assumed.

VERIFIED: `pnpm check` — lint 0 errors (1 pre-existing warning in BookingForm.tsx, untouched), `format:check` all files, `check:docs — 14 checks passed over 101 files` (ketentuan-verbatim among them, rules mapped from `KETENTUAN` and never quoted), `check:unit` 115/115. Semantics re-read from the live DOM: `<ol role="list" lang="id">`, 10 items, every numeral `aria-hidden`, zero inline `style` attributes in the section. Screenshotted and LOOKED AT at 375px and 1280px. `Emulation.setEmulatedMedia` is still denied by the browse CDP allowlist, so reduced motion is verified structurally again — but the structure is now trivially correct, since one colour transition is the only animated property in the file.

THREE DECISIONS TAKEN WITHOUT THE USER, ALL FLAGGED RATHER THAN BURIED, because `AskUserQuestion` does not reach a subagent: the hover treatment (fill-only vs DESIGN.md's slide, vs dropping it), whether the plate gets scroll-reveal motion at all, and the 2px blue painted edges, which are an addition to DESIGN.md's spec rather than an implementation of it. The recommended option was built in each case and the full question set handed back for the main session to relay. One further tension surfaced rather than resolved: Hero.tsx argues that on a navy plate an outlined word "reads as a hollow punched through the sign, and this direction is made of filled fields", which is a direct precedent against outlined numerals here — DESIGN.md and this turn's brief both specify outlined, so outlined is what shipped.

[2026-08-13] [designer] ORDER SECTION REDESIGNED AS "PELAT ENAMEL" — one enamel plate, square, hard-ruled, no shadow. Six files: OrderSection, SlotCell, DatePills, SlotLegend, Section (one additive prop), and HomePage's `#order` block only. THE ARRANGEMENT IS THE ARGUMENT, not the styling: the nine cells no longer sit in gutters, they butt together and each draws its own 2px navy right/bottom rule while the grid pulls itself 2px past its container on both axes, so the outer rules land outside the plate and are clipped. Every interior rule is drawn exactly once at one, two or three columns, with no orphan-cell special case — an orphan leaves white plate rather than the navy hole a gap-as-rule approach produces. `--shadow-lg` and both radii are gone from this section; the plate's edge is 3px `blue-600`, the hero's own bottom rule closed into a rectangle.

THE BIGGEST FINDING WAS NOT COSMETIC AND NOBODY HAD MEASURED IT: THE CONTAINER QUERIES HAD NEVER FIRED, AT ANY VIEWPORT, EVER. The panel measured 288 / 343 / 380.9 / 523.7 / 353.1 / 485.9 / 477px at 320 / 375 / 414 / 768 / 980 / 1280 / 1440 — a maximum of 523.7px against its own `@min-[640px]` and `@min-[1180px]` breakpoints, so every visitor on every device got a ONE-COLUMN grid while DESIGN.md promised two from 640px and three from 1180px. It was also NARROWER on a 1440px desktop (477px) than on a 768px tablet (523.7px), because above 980px it gave 45% of an already-indented row to the legend. The cause was composition, not the query: DESIGN.md's `1.1fr 0.9fr` two-column Order section, inside Section's `md:col-start-2` heading indent. Fixed by a new opt-in `contentFullWidth` prop on Section (default false, so Ketentuan and Location render byte-identically) plus moving the legend inside the plate's head. Plate now measures 288 / 343 / 588.8 / 706.6 / 1184 / 1184px at 320 / 375 / 640 / 768 / 1280 / 1440, and the grid genuinely renders 1 column at 640, 2 at 768, 3 at 1280 — verified from `getComputedStyle(...).gridTemplateColumns`, not from the class list.

PANCHANG SHIPS NO `tnum`, SO `tabular-nums` ON THE SLOT TIME WAS A NO-OP CLAIMING TO BE A FIX — the same defect this file recorded against Orbitron and then re-shipped against a different face. Probe on the live page with the face loaded: "111" and "888" at 100px measure 169.5px and 333.81px proportionally and measure **169.5px and 333.81px, byte identical**, under BOTH `font-variant-numeric: tabular-nums` and `font-feature-settings: "tnum" 1`. Panchang's digits are deliberately unequal — "1" is 56.00px where "0" is 119.61px at 100px/800, a 2.14:1 ratio — so the nine slot strings span 159.94px to 180.86px at 16px and no CSS closes that. Class removed rather than left in place with a comment. (Plus Jakarta Sans DOES ship it: 111/888 go 120.91/189.61 proportional to 180.00/180.00 tabular. It is the display face that cannot.)

EVERY GEOMETRY NUMBER IN THESE FILES WAS RE-DERIVED, and the stale ones are gone. SlotCell's "Menunggu Konfirmasi measures 145.5px at the sm step" was measured in a retired face; the real figure is **143.33px**, and it no longer binds anything — the HOUR is the binding string now that it runs at `--text-h3` (20.08px at 375px, 24.52px at 768px, capped 32px). Widest of the nine ("06.00 - 08.00") measures 220.9 / 221.6 / 270.7 / 334.6 / 353.4px at 320 / 375 / 768 / 1280 / 1440. Breakpoints reset to `@min-[640px]` / `@min-[1120px]` on the PLATE; 640 matching the old number is a coincidence, not a value carried forward. Tightest cell in the system is 3 columns at 1440px: **13.9px of slack**, and it was **5.6px** until the `0.02em` tracking came off the hour — DESIGN.md specifies that tracking for a 16px time in a narrower face, and at the 32px ceiling it cost 8.3px across thirteen characters. The nine strings cannot drift underneath this: they are `TIME_SLOTS`, guarded by `check:domain` and by check:docs' `slot-canonical-drift`.

ONE LIVE DEFECT FOUND BY MEASURING BEFORE WRITING, NOT BY REVIEW. The free-run badge carried a flat `text-[var(--color-interactive)]`, and a span's own colour beats the `!text-...` the button sets on itself — so selecting the one badged slot rendered `rgb(37, 99, 235)` on a `rgb(37, 99, 235)` fill. **1.00:1**: the sentence did not fade, it vanished, on the single cell in the grid with something extra to say. Read live at 375px on 16.00 - 18.00 with `getComputedStyle`. Now white, 5.17:1, and visible in the committed screenshot. The identical failure one step away — the same badge on the new navy hover fill — is covered by the same branch at 6.71:1.

CONTRAST COMPUTED, NOT EYEBALLED: every element in `#order` carrying its own text node, composited against the first opaque ancestor background. Twenty distinct fg/bg/size combinations, **all pass**, resting and with elapsed expanded and a slot selected. The five slot fills: available 17.07:1, hovered 17.07:1 hour with a 6.71:1 label, selected 5.17:1, pending 6.37:1, booked 6.80:1, elapsed 5.61:1. A GAP FOUND IN DESIGN.md WHILE DOING IT: the `#order` heading's accent word is `blue-600` on the `blue-50` page ground, which computes **4.75:1** — it clears both bars (large text at 56px, and AA outright), but DESIGN.md has no figure for that pair anywhere; every documented blue-600 ratio is "on white". PROGRESS.md's 2026-08-12 entry avoided the same pair by hand-computing it and choosing navy instead. It should be written into the Colors table rather than re-derived a third time.

MOTION: reveal-on-enter plus parallax, both through `lib/motion.ts`, no direct `gsap.to()` anywhere. Rows stagger 40ms top to bottom; verified it fires at the threshold and NOT before (grid top 1103px → opacity 0, grid top 603px → opacity 1 against a `top 85%` start on an 812px viewport) and does not repeat after scrolling away and back (opacity 1, transform none). PARALLAX IS ARGUED, NOT SPRINKLED: a flat sign has no depth, so the depth is layers of sign — an oversized outlined "WITA" in the band above the plate, drifting at 0.12 of the plate height, scrubbed, sinking behind the plate's top edge. It is a WORD and not a number on purpose: the obvious ghosts here are the free-slot count or the 06–24 envelope, and no number may appear in this section that could read as a price. Clipped by the wrapper's own `overflow-hidden` because "WITA" is 4.16x its font size in Panchang — 399.3px at the clamp floor inside a 343px plate.

REDUCED MOTION IS THE ONE THING I COULD NOT MEASURE DIRECTLY, AND SAYING SO IS THE POINT: `Emulation.setEmulatedMedia` is denied by the browse CDP allowlist, so there is no emulated render behind this. Verified structurally instead — the SSR HTML carries **no inline `style` attribute anywhere**, so nothing starts hidden in the markup and the resting state IS the finished state; all four `settle` bodies are correct against that (three empty on purpose, the elapsed one sets its own height because a collapsed height is JS-set); and the CSS half in globals.css neutralises the ring keyframe and every colour transition. Worth an emulated pass whenever the allowlist permits one.

PRODUCT TRUTH PRESERVED AND RE-VERIFIED FROM MOCK DATA RATHER THAN FROM THE CODE: clicking the first five date pills renders elapsed 2 / available 5 / pending 1 / booked 1 on today, and a fully-booked day reads "Tidak ada slot kosong di tanggal ini" with nine booked rather than an empty grid. `aria-disabled` never native `disabled`, `aria-pressed` on selection, deselect by re-tapping, the free-run affordance at most once per date, scarcity counting only genuinely available hours, `id="order"` unchanged, and the date row keeping `overscroll-behavior-x: contain` with its scrollbar hidden. Elapsed stays neutral and is never rendered as booked.

OVERFLOW: `#order`'s own `scrollWidth - clientWidth` is **0 at 320/375/414/640/768/980/1120/1280/1440**, with zero unclipped escapees. Page-level overflow is 45/41/22/131/57 at 320/375/414/768/1280 — **byte-identical to the pre-change baseline**, and all 18 offenders at 375px are inside `#lokasi` with `inOrder: false` for every one. That section is built for the retired face and is a later turn's.

TWO DECISIONS TAKEN WITHOUT THE USER, BOTH FLAGGED RATHER THAN BURIED. `AskUserQuestion` is not available inside a subagent — the exact tool/duty mismatch `ui-designer.md` was created to fix for `software-engineer`, recurring one level down — so the four-question motion and structure round that `design-process.md` requires BEFORE code could not be asked. The recommended option was built in each case and the full question set handed back for the main session to relay. Also a composition judgement a reviewer may want to reverse: with `contentFullWidth` the heading stays indented past the numeral while the plate spans the full width, so the plate's left edge no longer aligns with the lede. It reads as a full-bleed figure under an indented paragraph and it is what buys the product its width back, but it is the one thing here that is taste rather than measurement.

VERIFIED: `typecheck` clean, `lint` 0 errors (1 pre-existing warning in BookingForm.tsx, untouched), `format:check` all files, `check:docs — 14 checks passed over 101 files`, `check:unit` 115/115. DESIGN.md is NOT updated by this turn and now contradicts the build in the Order section, Slot Cell and Date Pill specs — that is expected, since it is written from the built result at the end of the redesign, but it is currently the stalest document in the repo for this section.

[2026-08-12] [engineer] LocationBlock.tsx built — the stacked display lines, metadata list (Alamat/Jam operasional/WhatsApp) and the designed map placeholder for the `#lokasi` section, per DESIGN.md's Location section spec. Scoped to exactly one new file by the launching agent, with Section/HomePage/Hero/SiteHeader/globals.css owned elsewhere this turn. ONE OPEN CONTENT DECISION MADE WITHOUT USER SIGN-OFF, flagged for review: DESIGN.md specifies "stacked uppercase display lines with the middle line outlined" but never states the words, unlike the hero/closing headings whose text is already fixed elsewhere in the doc. AskUserQuestion is unavailable inside a subagent, so chose ARENA / PLAYER (outlined) / LOMBOK — brand name plus the market region PRODUCT.md already confirms ("Market: Lombok, Nusa Tenggara Barat — and only Lombok"), reasoned as distinct information from the Section's own small "Datang & Main" heading rather than a restatement of it, and inventing nothing (Lombok is an established fact, not a specific unverified address). Worth a deliberate confirm-or-override pass before ship. TODO(content) applied twice, both category "address + maps coords": the Alamat row and the map placeholder's note — no address or coordinate invented anywhere, per the hard rule. VERIFIED, not assumed: `pnpm typecheck`/`lint`/`format:check`/`check:docs`/`check:unit` all pass (`check:docs — 14 checks passed over 101 files`; `115 passed (115)` unit tests, no regressions). Browser verification hit heavy flakiness from a shared browse daemon under concurrent multi-agent load (other sessions editing HomePage.tsx/Hero.tsx/globals.css/DESIGN.md live), so live-page injection kept losing state; switched to an offline harness — the dev server's actual compiled CSS chunk fetched and inlined into a standalone local HTML file, fonts resolved via `<base href>` pointing at the running server — for reliable, repeatable measurement. Confirmed: the three display lines (194.4px/209px/243.8px) all clear the 343px content box at 375px with room to spare (longest is "Lombok" at ~71% of the box); the map placeholder is exactly 4:3 (343×257.25px at 375px); computed contrast of the metadata muted text on the page ground is 6.38:1 (matches DESIGN.md's documented navy-400-on-blue-50 figure exactly), the WhatsApp link (navy-900, not the interactive blue — see below) is 15.69:1, and the map note against the gradient's blue-100 end-stop is 5.69:1 — all clear AA with margin. ONE DELIBERATE DEVIATION FROM THE OBVIOUS TOKEN CHOICE: the WhatsApp link uses `--color-fg` (navy-900) rather than `--color-interactive` (blue-600), because DESIGN.md never clears interactive-blue text sitting directly on the flat `blue-50` page ground — every documented blue-600 contrast figure in the Colors section is "on white" — and hand-computing it lands at ~4.75:1, an untested, borderline combination this design system has apparently never shipped. The existing WhatsApp link elsewhere on this page already makes the same choice for the same reason, so this follows precedent rather than introducing a new risk.

[2026-08-11] [decision] MARKET NARROWED TO LOMBOK (NTB), by the user. Recorded in PRODUCT.md Users + Operating Context, and in agent memory. Consequence found while recording it, NOT part of the ask: the entire date layer pins Asia/Jakarta (WIB, UTC+7) but Lombok is WITA (UTC+8, Asia/Makassar) — todayInJakarta, isPastSlot, the elapsed collapse, and the booking window all drift one hour if the field is really in Lombok. Logged as OPEN in PRODUCT.md Operating Context rather than fixed, because the fix crosses the frozen src/domain/ layer (admin repo must re-copy), its tests, and every "Asia/Jakarta" mention in both repos. Waiting on the user's explicit go before the sweep.

## Phase 1b — Design foundation

[2026-08-08] [user] CLIENT DIRECTIVE recorded: "minimalist UI, but modern — with many animations, transitions, and micro-interactions." Written into DESIGN.md Overview as an INPUT to Phase 1b task 1, explicitly NOT as a north star — the Overview still says the north star is unset, and promoting a client sentence into one would pre-empt the task the phase exists to run. TENSION RESOLVED as minimal FORM, rich BEHAVIOUR: few elements each responding precisely, on the reasoning that restraint is what makes dense motion legible — the same effects on a busy layout read as noise. "Minimalist" confirmed to mean few elements + whitespace, NOT reduced colour; the monochrome reading would have collided with the status triples, which are an accessibility requirement (pending 6.37:1, booked 6.80:1) rather than decoration. SCOPE SPLIT, recorded as a rule not a preference: expressive motion in hero / content / section transitions; the ORDER SECTION gets functional feedback only (state change, selection, elapsed collapse). Exempt for a stated reason — it is where the booking happens, where the per-section Lighthouse gate bites hardest, and where Product Principle 4 applies most sharply, since the organiser is deciding fast with 8-12 people waiting. Motion that delays that decision fights the outcome the client is paying for, so withholding it there is the directive being honoured rather than ignored. EXPLICITLY UNCHANGED and stated in the doc, because this is the half that gets forgotten: every effect is still user-chosen via AskUserQuestion before code (the directive raises the ceiling, pre-approves nothing), everything still routes through lib/motion.ts (more motion = more reduced-motion surface, not less), and no-CLS plus the performance budget are untouched — on a mid-range Android in an in-app webview the binding cost is CPU per frame, so an effect can pass the KB budget and still fail the Lighthouse gate. Also added to PRODUCT.md Brand Commitments as stated-by-client with a pointer to DESIGN.md for the resolution (not restated, to avoid a duplicated-rationale drift surface), and as question 5 on the 1b client gate so the deliberate absence gets confirmed rather than read as a shortfall.

[2026-08-07] [review] Client checkpoint 1b prepared — the DoD item that had never happened. Two artifacts. (1) ALUR section added to DESIGN.html, closing the critique's outstanding P0: the page was a component inventory when PRD 1b defines tasks 4+5 as one artifact whose handlers prove the landing -> order -> form journey. Three frames at the real 375px width: order section (today preselected, scarcity line, collapsed "Sudah lewat (6)", bookable slots), the literal WhatsApp message the CTA produces, and the form outcome INCLUDING THE 409 — which was rendered nowhere despite being the highest-stakes moment the product has and the entire reason uniq_active_slot exists. Built to the confirmed brief in .impeccable/surfaces/app-page-tsx.md. No motion, per the ask-before-animating rule and because static frames are correct for a walkthrough. (2) docs/checkpoint-1b.md — the record that makes it a checkpoint rather than a conversation, since delivery is a live screen share where nothing otherwise survives the meeting. Pre-written questions ordered by what they block: WhatsApp-only handoff (BLOCKS PHASE 2, and the client has never been asked despite the whole flow being redesigned around it), pricing (BLOCKS PHASE 3), the six missing content items, traffic channel, single-field confirmation. Plus a sign-off line. TWO SELF-INFLICTED FINDINGS caught by re-running the detector: I hardcoded #E7FFDB for a WhatsApp-green bubble — raw hex outside the palette on the one page whose own rule forbids it, and a brand colour that is not Arena Player's; replaced with --color-bg-subtle. The border-left I used instead then tripped the side-tab slop rule; dropped. Detector back to the 3 known false positives, no new findings.
[2026-08-07] [setup] PROGRESS.md split. All 38 planning-phase entries moved verbatim to progress-archive/00-planning.md; nothing rewritten, only relocated. Reason: the file had reached 44 KB and arena-gotchas plus all six agent files instruct agents to read it before working — roughly 11k tokens of context tax per agent per session, growing unboundedly. Deliberately did NOT distil "standing decisions" into this file: those already live in PRD/architecture/DESIGN/PRODUCT, and restating them would create a fourth drift surface, which this project has already paid for three times.
[2026-08-07] [review] /impeccable redesign on docs/DESIGN.html. Concept-seed roll (key 7a4384e8, mode read) dealt index 4, 'Papan Jadwal' — an Indonesian enamel signage world. USER PINNED THE CHALLENGER instead: brick build-instruction book. new-work.md permits this explicitly ('a user- or brief-pinned direction beats the roll, always'); the roll's job was to stop the run converging on my own top-ranked pick, and it did. Two challengers disposed of on record: Ikeda datamatics cannot honour a locked commitment (pure black-and-white contradicts navy #011A43 AND the client-named light/blue-white anti-reference — a factual failure, not taste), and the hand-drawn zine went to the re-roll pool (cream-plus-marker is the rendition default new-work.md warns about, and hand-drawn undercuts a document whose credibility rests on printing verifiable numbers). THE FUSION: every colour keeps both its instruction-book role and its product meaning. Ground = blue-50, keyline ink = the client's logo navy, inventory tags = amber-300. The source world's 'brick red for the new part' maps to Signal Blue because RED ALREADY MEANS BOOKED here — taking it literally would have collided with a state meaning, and clarity wins conflicts. STRUCTURE follows the form natively: parts inventory first, nine numbered assembly steps, the Alur journey as the finished model. The four open decisions sit BEFORE step 00, which the reviewer called the best structural decision on the page. BLOCKED ON A COLLISION FIRST: the other session had 152 uncommitted lines of Alur work plus a brand-new checkpoint-1b.md whose walkthrough script names this file section by section, and whose sign-off options end with 'Redirect — direction rejected, return to Phase 1b task 1'. A redesign IS a Redirect, and the checkpoint exists to let the CLIENT trigger one. Raised before touching anything; user chose to proceed and carry Alur forward. Their work committed verbatim at ff8f2a7 first so a wholesale rewrite could not destroy it. TOKEN CHANGE THAT ESCAPED THE DOCUMENT: grey-500 computes 4.44:1 on the blue ground and fails AA, so --color-fg-muted moved to a hue-tinted navy-400 (6.94:1 white, 6.38:1 blue). Strictly better on white too. FINISH REVIEW ran fresh with no inherited context and returned REWORK: the page argued a three-layer indirection its own stylesheet did not practise — 17 Layer 3 tokens skipped Layer 2 (there were NO semantic state tokens at all) and the worked example printed on the page was a two-hop chain described as three. Also: clicking a slot in Alur cleared the step 04 demo (toggleGroup fell back to document); frames claimed 375px and rendered 335px; selected pill day label 4.20:1; the guide arrow drew in a 27ms flash inside an authored 420ms because the box was 1068px holding 202px of centred ink; the checkmark was a Unicode glyph that fell out of Archivo into the platform sans; and nothing in the system had a loading, submitting, empty or failed state. Fixed all nine, plus fired --new-part which had a token, a comment justifying it, and zero applications — the reviewer's judgment was that its absence, not the hue remap, is what drained the world. SECOND VERDICT: conditional, with TWO REGRESSIONS FROM MY OWN FIX BATCH. The frames grid I 'fixed' put 1189px of content in a 1068px column, scrolling the page horizontally at 1240-1327px and clipping the 409 panel — the one thing checkpoint-1b.md calls the most important on the page. And .btn[aria-disabled=true] had no CSS rule at all, so the submitting state was byte-identical to idle, directly beneath a paragraph explaining why aria-disabled is correct. Both fixed and verified by objective measurement (no horizontal scroll at 375/1280/1300/1440; submitting computes grey-200 vs navy idle) but NOT re-scored by the reviewer. STILL OPEN by agreement: FIRST VIEWPORT (inventory on the second scroll), native disabled on Kirim, unstyled ::placeholder at 2.35:1, 'Sudah lewat (6)' expanding to 2 rows, and the expanded elapsed rows rendering in the booked red triple while labelled 'Sudah lewat' — which contradicts step 04's own 'elapsed hours are not booked hours' and is pre-existing, not from this build. DISCLOSED: this install ships neither impeccable-finish-reviewer nor impeccable-documenter nor a degraded/ reference. The review ran in a general-purpose agent with no inherited context (the property that matters); the documenter pass ran in-thread.

## Phase 1a — Engineering foundation

[2026-08-08] [build] Step 02 scaffold. Next 16.3.0 + React 19.2.8 + Tailwind v4 + TS 5.9.3, 25 packages pinned exactly, `pnpm dev` serves 200 with `<html lang="id">`, typecheck and lint clean. THE BUDGET IS BREACHED AND THE ESTIMATES WERE 30% LOW. Measured `/` on a modern browser (excluding the 38.7KB `noModule` polyfill legacy browsers only): framework 126.5KB against an estimated 90, GSAP+ScrollTrigger 43.6 vs 35, axios 17.5 vs 13, TanStack Query 10.0 vs 13, date-fns+tz 8.1 vs 4-6, react-icons 2.2 vs 1-2, zustand 0.7 vs 1. Subtotal 208.6KB against a 200 ceiling WITH ZERO COMPONENT CODE, where the plan assumed ~18-21KB of headroom. TWO THINGS WENT RIGHT: react-icons, flagged as a barrel-tree-shaking gamble, came in at 2.2KB for six icons so the extract-the-glyphs fallback was never needed; and React Compiler, which Next 16 enables by default, costs 0KB and stays on. USER CHOSE the 61KB fix: GSAP moves behind a dynamic import in `lib/motion.ts` (-43.6, a one-file change because hard rule 6 already funnels every animation through it, at the cost that a hero ENTRANCE must be CSS since nothing can animate before the chunk lands), and axios becomes a `/booking`-only dependency (-17.5, since `/` makes one GET that native fetch handles in 0KB, while `/booking` keeps axios for `onUploadProgress` on the 2MB proof upload which fetch cannot report). `/` now sits at ~147.5KB with ~52KB of real headroom. axios joins zod and react-hook-form under the same route-split rule.

[2026-08-08] [build] Step 02 also surfaced FIVE TRAPS worth not re-learning. (1) Next 16 REMOVED the per-route First Load JS table that Next 15 printed, and Turbopack emits no app-build-manifest.json, so step 08's planned `check:budget` had no output left to parse; `--experimental-analyze` prints a route count and nothing sized. Replaced with `scripts/measure-bundle.mjs`, which gzips `.next/static` and splits shared from route chunks using build-manifest.json. More honest anyway — it counts what ships. (2) `pnpm view <pkg> version` returns the `latest` DIST-TAG, and sorting the versions array by publish order returns something else entirely: it reported react 19.0.8, which is the `backport` tag, not 19.2.8. That same read caught next@15.5.23 sitting on `backport` — Next 15 is in maintenance — which is why this project is on 16. (3) `latest` is not always SUPPORTED: TypeScript's latest is 7.0.2 and ESLint's is 10.8.1, but create-next-app@16.3.0 pins ^5 and ^9; the peer ranges permit the newer majors and permitting is not testing. Took Vercel's combination. (4) pnpm's `minimumReleaseAge` policy rejected react-hook-form@7.85.0, published hours earlier — fixed by taking 7.84.0 rather than relaxing the policy, and editing package.json was NOT enough because the lockfile held the old resolution until `pnpm clean --lockfile`. (5) The icon probe route was first written as `app/_probe/` and SILENTLY MEASURED NOTHING — App Router treats a leading underscore as a private folder and excludes it from routing. The build succeeded, the route table simply had no probe in it, and the totals were identical. Caught by reading the route table rather than trusting the number.

[2026-08-08] [build] Step 01 (fc28921). Both open library choices closed: date-fns v4 + @date-fns/tz, and react-icons — chosen because no mainstream set still ships a WhatsApp brand mark and six glyphs are needed. Route split given a MECHANISM instead of an intention: structure (form libraries only under app/booking, app/api and lib/validation.ts, where route handlers are server-side so zod costs the client nothing), an ESLint no-restricted-imports zone rule that fails at author time with a readable message, and per-route check:budget as the backstop for transitive imports lint cannot see. CONSEQUENCE NAMED RATHER THAN DISCOVERED: choosing a date library over native Intl means lib/shared/dates.ts imports date-fns, and lib/shared/ is copied byte-identical into the admin repo — so that repo is now obliged to install it. check-shared.mjs therefore diffs the shared peer dependency's version range as well as the file bytes, because date-fns v3 and v4 differ in exactly the timezone API this relies on and two repos on different majors would produce a byte-identical dates.ts computing different dates.

[2026-08-08] [review] Pre-flight (commit 7a7dbeb). FOUR CONTRADICTIONS found in documents marked locked, all of which would have been discovered with code already written against the losing side. `notes` said 280 in the API contract and 500 in both the PRD field list and the notes_length SQL constraint — 500 won, since two sources agreed and one of them was the database. The folder tree still listed dates.ts/slots.ts/validation.ts at lib/ top level, contradicting the lib/shared/ contract added ONE COMMIT EARLIER: that commit's grep used the pattern `lib/dates`, which never matches a tree line written `│   ├── dates.ts`. The shared-contract table gave validation.ts the proof upload constraints while two other files said lib/proof.ts — resolved toward lib/proof.ts, because the admin reads proofs and never uploads them. Task numbers drifted three ways, including architecture.md calling the budget "task 7" and "task 8" three paragraphs apart. The Phase 1a DoD also had NO checkbox for task 8 at all, so the measured budget, check:budget and lib/motion.ts could have been skipped with every box ticked. Board completed to 8 step files; step 05 marked verify-don't-rewrite because the API contract was already written during planning.

[2026-08-09] [build] Step 02b — RESTRUCTURE to src/ + feature-driven modules, inserted between steps 02 and 03 at the user's request because the flat layout was unfamiliar to work in. Done now because it was the cheapest it would ever be: app/ held three files, components/ lib/ mocks/ were empty directories, and the admin repo still has zero code. It had to precede step 03, which authors the ESLint route-split zones and check:docs — both hard-code paths, and writing them against the old layout then rewriting them is the mechanical-edit defect class this repo has lost time to three times. NEW SHAPE: src/{app,modules,domain,server,services,components,lib,utils,mocks}. Modules are named after SURFACES — home renders /, booking-form renders /booking — and NEVER import each other; src/lib/ is narrowed to polish for installed libraries (cn, motion, query-client) and src/utils/ holds web-only helpers (error, formatter). db/ stays outside src/ because it is SQL run by hand, not source anything imports.

[2026-08-09] [decision] The frozen shared layer is now `src/domain/`, four files instead of three: slots.ts, dates.ts, status.ts, phone.ts. Took four rounds of naming to land. `shared/` was rejected because once src/modules/ exists, "shared" reads as _shared between modules_ and attracts the first cross-module button component into a folder that can never accept one. `constants/` was rejected because two-thirds of the contents are functions. `utils/` and `helpers/` were rejected because those are the names you reflexively want LATER for web-only helpers — spending them on a frozen folder sends the reflex to the one place that cannot accept it. The proposal to move it into src/modules/booking/domain/ was rejected on three counts: / needs TIME_SLOTS and isPastSlot for the order-section grid so home would cross-import booking; the ESLint zod zone stops being expressible as a clean folder boundary the moment / imports from the booking module; and the admin repo would have to invent a modules/booking/ folder for a surface it does not have, with its natural module being `bookings` plural anyway. Modules are named after surfaces, the domain after the business — different axes, which is why the module was renamed booking-FORM: / is also about booking, and one word meaning two things is what caused the confusion.

[2026-08-09] [decision] validation.ts SPLIT into status.ts + phone.ts, because half of it was vocabulary rather than validation — the same name-vs-contents mismatch that disqualified `constants/`. status.ts carries the two status vocabularies, ACTIVE_STATUSES mirroring uniq_active_slot's WHERE clause, and toSlotStatus() encoding the 4→3 mapping that architecture.md calls the half that gets guessed wrong. THREE OF THE FOUR DOMAIN FILES HAVE NO DEPENDENCIES and must stay that way: only dates.ts imports date-fns, so importing TIME_SLOTS costs no date library — and every package added there is one the admin repo is obliged to install. Merging slots into dates was considered and rejected for exactly that reason. src/domain/ also imports its own siblings RELATIVELY (./slots, never @/domain/slots), the one documented exception to @/-everywhere, so a byte-identical copy resolves the same in both repos regardless of either tsconfig.

[2026-08-09] [build] cn() added with clsx 2.1.1 + tailwind-merge 3.6.0, both resolved by dist-tags rather than by sorting versions — the trap that already produced two wrong pins here. MEASURED BEFORE APPROVING, three builds with each probe isolated against a control route so the "use client" boundary is subtracted rather than blamed on the library: control 0.5KB, clsx alone 0.2KB, clsx + tailwind-merge 8.2KB, so tailwind-merge accounts for 8.0 of it. User chose to keep both; the clsx-only fallback is measured and is a one-file swap since cn()'s signature does not change. Budget subtotal on / is now 155.7KB against the 240 ceiling, headroom ~84KB. Probe routes deleted in the same session.

[2026-08-09] [gotcha] `next build` REGENERATED .next/dev/types/validator.ts still pointing at ../../../app/page.js after the move, so typecheck failed on a file no longer in the tree while the build itself reported "Compiled successfully". Deleting just .next/dev was not enough — the next build recreated it from cached state. Clearing all of .next fixed it. Worth knowing before concluding a move broke the code: the compile succeeded the whole time.

[2026-08-09] [gotcha] The scripted path sweep introduced a defect of its own: lib/db/ and lib/storage/ both map to src/server/, so code-reviewer.md ended up reading "confirm it is imported in `src/server/` and `src/server/`". Caught by re-grepping rather than by trusting the sweep's own success output. Every replacement pattern carried a (?<!src/) lookbehind to prevent src/src/ double-prefixing, and a repo-wide grep for src/src/ confirmed none — but the lookbehind guards the wrong-path failure, not the collapsed-distinction one. Two files were also missed by the first sweep list entirely (PRODUCT.md, and one line in the database skill), found by a second grep pass.

[2026-08-09] [build] Step 03 — DX harness (d19b72f, 38932a9). prettier 3.9.6 + eslint-config-prettier 10.1.8 (dist-tags, pinned, dev-only, 0KB), vitest.config.ts carrying the SAME @/ -> ./src alias as tsconfig because a drift there makes tests assert against a file the app does not ship, check:lib = `vitest run src`, and check:docs with seven checks. EVERY CHECK PROVEN TO FAIL THEN REVERTED: check:docs on a planted TODO(phase2), a planted module barrel, a planted cross-module import, and an undeclared TODO(content) category; eslint on @/server from a module, axios on the landing module, the @/ alias inside src/domain/, and MSW v1 rest.get — plus the negative case, zod inside src/server/ staying CLEAN, which proves the allow-list works rather than the rule being off. check:lib verified with .env.local moved aside.

[2026-08-09] [gotcha] THE STOP HOOK FAILED ITS OWN FIRST TEST — exit 0 on a real violation, the exact never-fires defect this repo already shipped once, caught only because the test existed. Cause: check-docs.mjs wrote findings to stderr, and PowerShell 5.1 wraps every stderr line from a native command in a NativeCommandError; under ErrorActionPreference=Stop that is terminating, the hook's own catch swallowed it, and it exited 0. Fixed at the source rather than the symptom — findings go to STDOUT, the exit code carries pass/fail, and no 2>&1 is needed anywhere. Re-tested all three paths: clean exits 0 silently, a violation exits 2 with a readable message, stop_hook_active exits 0 without recursing.

[2026-08-09] [decision] check:docs asserts NO ORPHANS on TODO(content), not all-six-present. Only one of the six declared categories has code today; the other five mark content that arrives in Phase 2+, so all-six-present would have shipped red or been quietly deleted. It also distinguishes a marker from prose ABOUT a marker: docs write `TODO(phase2)` in backticks and "Phase 1" in quotes when naming what they forbid, so prose is scanned with code spans, fenced blocks and quoted spans stripped, and marker checks only read files that can carry a real marker. The first run reported ~40 findings, nearly all of them the checker matching its own patterns. NO --passWithNoTests: check:lib was red with zero tests, and a check that passes when it ran nothing is the antipattern this project keeps warning about — cn() is non-trivial shipping logic so per hard rule 8 it earned a real test instead.

[2026-08-09] [gotcha] TWO THINGS THE NEW CHECKS CAUGHT ON THEIR FIRST REAL RUN. database.md said "once the Phase 1 build starts" — bare Phase 1, fixed to 1a. And src/lib/cn.test.ts failed on MY OWN wrong assumption: tailwind-merge treats flex and block as one display conflict group, so cn("flex","block") is "block", not "flex block". Pinned as a test since it is the non-obvious half of what the 8.0KB buys. Separately, `git add -A` swept in files prettier had reformatted that are not ours to format — all 398 lines of .impeccable/design.json, which /impeccable document regenerates, so the diff would flip-flop forever. Restored from 7f58728 and added .impeccable/ plus docs/progress-archive/ to .prettierignore. PROGRESS.md deliberately stays formatted: it is appended every step.

[2026-08-09] [build] Step 04 — docs/dev-rules.md. No code. Named lowercase-kebab to match architecture.md, database.md and design-process.md: the UPPERCASE names in docs/ are externally fixed (PRD is a standard acronym, DESIGN.md is mandated by the Stitch format spec) and everything the project named for itself is kebab. 14 references across both repos already said dev-rules.md, and after four missed sweeps this session, casing is not worth a fifteenth. Content is deliberately the layer BELOW CLAUDE.md — conventions whose violation costs a review comment, not a phase — and it restates nothing: where another doc is the authority it is linked. The four ambiguous folder pairs each get a one-question test, the sharpest being "does this file exist because of a package in package.json?" for lib/ versus utils/. Accessibility baseline is the part with teeth because nothing fails when it is skipped: the 409 focus move is called out specifically, since without it a screen-reader user never learns their slot was taken. Added to CLAUDE.md's docs table (121 lines now, still under the ~95-gets-read/300-gets-skimmed threshold it names).

[2026-08-09] [gotcha] Step 04's own acceptance grep was stale in the same way step 03's was: it searched for "never imports from", which is pre-02b phrasing from when the rule read `lib/ never imports from app/`. Replaced with three greps covering all three current import rules. That is the fifth instance this session of a check or doc still describing the layout that existed before the restructure — every one found by reading, not by a command, which is precisely why check:docs now exists.

[2026-08-09] [reviewer] Checkpoint over d19b72f/38932a9/4ff1f4d before step 06 writes src/domain/. Verdict FIX-FIRST, and the timing was the point: four files were about to be written against guards that did not hold. FOUR REAL GAPS, each confirmed by probe rather than by reading. (1) The src/domain/ ban was ALIAS-ONLY — `@/lib/cn` errored, `../server/db` linted clean, in the one folder whose documented house style IS relative imports, so the wrong import is a single keystroke from the right one; and the failure is invisible twice over, because the admin repo has no src/server/ or src/utils/, meaning such a file stays byte-identical (check:shared passes) and simply does not build over there. (2) Import rule 1 — nothing under src/ imports from src/app/ — was enforced NOWHERE, while CLAUDE.md and dev-rules.md both said "three import rules, all lint-enforced". src/domain/ got it incidentally through the @/* ban; modules, lib, utils, components and services got nothing. A doc overstating a guard is the failure class this repo has already paid for. (3) Relative cross-module imports passed BOTH named guards: eslint matches `@/modules/*` and check-docs check 5 was a literal `line.includes("@/modules/" + other)`, so `import { schema } from "../booking-form/booking-form.schema"` inside src/modules/home/ was clean on lint AND check:docs — precisely the import CROSS_MODULE's own message says ships zod to /. (4) The booking-form block hardcoded `@/modules/home` instead of the CROSS_MODULE wildcard, so it alone would exempt a third module the day one exists. Also dismissed one suspicion on the record so nobody "fixes" it later: the domain block's bare `["@/*"]` looks like it should miss two-segment `@/domain/slots`, but eslint's pattern matching crosses `/` and it errors at both depths — the two-pattern spellings elsewhere are belt-and-braces, not a template.

[2026-08-09] [build] Guard fixes from the checkpoint, every ban re-probed with `eslint --stdin --stdin-filename` on both sides. ESLINT: new APP group bans `@/app`+`@/app/*` repo-wide, with src/app/** overriding it to nothing (app composes itself — a layout importing its page is the boundary working) and src/server/** keeping it, since server sits BELOW the extraction boundary while a route handler does not; the old combined `src/app/api/** + src/server/**` block is split for that reason. src/domain/ gains a `["../*"]` group — `./slots` unaffected, any `..` rejected. booking-form takes CROSS_MODULE instead of the home literal. Six must-error cases fire and four must-be-clean cases stay clean, including api importing @/server + zod and booking-form importing zod + axios. CHECK:DOCS now 10 checks: check 5 resolves relative specifiers with posix.normalize and reports cross-module-import, app-boundary and domain-escape, which is the half no glob can express — banning `../*` outright would break `home/components/x.tsx` importing `../home.service`, which is correct and routine. Plus unbalanced-fence, because the fence toggle FAILS OPEN and a stray ``` silently disabled checks 1-3 for the rest of a file. SHIPS gains .html so docs/DESIGN.html is scanned for markers — it was in neither SHIPS nor isProse, and hero copy is one of the six TODO(content) categories. Check 7 now scans .claude/agents/ and .claude/hooks/ as its own comment always claimed, and matches integer ratios (3:1 is the WCAG 1.4.11 bar and was invisible to a decimals-only pattern). vitest include widened to .test.{ts,tsx} so a Phase 2 component test cannot be silently uncollected while check:lib reports green.

[2026-08-09] [gotcha] TWO OF MY OWN FIXES WERE WRONG ON FIRST RUN, both caught by probing rather than by reading. unbalanced-fence reported three times per file because checks 1, 2 and 3 each walk the same file through scannable(); deduped with a module-level Set. And the ratio comparison was `flatDesign.includes("3:1")`, which is TRUE for any doc containing "14.53:1" — substring matching quietly accepts every integer ratio that shares digits with a decimal one. Replaced with set membership over ratios parsed from DESIGN.md by the same regex. The first probe of that check passed against a value DESIGN.md does not contain, which is exactly the never-fires shape this repo keeps re-learning; re-probed with 9.99:1 and 7:1 and both are now reported.

[2026-08-09] [decision] The "where does a thing go" table in dev-rules.md is now ORDERED, because unordered it mis-answered one of the four files step 06 was about to write. `dates.ts` exists because of date-fns AND the admin repo needs it, so the lib-vs-utils question — the one the step 03 commit called the sharpest — sends a frozen, byte-identical file to src/lib/ when asked first. Three of the four domain files hit the same contradiction more mildly. The admin question now comes first and stops the walk, on the reasoning that it is the only one whose wrong answer is a contract in two repos rather than a move. Also corrected eight folders to nine (src/mocks/ was never accounted for) and added check:shared to the verification table before it goes stale one step from now.

[2026-08-09] [build] db/migrations/20260809_create_bookings.sql — the folder had been empty since the repo started while database.md said the file lived there. Copied byte-identical from the SQL block in database.md and asserted so with a round-trip comparison, not by eye. Nothing runs it; it is still pasted by hand into the Neon SQL editor. It exists so ACTIVE_STATUSES has a real artifact to be checked against rather than a paragraph.

[2026-08-09] [build] Step 06 — src/domain/ is no longer a .gitkeep. slots.ts (TIME_SLOTS, canonicalSlot, slotStartHour), dates.ts (todayInJakarta, bookingWindow, isBookingDateString, isWithinBookingWindow, isPastSlot), status.ts (the two vocabularies, ACTIVE_STATUSES, toSlotStatus) and phone.ts (normalisePhone, isValidIndonesianMobile), 40 tests passing. THREE OF FOUR STAY DEPENDENCY-FREE as required; only dates.ts imports date-fns + @date-fns/tz, and it imports its sibling RELATIVELY (./slots) so the copy resolves in both repos. THE TZDateMini QUESTION IS MOOT: with the hoisted `{ in: JAKARTA }` context, no TZDate or TZDateMini is ever constructed at all — plain Date objects flow through, so the toISOString ban is enforced by the method being out of reach rather than by review, and the class costs nothing because it is never imported. Bundle impact is re-measured in step 08, where / actually imports this; a probe route today would measure a page nothing renders. canonicalSlot returns NULL rather than the cleaned-up input for an unrecognised slot — the tolerant version would let '07.00 - 09.00' through as a real slot nobody sells.

[2026-08-09] [build] check:shared + scripts/check-shared.mjs, wired as `check:lib = node scripts/check-shared.mjs && vitest run src` so it cannot be skipped. Diffs BOTH DIRECTIONS (a one-way walk cannot see a file present on the far side and absent here), diffs TESTS as well as sources on the user's decision — the admin repo inherits the proof, not just the code, which obliges it to carry vitest alongside date-fns and @date-fns/tz — and diffs the declared range of both shared peer dependencies, because v3 and v4 differ in exactly the timezone API dates.ts uses and two repos on different majors produce a byte-identical file computing different dates. Sibling resolved from ../arena-player-admin, overridable with ARENA_ADMIN_PATH, documented in .env.local.example as a path rather than a secret. PROVEN TO FAIL against a throwaway copy on all three drift shapes: one character of separator drift (reported with the exact line and both sides), a file missing on the far side, and a peer-dep major mismatch with byte-identical sources. Today it SKIPS LOUDLY — the admin repo has no src/ — naming how many files are unguarded, because a check reporting success having compared nothing is worse than no check.

[2026-08-09] [gotcha] THE TZ PIN IS WHAT MAKES THE DATE TESTS MEAN ANYTHING, and this was demonstrated rather than argued. Removed `{ in: JAKARTA }` from todayInJakarta: under vitest's pinned TZ=UTC three tests went red, and the same broken helper run under this machine's native Asia/Jakarta returned 2026-08-10 — exactly what the test expects. So without the pin the suite would have been green against a helper with no timezone handling at all, on the developer's machine and nowhere else.

[2026-08-09] [gotcha] TWO OF MY OWN TEST ASSERTIONS WERE WRONG, both caught on first run. (1) isPastSlot at exactly 12:00 was expected to leave the 12.00-14.00 slot bookable; the code says it is past, and the code is right — a slot starting this second cannot be sold to a team that has to travel. The boundary is now inclusive by decision and pinned by a named test. (2) normalisePhone was expected to accept 08123456789012, which is a 13-digit national number and not a real Indonesian mobile; the range is 8 followed by 8-11 more digits, so the true boundaries are 0812345678 and 0812345678901 and both are now asserted.

[2026-08-09] [gotcha] THE NEW slot-canonical-drift CHECK FELL FOR ITS OWN FAILURE MODE. The nine canonical strings now live in two places — TIME_SLOTS and the time_slot_canonical constraint — which is the copied-value shape this repo has lost time to three ways, so check:docs gained an eleventh check to compare them. The first version matched every quoted slot-shaped string in each file and therefore compared THREE PROSE MENTIONS from slots.ts' own doc comments against the nine in the SQL. It was a checker taken in by exactly the confusion it exists to catch. Both sides are now scoped to their declaration first — the TIME_SLOTS array literal and the constraint body — and extraction happens inside that slice. Proven to fail from both directions: a separator drift planted in the SQL, and a wrong end-hour planted in slots.ts.

[2026-08-10] [build] RENAMED the two check scripts to match what they do, on the user's ask. `check:lib` was named when a top-level lib/ existed and now runs `vitest run src` — nothing named lib is involved, and it also secretly ran the domain diff first, so the name was wrong twice. `check:shared` was named when the frozen folder was lib/shared/; step 02b renamed the folder and the script never followed. Now `check:unit`, `check:domain`, and `scripts/check-domain.mjs`. Sixth instance this session of a name describing a layout nobody has any more, and the first one nothing was going to catch: it broke nothing, it only misled.

[2026-08-10] [decision] The bundling is gone, replaced by `pnpm check` — lint, typecheck, format:check, check:domain, check:docs, check:unit, cheapest first. check:domain was folded inside check:lib so it could not be skipped; that bought the guarantee at the cost of a command that lied about what it ran. The umbrella is the honest version and covers six checks instead of two, though it is opt-in where the bundle was automatic — mitigated by every doc that said "run check:lib" now saying "run pnpm check", and by the Stop hook running check:docs independently. Verified all three ways: check:unit runs 40 tests with NO domain output (the actual behavioural change, and the one thing a rename could silently get wrong by leaving the && in place), `pnpm check` exits 0 across all six, and a planted prettier violation makes it exit 1 from the MIDDLE of the chain rather than only the first link.

[2026-08-10] [build] Sweep covered 26 references in web and 14 files in admin. HISTORY DELIBERATELY NOT SWEPT — 6 mentions in web's PROGRESS.md, 2 in the archive, 2 in admin's — because those record what was true when the work ran. Anyone "fixing" them later is introducing the defect, not removing one. Also ticked five Phase 1a DoD boxes that were provably done and had stayed unticked, exactly the failure docs/tasks/README.md warns about; the API-contract box was left UNTICKED because step 05 is verify-don't-rewrite and that verification has never formally run — ticking it would be the inverse defect.

[2026-08-10] [build] Admin repo swept in the same pass, backup re-taken first because it still has no git. Three statements there were made false by web's step 06 and are now corrected: architecture.md and PRD.md both said "web's src/domain/ does not exist yet" and specified a skip message accordingly — it exists, and the repo that is now missing from the comparison is admin itself. Its code-reviewer said a skip means web is absent; it now means admin is. And database.md called source-vs-constraint "the part no check in either repo currently does" — web's check:docs does it now as slot-canonical-drift, so that passage was rewritten to name THREE distinct surfaces rather than deleting admin's check:schema: check:domain is source vs source, web's slot-canonical-drift is source vs the SQL FILE, and check:schema is source vs the LIVE DATABASE. Only the last proves the migration was actually run, and run unedited. Admin also gains the same `pnpm check` umbrella, minus the two credentialed scripts — an umbrella that cannot run on a fresh clone is one people stop running.

[2026-08-10] [decision] docs/progress-archive/ KEPT. Its context cost is already zero, which is the entire reason it was split out: excluded in check-docs HISTORY, excluded in .prettierignore, and no agent reads it routinely. It holds the only record of WHY — why the project was replanned from scratch, why the DB was re-locked to Neon+R2 after the Supabase pivot and revert. PRD, architecture, DESIGN and PRODUCT hold what is TRUE, not why.

[2026-08-10] [build] src/hooks/ added on the user's ask — cross-module React hooks, `use-<thing>.ts`, tenth folder under src/. Boundary written down rather than left to be guessed: it takes the SAME one-consumer question as src/components/, with one exception that matters more than the rule. A data-fetching hook is NEVER promoted however many surfaces call it, because the query key, cache lifetime and retry policy are decisions about a surface, not reusable machinery — `home.queries.ts` stays in home. The test that settles it: if a hook's name would have to mention a route to be honest, it is a module hook. Also stated plainly that hooks/ and components/ are organised by KIND in a repo otherwise organised by FEATURE, and that the inconsistency is tolerated only because the one-consumer rule keeps them small — a src/hooks/ past a handful of files means the rule stopped being applied, not that it needs subfolders.

[2026-08-10] [decision] The new folder came with a new import rule, because adding it unguarded means the first cross-module hook can import a module and nothing complains. src/components/ and src/hooks/ sit BELOW modules: a shared hook reaching into @/modules/home is not shared, it is a home hook in the wrong folder, and it drags whatever that module imports onto every surface using it. Enforced the same two ways as the other three rules — a new ESLint zone for both folders (DEFAULT_PATTERNS + CROSS_MODULE) and a twelfth check:docs check, `shared-layer-inversion`, for the relative spelling no glob can express. Six probes: hook->module, component->module, hook->axios and hook->@/app all error; hook->domain+lib and module->hook (the correct direction) stay clean. Relative form `../modules/home/home.service` from src/hooks/ proven to exit 1.

[2026-08-10] [gotcha] The scaffold repo MOVED and the earlier work in it is gone. It is now at raygent/raygent-scaffolds/client-project-scaffold and has been rewritten as a Handlebars project GENERATOR — bin/create.mjs plus templates/ — so the CLAUDE.md and docs/architecture.template.md written for it earlier this session no longer exist. The structure now lives in templates/docs/_folder-structure.partial.hbs, which has five conditional trees. src/hooks/ added to the five FRONTEND branches only; the two backend branches deliberately do not get it, since there is no React there. Worth knowing before anyone looks for the old files: they were not deleted by us, the repo was replaced.

[2026-08-10] [build] Step 07 — mock layer and data plumbing. src/mocks/{availability,handlers,browser,server}.ts, src/lib/query-client.ts, src/services/api-client.ts, src/modules/home/{home.types,home.service,home.queries}.ts, src/app/providers.tsx wired into the layout. 58 tests, up from 40. MSW v2 confirmed against the INSTALLED package's own exports rather than Context7, which is not connected in this session — `require("msw")` lists http/HttpResponse and msw/browser lists setupWorker, which is more authoritative for 2.15.0 than any doc would be.

[2026-08-10] [gotcha] THE STEP'S OWN ACCEPTANCE COULD NEVER PASS. It said `curl localhost:3000/api/availability` — but MSW is a SERVICE WORKER: it intercepts browser fetches and nothing else, so curl reaches Next's router and 404s no matter how correct the handlers are. The paths in that block were pre-src/ as well. Raised before writing anything and the user chose browser-worker + msw/node for tests, which keeps src/app/api/ EMPTY until Phase 4 — that phase then ADDS a route file rather than replacing the body of one that already contains a dev-only branch. Acceptance rewritten around `pnpm check:unit` and two greps of .next/static/.

[2026-08-10] [build] THE PRODUCTION TRAP IS CLOSED AND PROVEN, not deferred to Phase 4's checklist. providers.tsx compares process.env.NODE_ENV, which the bundler INLINES, so in a production build the branch is a literal false and the dynamic import("@/mocks/browser") is never emitted — msw is not in any page's module graph rather than merely unreached. A runtime flag would have shipped the mock and then trusted a value. Verified on a real `pnpm build`: `grep -rl "mockServiceWorker\|setupWorker\|onUnhandledRequest" .next/static/` returns nothing, and so does the same grep for TEST409 and availabilityFor. onUnhandledRequest is "bypass" so an un-mocked call fails like a real one instead of being swallowed.

[2026-08-10] [decision] Error triggers are RESERVED TEAM NAMES — TEST409, TEST429, TEST400 — chosen by the user over a URL flag and over "one slot is always full". The URL flag needs a special path through api-client.ts that outlives the mock; the always-full slot can only ever produce 409, leaving the 400 and 429 screens built blind. A team name needs no devtools, and a reviewer can reproduce a 409 mid-walkthrough by typing. Written into architecture.md's API contract section as a table, next to the 409-vs-429 rule they exist to exercise.

[2026-08-10] [gotcha] MSW HANDLERS REGISTERED WITH RELATIVE PATHS MATCH NOTHING UNDER msw/node. `http.get("/api/availability")` resolves against location.origin in a browser; in Node there is no origin, so every test failed with "Cannot bypass a request when using the error strategy". Fixed with the origin wildcard `*/api/availability`, which matches both contexts — the browser request is absolute by the time MSW sees it anyway.

[2026-08-10] [gotcha] MY HANDLER WAS WRONG AND THE TEST WAS RIGHT: it validated `slot` with canonicalSlot, which REPAIRS a near miss by design, so '18.00-20.00' was accepted as valid. The contract says slot is an EXACT member of TIME_SLOTS. Switched to isTimeSlot. The distinction is the whole point of having both: canonicalise where user or URL text ENTERS the client, reject at the API — an API that silently repairs means a caller sending the wrong format never learns, and the Phase 4 route becomes the only place the format is enforced. This handler is the rehearsal for that route, so it has to be as strict as the route must be.

[2026-08-10] [build] Budget re-measured now that / actually imports something: 126.5 framework + 10.7 route chunk = 137.2 KB on a real build, excluding the 38.7 noModule polyfill. TanStack Query's table row moved 10.0 -> 10.7 because 10.0 came from an isolated probe route and 10.7 is what ships — library plus providers.tsx plus the query defaults. The projected subtotal is now 156.4. The gap between 137.2 and 156.4 is NOT headroom: date-fns, cn(), react-icons and zustand are written but not yet reached by any page, so it is the bill for code that already exists.

[2026-08-10] [decision] Query defaults are correctness decisions, not tuning. staleTime 30s matches the contract's cache line — availability that goes stale slowly shows a slot as free after someone took it, and the visitor learns at the 409, after filling the form and uploading a receipt. retry 1 not 3, because four round trips on stadium wifi means staring at an empty grid instead of an error you can act on. mutations retry 0, ALWAYS: a retried POST is a second insert attempt, and the only thing between that and a double booking is a unique index doing its job twice. Also: validateStatus < 500 on the axios instance, because 409/429/400 are expected answers with different UI and turning them into exceptions loses the body holding `fields`.

[2026-08-10] [gotcha] First draft of providers.tsx started the worker from a useState INITIALISER — a side effect during render, which React 19 strict mode double-invokes. Rewritten with useEffect and a cancelled flag. Caught by reading it back before running anything, not by a check; nothing in the harness looks for side effects in render.

[2026-08-10] [build] Step 08 — scripts/check-budget.mjs, src/lib/motion.ts, and hard rule 6 turned from a sentence into two mechanisms. check:budget is wired as its own script plus `pnpm check:ship` (= check && build && check:budget) rather than into `pnpm check`, on the user's decision: the budget needs a fresh build, and putting one inside the ten-second command turns it into a forty-second command people stop running. Same reasoning that unbundled check:domain from the test run.

[2026-08-10] [gotcha] THE INPUT STEP 08 WAS PLANNED AROUND DOES NOT EXIST. It said "reads Next's per-route First Load JS from the build output" — Next 16 removed that table, Turbopack emits no top-level app-build-manifest.json, and the per-route .next/server/app/<route>/build-manifest.json it DOES emit lists only the polyfill and the shared root files. The route's own chunk is missing from it, which is precisely the number the check exists to watch. The PRERENDERED HTML is the honest source: it is literally the list of scripts the browser is told to fetch for that route, and index.html references all six chunks including the 10.7KB one the manifest omits. That also makes the "per route, not just a total" requirement achievable, which it was not from any manifest.

[2026-08-10] [decision] check:budget READS THE CEILING OUT OF architecture.md instead of holding its own copy. That file calls itself "the single source — reference it, never copy the numbers", and this repo has lost time three separate ways to a value copied with nothing checking the copy; a budget script restating 240 would have been the fourth. If the row cannot be parsed the check EXITS NON-ZERO rather than defaulting — a budget check guessing its own budget is worse than no check. Editing the number in the table now changes what the check enforces, which is what "single source" was supposed to mean all along.

[2026-08-10] [build] PROVEN TO FAIL THREE WAYS, and the second is the one that matters. (1) --limit 50: all three routes BREACH, exit 1. (2) THE LEAK: a client component in src/modules/home/components/ calling useAvailability, rendered from page.tsx — every link in that chain is legal, ESLINT REPORTED NOTHING, and / went 137.0 -> 151.8 KB while /_not-found stayed at 137.8. Set --limit 145 and only / breaches. That is the whole argument for per-route rather than one total, demonstrated rather than asserted. (3) with .next/server/app moved aside it fails loudly instead of reporting success over nothing. Probe reverted; final clean build has / at 137.0 KB against the 240 ceiling, 103 KB headroom.

[2026-08-10] [gotcha] THE STEP FILE TOLD ME TO USE useGSAP AND IT CANNOT BE USED. @gsap/react's source opens `import gsap from "gsap"`, and useGSAP is a hook, so importing it means importing it STATICALLY — which puts the full 43.6KB back on first load and undoes the lazy decision this same step exists to enforce. Verified by reading node_modules/@gsap/react/src/index.js, not by recall. useMotion does directly what useGSAP wraps: gsap.context() for scoped cleanup, driven from a useEffect around a dynamic import("gsap").

[2026-08-10] [decision] HARD RULE 6 IS NOW MECHANICAL, using a property worth recording: eslint's no-restricted-imports matches STATIC imports and does NOT match import() — probed both directions before relying on it. So `gsap`, `gsap/*` and `@gsap/react` are banned repo-wide in every zone (all six override blocks restate it, the replace-not-merge trap again), leaving the dynamic form as the one remaining way in, and check:docs gained a 13th check asserting that form appears in src/lib/motion.ts alone. A second dynamic import elsewhere would both bypass the reduced-motion branch and split the 43.6KB across another chunk. Seven eslint probes and one check:docs probe, all fired.

[2026-08-10] [decision] MotionSpec.settle is a REQUIRED field, not a convention. You cannot describe an animation through useMotion without also saying what a reduced-motion visitor sees — and "nothing" is the wrong answer, because an entrance animation starts at opacity 0, so skipping the tween leaves the element invisible forever. The reduced branch applies the END STATE. GSAP has no built-in prefers-reduced-motion handling at all, which is the entire reason a bare gsap.to() in a component is banned.

[2026-08-10] [gotcha] TWO REACT RULES CAUGHT MY FIRST DRAFT of useMotion, neither by a test. The hook depended on `spec`, a fresh object literal every render, so it rebuilt the animation on every re-render; moved to a ref. Then React 19's lint rejected writing that ref during render — correctly, since it makes output depend on how many times a component happened to render — so the ref is updated in its own effect declared BEFORE the animation effect, with useRef(spec) seeding the first value. The remaining eslint-disable is one line, for the deliberate `...deps` spread, and says why.

[2026-08-10] [build] Final Phase 1a build verified on both lazy boundaries: `grep -rl "gsap\|ScrollTrigger" .next/static/` returns nothing and so does the msw grep from step 07. Both 43.6KB and the mock are genuinely absent rather than merely unreached.

[2026-08-10] [build] Step 05 verified rather than rewritten, as the step file demanded, and running its acceptance for the first time is what found the rest of this entry. CONTRACT IS INTACT: notes is 500 in all four copies with no stray 280 anywhere it means the field, FIRM and PROVISIONAL both present twice, and every response code specified. DoD box ticked because it was checked, not assumed — it had survived two earlier ticking passes precisely because this had never run.

[2026-08-10] [gotcha] I INTRODUCED A GAP IN GATE 1 AND DID NOT NOTICE FOR TWO STEPS. Writing db/migrations/20260809_create_bookings.sql made a THIRD copy of the schema alongside docs/database.md and docs/PRD.md, and slot-canonical-drift only compared src/domain/slots.ts against db/migrations/. The other two carry the same nine slot strings, the same four statuses, and the same uniq_active_slot WHERE clause, watched by nothing. That is the copied-value failure this repo has paid for three times, committed by the person who had just built the check against it.

[2026-08-10] [build] slot-canonical-drift REPLACED by schema-value-drift, same slot in the check list, wider reach. Asserts across db/migrations/*.sql, docs/database.md and docs/PRD.md that time_slot_canonical matches TIME_SLOTS, status_valid matches BOOKING_STATUSES, uniq_active_slot's WHERE matches ACTIVE_STATUSES, and notes_length is 500 — the four value families whose drift is dangerous rather than the text around them. Plus one byte-identity assertion: docs/database.md's block must stay identical to the migration, which line 54 already claims. docs/PRD.md is deliberately EXEMPT from that one — its block is a 28-line comment-free summary against the migration's 45, which is correct for a spec and means a whole-block diff is the wrong tool there. PROVEN TO FAIL FIVE WAYS, one per source and one per value family; the ACTIVE_STATUSES probe fired from all three copies at once, which is exactly the coverage that was missing.

[2026-08-10] [gotcha] TWO OF STEP 05'S OWN ACCEPTANCE GREPS WERE WRONG AND NEITHER DOCUMENT WAS. `^// (201|400|409|429)` returned 0 while every code sat at lines 45 and 97-103, and a bare `grep -rn "280"` returned six hits that are --dur-base:280ms in DESIGN.html plus the PROGRESS entries recording this very contradiction. Both fixed rather than deleted: the assertions are worth keeping, and an acceptance that fails on correct content trains people to ignore it, which is worse than not having one. Also dropped the field-name grep entirely — it compared architecture.md against "the PRD form spec", but the PRD lists UI LABELS (Nama Tim, Nomor WhatsApp) and never wire names. That is correct single-sourcing, and a grep implying a gap invites someone to duplicate the field table into the PRD.

[2026-08-10] [decision] Recorded in the API contract, because it is deliberate and looks like a mistake: THE QUERY PARAM IS `time` AND THE POST FIELD IS `slot`. /booking?date=…&time=… is a link a human admin types into WhatsApp and `time` is the word they would guess; `slot` is the wire name matching TIME_SLOTS and the time_slot column. Renaming the param breaks every link already pasted into a chat — the one place in this system with no deploy and no rollback. Consistent across all four places it appears. Phase 3 reads `time` and submits `slot`.

[2026-08-11] [milestone] PHASE 1a CLOSED, 9/9. Last DoD box ticked: /plan-eng-review, /plan-devex-review and /devex-review have all run, all six P1 findings are closed and pushed at a4fdd0b. Twelve P2s and three P3s stay open in ~/.gstack/projects/arayhan-arena-player/ and none of them gates Phase 1b — P1 was the ship-blocking bar and nothing below it touches the design foundation. Final state: pnpm check exit 0, 13 doc checks, 74 unit tests, / at 137.0KB gzip with 103.0KB headroom under the 240KB ceiling. The heaviest remaining debt is T7 (an eslint zone() helper — the replace-not-merge trap in flat config has now bitten three separate times in this repo) and T8 (fixture tripwires proving the 17 check-docs assertions can actually fail). Both are P2 by agreement because they harden tooling that currently works, not code that currently ships.

[2026-08-11] [gotcha] PHASE 1b WAS BUILT BACKWARDS AND THE DOCS DID NOT NOTICE. PRD tasks 4 and 5 — the design system page and its clickable journey — already exist as docs/DESIGN.html: 1116 lines, eleven sections, ending on the Alur frames. Tasks 1, 2 and 3 never ran. So the artifact commits to an entire visual world (brick build-instruction book, user-pinned over the rolled Papan Jadwal challenger) while docs/DESIGN.md line 139 still reads "Creative North Star: not yet established. Art direction is Phase 1b task 1 and has not been decided." That line is now false, and it is the line an agent starting Phase 2 would read first. The direction survives only in PROGRESS prose and in the HTML itself, neither of which is where DESIGN.md's own format spec says a north star lives. Task 1 is therefore mostly transcription of a decision already taken — the exception being docs/references/benchmark-bataskotapoint.png, 6.8MB, gitignored, and never read by anyone, which is both a 1b DoD item and the only way to answer what "surpass the benchmark, inverted to light blue-white" means concretely.

[2026-08-11] [build] PHASE 1b TASKS 1 AND 3. Benchmark finally read — docs/references/benchmark-bataskotapoint.png, 6.8MB, a full-page desktop capture at 1920x7888 that had sat unread since it was dropped in. Findings written into DESIGN.md as a table plus four falsifiable claims, because the file is gitignored and deleted and anything not written down goes with it. THE MOST USEFUL FINDING WAS A CONVERGENCE, NOT A CONTRAST: the benchmark also renders booked slots in a red triple with a lock glyph and also uses filled pills for the selected date. Arena Player arrived at both independently, and that is a reason to keep them rather than differentiate — a booking grid is the wrong place to be original, and this is the convention the local audience already reads. What it does badly is the useful part: the booking section sits roughly five scrolls down behind hero, video, specs and gallery, which is the evidence behind our two-scroll rule; and six sections carry six identically-treated centred two-tone headings, so a visitor has no sense of place. It also shows prices, which we cannot.

[2026-08-11] [decision] ART DIRECTION WRITTEN DOWN — "the build-instruction book". DESIGN.md line 139 had said "Creative North Star: not yet established" while DESIGN.html shipped a fully committed world, so the document an agent reads first was contradicting the artifact it points at. Mostly transcription; the one genuinely undecided part was the SECTION-TRANSITION LANGUAGE, chosen by the user from three options: numbered assembly steps, an oversized Orbitron numeral in grey-200 bleeding off the left edge with a navy keyline between steps. Picked over keyline dimension rules and over bands-and-hairlines because it costs zero kilobytes, needs no motion, works at 375px, and fixes the exact defect both the benchmark and our own first draft were graded on — the critique's phrase was "one compositional idea repeated eight times". Also recorded the fusion rule that makes the world cohere: every colour carries both an instruction-book role and a product meaning, and where they conflict the product wins — which is why "the new part" is Signal Blue and not the source world's brick red, since red already means booked.

[2026-08-11] [decision] TWO SETTLED, BOTH ON THE RECOMMENDATION. Inter STAYS as the body face: the detector's flag was correct (it is the face every AI-generated interface converges on) but it is a stated client commitment in PRODUCT.md, "invisible" is the right brief for body type a captain reads at speed, and every printed number in the system — the 60-68ch measure, the 14px/12px fixed sizes, the state-label ratios — was computed against Inter's metrics, so a swap invalidates them all at once. The reason is now written down, which is the actual fix; the failure mode was inheriting it without noticing. HERO-VIDEO GATE DECIDED AND FAILED, before anything was produced, as the PRD requires. It failed on condition 2, which was always the binding one: iOS Low Power Mode and in-app webviews block autoplay outright and the in-app browser is the PRIMARY device here, so a real share of visitors would only ever see the poster. Conditions 1 and 3 were never reached. LCP element is the Orbitron headline plus logo; the "no autoplaying video" guardrail in CLAUDE.md stays unamended because it only needed an exception clause if the gate passed; /remotion-create is reserved for off-site social assets.

[2026-08-11] [fix] THE FIVE ITEMS LEFT "OPEN BY AGREEMENT" ARE CLOSED, and three of them were accuracy defects rather than taste. (1) ELAPSED IS NOW A FOURTH SLOT STATE. It was rendering in the danger triple while labelled "Sudah lewat" — contradicting the paragraph printed directly above it in step 04, and overstating how busy the day was on a product measured by filling empty hours. Now grey-50 fill, navy-400 text, and a 3px navy-400 left rule at 6.64:1: the separation is STRUCTURAL, not chromatic, because both states are unavailable and a hue swap alone leaves them reading as the same family — only one of the two is somebody else's booking. Added to the step 04 state inventory, which otherwise omitted a state the system ships. (2) "Sudah lewat (6)" rendered TWO rows. Now six, which with the three below makes the full nine-slot day — a small lie, but on the one page whose entire argument is that its numbers are checkable. (3) The DIRECTION CONTRACT comment still claimed the first viewport was the parts inventory; the page has opened on the client's decisions since the peak-end fix, which the reviewer called its best structural decision. Comment corrected rather than the page reverted. (4) Kirim used native `disabled`, the only control on the page breaking the rule the page itself prints twice — a keyboard user reaches the last field and finds nothing. Now aria-disabled, with the CSS comment corrected too: it claimed pointer-events:none stops the press, which covers the MOUSE only and leaves keyboard Enter live, so Phase 3 owes a handler guard. Half a fix described as a whole one is exactly the pattern the P0 punished. (5) The system had NO ::placeholder rule at all, so every Phase 3 field would have inherited the UA default at roughly 2.35:1; added --input-placeholder-fg at navy-400, 6.94:1 on white, opacity:1 explicit because browsers stack their own alpha on top. Gate green throughout: 13 doc checks, 74 tests.

[2026-08-11] [decision] HERO COPY CHOSEN — "Pilih Jam. Kirim. Main." Three registers were drafted and the user picked the three-step imperative over an answer-before-asked framing ("Jam Kosong, Terlihat Langsung.") and a same-day energy one ("Malam Ini, Lapangan Menunggu."). It wins on two grounds. The headline IS the assembly, so it says what the art direction says in the visitor's own words rather than in ours. And length is a hard constraint here, not a stylistic one: at Orbitron 900 on a 375px screen the LONGEST WORD is what breaks the layout, and this headline's longest word is five characters against nine for both rejects. The same-day option was also factually risky — a hero cannot know what time of day it is being read without client JS, so "malam ini" is simply wrong to anyone arriving at 9am. Subheadline "Jadwal Arena Player tampil langsung. Pilih jam kosong, lanjut lewat WhatsApp." Meta wired into src/app/layout.tsx. It stays TODO(content) deliberately: it is drafted in-house, so a client rewrite swaps it in three places at once — unlike the Ketentuan, which is verbatim client content and must never be reworded.

[2026-08-11] [gotcha] "ALL THREE SLOT STATES" APPEARED IN THREE PLACES AND IS NOW FOUR. Splitting elapsed out of booked changed a count that the PRD stated in the Phase 1b task table, in the client-checkpoint paragraph, and in the Definition of Done. None of the 13 doc checks could catch it — check:docs asserts value families like TIME_SLOTS and BOOKING_STATUSES against their SQL, but a prose numeral describing a design artifact has no machine-readable counterpart to drift against. Fixed by hand in all three. Worth recording because it is the shape of defect this project keeps producing: a change that is correct in the artifact and stale in every document that counted it.

[2026-08-11] [gotcha] I ADDED THE SIDE-TAB ACCENT BORDER AGAIN. The detector caught a 3px navy-400 left rule on the new elapsed slot state — the same rule this project already added and removed once, during the checkpoint build, for the same reason. Worse, I had written a paragraph in both DESIGN.md and DESIGN.html arguing it was "structural, not chromatic" and therefore fine, which is precisely how a slop pattern survives review: with a rationale attached. Two things made it feel justified. The critique DID prescribe a 3px left rule — but for the available/pending/booked triple, whose fills computed 1.11:1 and 1.22:1 and genuinely carried no signal; elapsed versus booked is neutral-family against danger-family, which is not the same problem. And the elapsed rows sit behind a collapsed opt-in disclosure, so the scannability argument that justified the rule elsewhere does not reach them. Removed. Three signals already separate the two states: colour FAMILY not hue, the resting border, and the label itself, which is what satisfies WCAG 1.4.1 — a fourth that costs the page its credibility is not a trade worth making on an artifact whose whole argument is that it is checkable. The lesson generalises: a recommendation from a critique is scoped to the component it was written about, and porting it to a neighbouring component is a new decision, not a continuation.

[2026-08-11] [decision] Inter's overused-font finding persisted as a shared ignore-value in .impeccable/config.json, with the full reasoning in the ignore record rather than only in DESIGN.md. The detector is right and the flag stays true; suppressing it is correct only because the user settled the question deliberately this session. Recorded as shared, not local, so the next session sees the decision instead of re-litigating it — which is the same failure mode as the gitignored critique folder in hard rule 10.

[2026-08-11] [fix] THE CLIENT CHECKPOINT WAS SHOWING THE CLIENT A STALE LIST OF ITS OWN QUESTIONS. DESIGN.html's "Butuh keputusan Anda" block — the first thing the client reads, moved there deliberately by the peak-end fix — still listed art direction and hero copy as open, both decided today, and DID NOT LIST THE WHATSAPP-ONLY QUESTION AT ALL. That is the one that blocks Phase 2 and the one the client has never been asked, so the block was simultaneously wrong about what is settled and silent about what matters most. Rewritten to the three that are actually open: the WhatsApp-only flow, pricing, and the six missing content items. Also rewritten IN INDONESIAN, unlike the rest of the page: the critique's second-lowest heuristic score was English jargon-dense chrome on an artifact whose checkpoint audience is an Indonesian field owner, and this is the one section that audience must act on rather than be walked through.

[2026-08-11] [decision] THE TWO BLOCKING ANSWERS ARE DECOUPLED FROM SCHEDULING A CALL. The checkpoint doc had one format — a live screen share — and every question inside it, which makes the client's calendar the gate on Phases 2 and 3. Scheduling IS the schedule risk the document itself keeps warning about. Added a copy-paste Indonesian WhatsApp message carrying only questions 1 and 2 plus the content list, to send now; the walkthrough still happens and still confirms the direction, but if it slips, the two answers that actually gate work are already moving. Deliberately NOT all six questions: the other four change what gets prioritised or what fills a placeholder and none of them stops work, and a six-question message gets two answered. Also corrected three things the day's work made stale in the walkthrough script — the slot list is four states not three, the decisions block is three items not four, and the headline is now DRAFTED and needs approval rather than supply, which moves it out of the same "waiting on client" bucket as the bank details.

[2026-08-11] [review] /design-review ON docs/DESIGN.html — FOUR FINDINGS FIXED, ONE ACCEPTED, ALL FIVE ARE NUMBERS THAT DISAGREED WITH A SENTENCE. Ran DEGRADED and the DoD says so rather than hiding it: Codex was not installed on this machine so the outside-voice source audit never ran, and no subagent was dispatched because this repo's standing instruction is not to spawn one unasked. Single-reviewer, stated plainly. (1) ELAPSED CARRIED NO SURFACE SIGNAL AT ALL — measured, its grey-50 fill against an available cell's white computes 1.05:1, so after splitting elapsed out of booked earlier today I had left it distinguished by border HUE and text alone, which breaks this system's own Shapes rule that no state depends on colour alone. Fill and border both to grey-200, making elapsed the only borderless cell — structural, not an accent stripe, and the same grey-200 the disabled button already uses. navy-400 on grey-200 is 5.61:1. With six rows expanded at 375px the day now reads six grey, one amber, two available, which is the honest picture and the entire reason the state was split. (2) The states badge said 5; the cell has six. Every other badge on the page is an exact count. (3) The Alur journey opened a 320px dead gutter between 900 and 1299px — space-between on two 375px frames in a 1068px column — on the one section whose job is showing that the screens happen IN ORDER; 1280 is the most common laptop viewport there is. (4) THE FRAMES CLAIMED "the real device width" AND WERE FALSE BY 36px: each draws its own border and padding inside the page column, so rows got 307px where the product gets 343. This document has already taken a P0 for exactly that class of error. Padding recovered 8px; the claim now states the gap instead of denying it.

[2026-08-11] [gotcha] I ALMOST SHIPPED A NO-OP FIX WITH A COMMENT SAYING IT WORKED. Slot times vary with their digits — "18.00 - 20.00" measures 127px, "10.00 - 12.00" measures 120px — so whether a row wraps depends on which numbers it contains. The obvious fix is font-variant-numeric:tabular-nums, DESIGN.md's own typography checklist already asks for it on number columns, and I wrote it in with a confident comment. MEASURED AFTER: 127 and 120, unchanged. ORBITRON SHIPS NO tnum FEATURE, so the property does nothing on this face. Reverted rather than left in place, because a no-op with a comment claiming it solved the problem is worse than the problem — it retires the finding while leaving it live. The CSS now records the measurement and flags the line to revisit first if the display face ever changes. Accepted rather than fixed: in the PRODUCT a 343px row leaves 26px of slack, so this only surfaces inside the narrower Alur frames. Second time in one session that verifying a fix I had already written up contradicted the write-up.

[2026-08-11] [milestone] PHASE 1b IS 7/8. Benchmark deleted after its findings outlived it, which is the condition docs/references/README.md sets. The only box left is the CLIENT APPROVAL, which no amount of work here can tick — it needs the client. The two blocking questions are drafted and ready to send as a WhatsApp message; sending them is the single highest-value action available on this project right now, and it costs one message.

[2026-08-11] [build] DESIGN.HTML PUBLISHED AS A SHAREABLE LINK FOR THE CLIENT CHECKPOINT. A .html attachment is close to useless to a field owner on a phone — WhatsApp blocks the type on some clients, and on Android opening one means Files, Downloads, then pick a browser. A URL opens in one tap. THE TRAP THAT MADE THIS NON-TRIVIAL: the artifact host applies a strict CSP that blocks every external request, and DESIGN.html loads Archivo, Orbitron and Inter from a Google Fonts CDN <link>. Published as-is, all three fall back to system sans — and the typography is a large part of what the client is being asked to approve, so it would have shown them the wrong page while looking like it worked. Fixed by fetching the LATIN subsets only (Google serves ~36 woff2 files across vietnamese, latin-ext, cyrillic and greek; the page is Indonesian and English) and embedding all 10 as base64 data URIs. 312KB of fonts, 0.46MB total, verified after publishing that document.fonts.size is 10 and h1/body/slot-time resolve to Archivo/Inter/Orbitron rather than fallbacks. Build script kept in the scratchpad, not the repo — a 0.46MB base64 file does not belong in git, and the repo copy stays 72.8KB with its CDN link intact for local dev.

[2026-08-11] [decision] THE PUBLISHED COPY HAS ITS COMMENTS STRIPPED; THE REPO COPY KEEPS EVERY ONE. 15.9KB of HTML and CSS comments removed from the shareable build only. The reasoning in those comments is exactly what a future developer needs, so it stays in git — but the published page gets a URL the client can forward, view-source is one tap away, and the comments carry candid internal notes ("a small lie", "WHICH WAS FALSE BY 36px"), the /impeccable seed key and roll index, and internal repo paths. PRODUCT.md records "does the git repository transfer to the client at handover?" as an OPEN DECISION whose conservative default is that internal material stays internal; this applies that default rather than pre-empting it. Verified after stripping: seed key, roll index, impeccable, and the candid phrases all return 0 hits. Two internal paths survive deliberately — docs/DESIGN.md and src/lib/motion.ts appear in VISIBLE body copy, where a design system legitimately names its own source of truth.

[2026-08-11] [fix] TWO STALE CLAIMS CAUGHT BY READING THE WHOLE FILE BEFORE PUBLISHING, WHICH IS THE ONLY REASON THEY WERE FOUND. Step 03 still told the reader "Whether the product keeps them is Phase 1b task 1, still open" — task 1 ran today and settled Orbitron and Inter deliberately, so the page was telling the client a decision was open that had been closed hours earlier. And step 04 called elapsed "a fourth state" directly under a badge reading "+ 6 states"; both numbers are right under different definitions (four come from the data, six are what the cell renders including hover and selection) but printing them four paragraphs apart with no reconciliation is the kind of thing that makes a reader stop trusting the other numbers. Both fixed. Neither was reachable by any check in the repo — check:docs asserts value families against SQL, and no assertion can know that a sentence describing a phase went stale when the phase advanced.

[2026-08-11] [milestone] CLIENT ANSWERED. Alur WhatsApp-saja DIKONFIRMASI — admin standby membalas dengan link formulir, dan klien sendiri yang mengangkat bot WhatsApp sebagai pengganti nanti, yang sudah jadi item pasca-Phase-4 di PRD tapi sekarang dengan dukungan klien, bukan asumsi kita. PHASE 2 TERBUKA. Harga: TIDAK di halaman utama, TAPI DITAMPILKAN di formulir booking — ini menutup OPEN DECISION yang menahan hard rule 2 sejak PRD ditulis, dan mempersempitnya dari "no prices anywhere" jadi "no prices on /". Klien menjawab DI MANA harga muncul tanpa menjawab BERAPA, jadi daftar tarif jadi kategori TODO(content) KETUJUH — perubahan pada hard rule 3 dan pada scripts/check-docs.mjs. Harga adalah satu-satunya placeholder yang TIDAK BOLEH diberi angka sementara: enam lainnya kalau lolos ke produksi cuma terlihat belum diisi, sedangkan harga karangan terlihat persis seperti harga asli, ada orang yang mentransfer berdasarkan itu, dan itu jadi angka developer yang menempel pada bisnis klien.

[2026-08-11] [decision] APPROVED WITH CHANGES, ENAM PERMINTAAN, TIGA BERTABRAKAN DENGAN ATURAN YANG SUDAH DITULIS. Dunia visualnya bertahan — terang, biru-putih, build-instruction book — jadi ini bukan Redirect. (1) THREE.JS DITOLAK OLEH ARITMATIKA, BUKAN OLEH SELERA. three.js ~150KB gzip; / terukur 137.0KB dari plafon 240KB, jadi three.js LEBIH BESAR dari seluruh sisa budget, dan 3.75x cap 40KB yang jadi syarat izin WebGL. anime.js = runtime animasi kedua yang tidak menambah kemampuan apa pun di atas GSAP. architecture.md baris 330 sudah menulis jawabannya jauh sebelum permintaan ini datang: "Reach for the shader, not the engine." Efeknya tetap dapat — shader GLSL ~3-5KB atau OGL ~10KB. (2) SLOT GRID: batasan 146px untuk "Menunggu Konfirmasi" itu nyata tapi HANYA MENGIKAT DI 375px. Jadi HP tetap satu kolom, >=768px jadi 2 kolom, >=1024px jadi 3, dan labelnya pindah ke bawah jam supaya 20 karakternya utuh di berapa pun kolomnya. Bukan kompromi — itu pembacaan yang benar atas batasannya. (3) ROUNDED: 2px/4px jadi 10px/14px, tapi date pill TETAP 999px. Jarak antara "jelas membulat" dan "bulat penuh" itulah yang masih membawa sinyal "baris ini bisa digeser"; kalau semuanya jadi 24px, pill cuma jadi yang paling bulat, bukan satu-satunya yang bulat, dan baris tanggal butuh panah atau fade untuk mengatakan apa yang bentuknya sudah katakan gratis.

[2026-08-11] [gotcha] TOKEN RADIUS DIGANTI NAMANYA BERSAMA NILAINYA, DAN ITU DISENGAJA. --radius-sharp:2px jadi --radius-control:10px, --radius-card:4px jadi --radius-panel:14px. Token bernama "sharp" yang isinya 10px persis jebakan nama-basi yang proyek ini terus-terusan tangkap; "control" dan "panel" menyebut token itu UNTUK APA, bukan dulu kelihatan seperti apa. Sekalian: lede step 04 masih berbunyi "One column, full-width rows" tepat di sebelah grid tiga kolom yang baru saja dirender — ditangkap karena screenshot diambil setelah perubahan, bukan karena ada check yang bisa melihatnya. Tidak ada assertion di repo ini yang bisa tahu sebuah kalimat jadi salah ketika CSS di bawahnya berubah.

[2026-08-11] [milestone] PHASE 1b CLOSED 8/8, PHASE 2 STARTED. Kotak terakhir dicentang sebagai APPROVED WITH CHANGES: empat dari enam permintaan klien yang masuk lingkup 1b sudah dilipat masuk (slot grid, sudut membulat, harga, alur WhatsApp); dua sisanya — shader header dan motion scroll/hover — memang eksekusi Phase 2 karena hero-nya belum ada, dan keduanya sudah dispesifikasikan di DESIGN.md supaya Phase 2 tinggal mengeksekusi, bukan memutuskan ulang. PRD memang mengizinkan ini: "or their changes are folded in before Phase 2 starts".

[2026-08-11] [build] PHASE 2 TASK 1 — LAYOUT. Tiga hal yang sebelumnya sengaja ditahan sampai 1b selesai, sekarang ditulis. (1) globals.css akhirnya dapat blok @theme-nya, ditranskrip dari frontmatter DESIGN.md yang normatif, tiga lapis: primitives (nilai mentah), semantics (untuk apa), components (di mana). Phase 1a sengaja mengosongkannya — menyalin token sebelum arah desain diputuskan akan membuat sumber kebenaran kedua untuk nilai yang masih bergerak. (2) next/font/google untuk Orbitron + Inter, self-hosted; architecture.md mencatat next/font sebagai load-bearing untuk jaminan no-CLS dan LCP, jadi CDN <link> bukan pilihan. (3) src/modules/home/ — komponen Section yang membawa perangkat utama arah desain: ordinal Orbitron raksasa di grey-200 di samping heading, keyline navy antar langkah. Hero tidak diberi nomor: dia membuka halaman, bukan melanjutkan urutan, dan menomorinya 00 cuma menghitung demi menghitung. BIAYA BUNDLE NOL — / tetap 137.0KB dari plafon 240KB, sisa 103.0KB.

[2026-08-11] [gotcha] PORT 3000 ADALAH PROYEK ORANG LAIN, LAGI. curl ke localhost:3000 mengembalikan 200 dan saya hampir memakainya sebagai bukti. Screenshot-nya ternyata "SMILE — Sistem Monitoring Inventaris Logistik Kesehatan", aplikasi yang sama sekali berbeda. Arena Player ada di 3002; `pnpm dev` saya sendiri gagal start karena sudah ada dev server lain berjalan, dan pesan itu tenggelam di log. Proyek ini SUDAH pernah mencatat jebakan ini persis (dulu port 3000 dipakai "Food Allergy Certified", Arena Player di 3001) dan tetap kena lagi. Pelajarannya bukan "cek portnya" — melainkan STATUS 200 TIDAK MEMBUKTIKAN SIAPA YANG MENJAWAB. Yang menangkapnya adalah screenshot, bukan status code.

[2026-08-11] [fix] DRAF PERTAMA Section MEMBUAT BADAN TEKS TIDAK SEJAJAR DENGAN JUDULNYA. Numeral dan judul dipasang sebagai flex row, lalu children digantung di bawahnya selebar penuh — jadi heading ter-indent sejauh lebar numeral sementara badan teksnya tidak, dan di 375px itu terbaca sebagai kesalahan, bukan sebagai perangkat desain. Diganti jadi grid dua kolom: numeral memiliki kolom 1, semua yang dikatakan section itu ada di kolom 2. Terukur setelah perbaikan: h2 di x=91, paragraf di x=91. Ditemukan dari screenshot, bukan dari check mana pun — tidak ada assertion di repo ini yang bisa melihat dua elemen yang seharusnya sejajar ternyata tidak.

[2026-08-11] [decision] ATURAN MOTION ORDER SECTION DIBUKA. DESIGN.md sebelumnya menyatakan dengan tegas bahwa order section hanya boleh functional feedback — perubahan state, seleksi, collapse — dengan alasan yang diukur: di situ booking terjadi, di situ gate Lighthouse per-section paling menggigit, dan di situ organiser memutuskan paling cepat dengan 8-12 orang menunggu. USER MEMBUKA ATURAN ITU: klien minta banyak animasi dan tidak mengecualikan bagian tempat produknya sebenarnya terjadi, jadi order section tidak jadi satu-satunya ruangan diam di halaman yang selebihnya bergerak. Dicatat sebagai keputusan bertanggal, bukan dilonggarkan diam-diam, karena aturan lama ditulis dengan tegas dan sesi berikutnya akan membaca keduanya sebagai kontradiksi kalau tidak dijelaskan. SATU HAL BERTAHAN, DAN ITU SOAL LATENSI BUKAN SELERA: feedback seleksi tetap seketika. Bukan pembatasan seberapa banyak section itu bergerak — entrance, reveal, date row, collapse, grid semuanya boleh — melainkan aturan tentang satu momen di mana animasi DAN feedback adalah kejadian yang sama, dan penundaan di situ bukan hiasan melainkan jawaban yang lebih lambat atas "jam 8 kosong tidak".

[2026-08-11] [decision] TIGA EFEK ORDER SECTION DIPILIH USER SEBELUM ADA KODENYA, sesuai aturan yang masih berlaku. (1) MASUK LAYAR: heading fade + naik 12px, date pills geser dari kiri, lalu baris slot menetas berurutan 40ms berjarak, total ~400ms. Stagger-nya adalah gagasan instruction-book yang dibuat harfiah — bagian-bagian datang satu per satu lalu duduk di tempatnya, yang persis argumen seluruh halaman ini tentang apa itu booking. (2) TAP SLOT: fill biru mendarat di 0ms, ring melebar dari titik tap lalu memudar selama 300ms. DIPISAH DUA LAPIS DENGAN SENGAJA — fill adalah JAWABAN atas "slot ini jadi milik saya sekarang tidak", jadi mendarat seketika; ring itu hiasan dan jalan setelahnya. Satu transisi 300ms tunggal akan membuat jawabannya datang 300ms terlambat, yaitu balasan lebih lambat yang menyamar sebagai kehalusan. (3) COLLAPSE: caret berputar 90deg, tinggi kontainer membuka 280ms, enam baris elapsed menetas 30ms berjarak; GSAP mengukur tinggi aslinya sendiri alih-alih menganimasikan ke auto, dan itulah yang menjaganya keluar dari budget CLS. Semuanya transform + opacity saja, semuanya lewat src/lib/motion.ts.

[2026-08-11] [build] PHASE 2 TASK 2 — ORDER SECTION. Date pills 14 hari, slot grid responsif 1/2/3 kolom, empat state melawan MSW mock, collapse "Sudah lewat", skeleton, error state, dan CTA. Logika pemisahan elapsed ditaruh di order.utils.ts sebagai fungsi murni dengan `now` yang diinjeksi, bukan dibaca dari jam internal — fungsi yang memanggil new Date() sendiri lulus jam 09.00 dan gagal jam 21.00, di mesin orang lain. 16 tes baru, total suite 90 (naik dari 74). BUNDLE: / naik dari 137.0KB ke 164.0KB, sisa 76.0KB dari plafon 240KB. Kenaikan 27KB itu TanStack Query plus komponen section; GSAP belum terhitung karena masih lazy dan baru dimuat saat efeknya jalan.

[2026-08-11] [gotcha] TES SAYA SALAH, KODENYA BENAR — UNTUK KETIGA KALINYA DI REPO INI. Saya assert 6 elapsed / 3 live pada jam 19.00 Jakarta, menalar dari mock di DESIGN.html alih-alih dari aturannya. isPastSlot membandingkan `currentHour >= slotStartHour`, jadi jam 19.00 slot 18.00-20.00 SUDAH LEWAT — dan itu benar: tidak ada yang bisa memesan jam yang sudah berjalan. Jawabannya 7/2. Tesnya diperbaiki dan satu tes tambahan ditulis khusus untuk mengunci perilaku batas itu, supaya asumsi yang sama tidak diulang sesi depan. Pola yang sama sudah muncul di isPastSlot dan di normalisePhone sebelumnya: yang salah ekspektasi saya, bukan implementasinya.

[2026-08-11] [gotcha] OVERFLOW 41px DI 375px, TIGA PENYEBAB BERLAPIS, DAN DUA PERBAIKAN PERTAMA SAYA TIDAK MENGUBAH APA PUN. (1) `min-w-0` pada grid item — tidak berpengaruh, karena `1fr` itu `minmax(auto,1fr)` dan minimum otomatisnya ada di TRACK, bukan di item; perbaikannya `minmax(0,1fr)`. (2) Root OrderSection juga `grid`, dan track implisitnya `auto`, jadi date row menyusun ukuran track dari kontennya sendiri dan `overflow-x-auto` tidak pernah aktif; diganti flex column. (3) YANG SEBENARNYA PALING PENTING: numeral section memakan 59px + gap 16px, menyisakan 268px untuk konten — sementara DESIGN.md sudah MENGUKUR bahwa slot cell butuh 343px karena "Menunggu Konfirmasi" 20 karakter dan tidak boleh dipotong. Terukur: cell butuh 323px, dapat 268px. Jadi children dibuat selebar penuh di HP dan baru ter-indent dari md ke atas. Indentasi itu selera, 343px itu batasan produk yang terukur — dan ketika keduanya bertabrakan, kejelasan menang, aturan yang sama yang menaruh Signal Blue di tempat dunia sumbernya mau merah bata. Tidak ada satu pun check di repo ini yang bisa melihat ini; yang menangkapnya screenshot dan getBoundingClientRect.

[2026-08-11] [milestone] NOMOR WHATSAPP KLIEN MASUK — PLACEHOLDER PERTAMA DARI TUJUH DICABUT. +6289682620666 diverifikasi dulu terhadap aturan domain sebelum ditulis: normalisePhone menghasilkan 6289682620666, national 89682620666 lolos /^8\d{8,11}$/. Disimpan dalam BENTUK wa.me (628…, tanpa +, tanpa spasi) di src/modules/home/home.constants.ts, bukan bentuk yang diketik klien — itu bentuk yang sama yang dipakai wa.me DAN WhatsApp Business API, dan bentuk yang sama yang dihasilkan normalisePhone dari input visitor di formulir. Menjaga kedua sisi dalam satu format itulah yang nanti membuat bot Phase 4 bisa mencocokkan pesan masuk ke booking lewat lookup langsung, bukan fuzzy matching. CTA jadi ANCHOR, bukan button dengan handler: di HP wa.me deep-link ke aplikasi WhatsApp, bukan buka tab, jadi memasangkannya dengan navigasi same-tab persis kombinasi yang ditangani tidak konsisten oleh in-app webview dan popup blocker — dan in-app browser Instagram adalah trafik utama di sini. Satu aksi, satu tujuan. Tanpa target=_blank juga: di HP pengambilalihan aplikasi ITULAH navigasinya, dan tab baru paksa cuma meninggalkan tab kosong.

[2026-08-11] [gotcha] HARD RULE 3 DITULIS SEBAGAI HITUNGAN, PADAHAL SEHARUSNYA ALLOWLIST. Bunyinya "rg TODO(content) must find all seven and nothing else" — begitu nomor WA masuk dan markernya dicabut, aturan itu langsung dilanggar oleh keberhasilan. Diperbaiki jadi allowlist: setiap TODO(content) harus menyebut salah satu dari tujuh kategori, dan item yang sudah dipasok kehilangan markernya. Enam tersisa. Sekalian PRD baris 41 yang masih menyebut "exactly the six declared categories" ikut diperbaiki. Pola yang sama muncul lagi: aturan yang benar saat ditulis, jadi salah karena pekerjaan berjalan — dan tidak ada check yang bisa melihatnya, karena check-docs memang menerapkan versi allowlist-nya sejak awal.

[2026-08-11] [build] Verifikasi wa.me dilakukan di browser sungguhan, bukan dari unit test saja: pilih tanggal, pilih slot available, baca href yang dirender. Hasilnya https://wa.me/6289682620666?text=Halo%2C%20saya%20mau%20booking%20lapangan%20Arena%20Player%20tanggal%2016%20Agu%202026%20jam%2010.00%20-%2012.00 — nomor benar, tanggal Indonesia benar, dan SEPARATOR SLOT "10.00 - 12.00" utuh dengan spasi-hubung-spasi. Yang terakhir bukan detail kosmetik: uniq_active_slot membandingkan time_slot sebagai TEXT, jadi slot yang sampai ke admin berbunyi "10.00-12.00" adalah slot yang BERBEDA bagi database. Ada tes khusus yang mengunci itu untuk kesembilan slot. Bundle / naik tipis ke 164.2KB, sisa 75.8KB.

[2026-08-11] [build] PHASE 2 TASK 3 — HERO, TERMASUK SATU MOMEN WEBGL. Logo monogram AP (TODO(content), sengaja monogram bukan sesuatu berbentuk lapangan — placeholder harus KELIHATAN seperti placeholder, bukan seperti logo yang belum disetujui siapa pun), headline Orbitron, subheadline, CTA "Pesan Lapangan" ke #order dengan scramble saat hover. LCP-nya HEADLINE: teks, server-rendered, font self-hosted, tanpa layout shift — bukan canvas yang client-only dan datang belakangan, bukan gambar karena gate hero-video gagal dan tidak ada foto lapangan ini yang dipasok siapa pun.

[2026-08-11] [milestone] CHUNK WEBGL TERUKUR 1.9 KB GZIP, CAP-NYA 40 KB. Fragment shader tulis tangan di atas satu segitiga fullscreen, TANPA LIBRARY SAMA SEKALI — bukan three.js (~150KB, lebih besar dari SELURUH sisa budget dan 3.75x cap), bukan OGL (~10KB), bukan apa pun. architecture.md baris 330 sudah menulis jawabannya jauh sebelum permintaan klien datang: "Reach for the shader, not the engine." Klien tetap dapat gradient bergerak yang mereka minta; yang berubah kendaraannya. Semua syarat izin dipenuhi dan bisa diperiksa: next/dynamic ssr:false, fallback statis dicat lebih dulu dan bertahan kalau context creation gagal, mati di bawah prefers-reduced-motion DAN saveData, DPR di-cap 1.5 karena gate-nya CPU-per-frame bukan kilobyte, rAF berhenti saat tab disembunyikan, dan konteksnya dilepas eksplisit lewat WEBGL_lose_context saat unmount — browser membatasi jumlah konteks WebGL per halaman, dan route change yang membocorkan satu adalah alasan kunjungan kedua tidak dapat hero sama sekali. Bisa dihapus dalam satu commit: tidak ada yang meng-import file itu selain satu panggilan next/dynamic.

[2026-08-11] [gotcha] SCRAMBLE TEXT ITU PLUGIN BERBAYAR. GSAP ScrambleTextPlugin bagian dari Club GSAP dan tidak terpasang, jadi efek yang klien minta ditulis tangan: tween pada objek proxy yang menggerakkan progress, dan onUpdate menyusun ulang stringnya. Nol kilobyte tambahan, tetap lewat motion.ts jadi reduced-motion tetap dihormati. Satu detail yang bukan kosmetik: labelnya di-set ulang secara eksplisit di onComplete. Pembulatan di onUpdate tidak bisa dipercaya mendarat tepat di indeks terakhir, dan scramble yang meninggalkan satu karakter salah di call-to-action utama lebih buruk daripada tidak ada efek sama sekali.

[2026-08-11] [build] PHASE 2 TASK 4 — KETENTUAN + LOKASI & KONTAK. Sepuluh aturan dirender dari src/modules/home/home.content.ts, disalin kata per kata dari PRD. Pakai <ol>, bukan <ul> dengan angka yang digambar: aturannya MEMANG bernomor dan klien merujuknya lewat nomor, jadi daftar bernomor palsu akan terlihat identik dan terbaca salah oleh screen reader. Kontak memakai nomor WhatsApp yang sudah masuk. Bundle / tidak bergerak di 166.7KB — konten statis, sisa 73.3KB.

[2026-08-11] [build] CHECK KE-14: ketentuan-verbatim, DAN DIBUKTIKAN GAGAL SEBELUM DIPERCAYA. Hard rule 5 bilang Ketentuan disalin verbatim, tapi aturan itu tidak bisa ditegakkan dengan mata: kegagalannya bukan crash dan bukan cacat visual, melainkan seseorang merapikan "Diluar" jadi "Di luar", menormalkan kapitalisasi campur, atau memendekkan aturan 7 — masing-masing sendirian terasa seperti perbaikan, dan bersama-sama adalah penulisan ulang perjanjian yang ditulis klien dan bukan milik kita. Visitor MENYETUJUI teks itu saat memesan. Jadi blok PRD dan array yang dikirim dibandingkan karakter per karakter. PROBE: ditanam persis perbaikan ejaan yang paling mungkin dilakukan editor bermaksud baik ("Diluar" -> "Di luar"), check langsung menyebut aturan 2 dan mencetak kedua versinya berdampingan; dikembalikan, hijau lagi. Guard yang tidak pernah dilihat gagal adalah guard yang tidak diketahui bekerja.

[2026-08-11] [decision] ALAMAT DIRENDER SEBAGAI LUBANG YANG TERLIHAT, BUKAN TEKS ISIAN ATAU UBIN PETA ABU-ABU. Kartunya bergaris putus-putus dan berbunyi "Alamat dan titik Google Maps menyusul". Product Principle 7: placeholder harus KELIHATAN seperti placeholder, karena mode kegagalan yang jujur adalah lubang jelas yang klien isi — bukan detail karangan yang tampak masuk akal lalu lolos tanpa disadari. Alamat yang tidak pernah dicek mengirim pelanggan ke lapangan yang salah, dan pin peta lebih buruk lagi: pin itu DIIKUTI tanpa dibaca.

[2026-08-11] [build] PHASE 2 TASK 5 — FOOTER + CLOSING CTA. Bukan Section dan tidak diberi ordinal: footer MENUTUP halaman, bukan melanjutkan perakitan, sama seperti hero yang membuka. CTA penutup kembali ke #order, BUKAN ke /booking — situs ini punya satu tujuan dan visitor belum memilih slot, jadi mengirim mereka ke formulir yang belum bisa diisi adalah tujuan kedua yang justru dihindari seluruh alur ini. Footer sengaja tidak diisi tautan sosial atau alamat karangan: footer yang dipadatkan dengan isian yang tampak masuk akal adalah cacat yang sama dengan alamat karangan, cuma lebih senyap.

[2026-08-11] [milestone] PHASE 2 DoD 5 DARI 6 TERCENTANG, SEMUANYA DIUKUR BUKAN DIKLAIM. (1) Lima section render, overflow horizontal NOL di 375/768/1024/1280/1440. (2) ATURAN DUA-SCROLL: order section mulai di 812px dengan viewport 812px — TEPAT 1.00 layar, di dalam batas 1-2 yang jadi hard rule 7. Dua CTA menuju #order, dari hero dan dari footer. (3) Slot grid membaca mock, EMPAT state bukan tiga. (4) wa.me diverifikasi di browser sungguhan. (5) TANPA HARGA di / — diuji terhadap teks yang benar-benar dirender: tidak ada "Rp", tidak ada "IDR", tidak ada angka berpemisah ribuan. Satu-satunya kata "harga" ada di dalam Ketentuan aturan 1 dan tidak menyebut angka apa pun. Kotak keenam — "setiap section mengeksekusi art direction 1b" — sengaja DIBIARKAN KOSONG: itu penilaian, bukan pengukuran, dan menandainya sendiri adalah persis jenis klaim yang /design-review ada untuk memeriksanya. Bundle / 166.7KB dari plafon 240KB, sisa 73.3KB.

[2026-08-11] [build] PHASE 3 DIMULAI — params, proof, schema, service. booking-form.params.ts memetakan empat keadaan masuknya /booking (valid, unusable, expired) dengan `now` diinjeksi supaya bisa dites di jam berapa pun; 10 tes, termasuk yang mengunci PENOLAKAN FORMAT SLOT NYARIS-BENAR: "20.00-22.00" tanpa spasi adalah slot BERBEDA bagi uniq_active_slot yang membandingkan time_slot sebagai TEXT, jadi regex yang menerimanya akan membiarkan jam yang sama dipesan dua kali tanpa error di mana pun. Satu keadaan sengaja TIDAK ADA di fungsi itu: "slot sudah tidak tersedia". Mengeceknya di sini adalah check-then-insert — dua visitor sama-sama lolos, sama-sama submit, dan hanya database yang bisa memutuskan. 409 saat submit yang berwenang, hard rule 1. Suite naik ke 105 tes.

[2026-08-11] [gotcha] GATE MERAH SAAT COMMIT INI, DAN BUKAN KARENA KODE DI DALAMNYA. arena-player-admin MUNCUL di disk sesi ini — sebelumnya check:domain melaporkan SKIPPED karena repo itu tidak ada, sekarang repo itu ada lengkap dengan .git, node_modules, docs dan CLAUDE.md, tapi src/domain/-nya kosong kecuali .gitkeep. Jadi check:domain melaporkan 8 file hilang di sisi admin, dan itu PERSIS pekerjaan guard tersebut: src/domain/ wajib byte-identical di kedua repo karena satu karakter yang meleset di TIME_SLOTS mematikan anti-double-booking di kedua aplikasi tanpa error. TIDAK SAYA PERBAIKI SENDIRI: menyalin file ke repo lain adalah tindakan ke luar batas yang tidak diminta di giliran ini, dan repo itu punya git sendiri. Diserahkan ke user. Sisanya hijau dan diverifikasi terpisah: lint, typecheck, format, 14 doc checks, 105 tes.

[2026-08-11] [engineer] `/booking` route shell — src/app/booking/page.tsx (composition only, searchParams typed explicitly rather than through the generated PageProps helper, same reasoning src/app/layout.tsx already records for LayoutProps) and src/modules/booking-form/booking-entry.tsx, which switches on `readBookingParams` and renders one of three things: `<BookingForm date slot />` for valid, a named ExpiredNotice ("tautan ini sebelumnya untuk tanggal {date} jam {slot}") for expired, and a deliberately vaguer UnusableNotice for missing/unparseable — the two copy blocks are not shared, per the instruction that a visitor whose link expired needs to know it once worked. Both notices share one CTA back to `/#order`. WORKED AROUND A LINT RULE NOT YET SEEN IN THIS REPO: `<a href="/#order">` from `/booking` trips `@next/next/no-html-link-for-pages` because it is a cross-route internal link, unlike home-page.tsx's same-page `#order` anchors — fixed with `next/link`, first use of it in the codebase. WAITED ON THE OTHER AGENT rather than stubbing `booking-form.tsx` myself, per the task's explicit instruction: typecheck failed on the missing module, polled the directory every 20s, found it 20s later, typecheck passed clean on retry. VERIFIED IN A REAL BROWSER REQUEST, not just the type system — hit the already-running dev server on port 3002 (3000 was taken by my own attempt, another one already up from a different session), and confirmed by content grep, not status code alone, per this project's own port-collision gotcha: expired link renders "Tautan Booking Ini Sudah Kedaluwarsa" plus the actual date/slot named ("1 Jan 2020", "10.00 - 12.00"), missing params render "Tautan Booking Tidak Valid", and a valid future date+slot renders neither notice (falls through to BookingForm) with all three returning 200. `pnpm lint`, `pnpm typecheck`, and `prettier --check` on both files pass clean; the one repo-wide warning and one prettier finding at commit time are both inside `booking-form.tsx`, which is out of this task's scope and was still being written by another agent in the same session.

[2026-08-11] [build] PHASE 3 — booking-form.tsx DAN booking-form.queries.ts. useCreateBooking membungkus createBooking lewat useMutation v5 (isPending, object-form args); mutation TIDAK PERNAH throw untuk hasil yang DIHARAPKAN (201/409/429/400 sudah dipetakan ke BookingOutcome di service.ts), hanya kegagalan transport sungguhan (jaringan putus, atau 5xx yang membuat validateStatus axios reject) yang lewat mutation.isError, dan komponen memperlakukannya sama seperti server_error. BookingForm merender: kartu ringkasan terkunci (tautan "Ubah jadwal" kembali ke /#order, tidak bisa diedit inline), kartu pembayaran dengan DUA TODO(content) — rekening+pemilik sebagai lubang bergaris putus-putus mengikuti pola alamat, dan rate card dengan komentar eksplisit bahwa TIDAK ADA angka rupiah boleh dikarang di situ karena itu satu-satunya placeholder yang akan DITINDAKLANJUTI pengunjung — lalu form dengan Nama Tim, Nomor WhatsApp (type=tel, inputMode=tel), Catatan (counter hidup, max 500), honeypot (aria-hidden, tabIndex=-1, present-and-empty), dan upload Bukti Transfer (accept dari PROOF_ACCEPT, checkProof dipanggil ulang saat file dipilih untuk feedback instan). Lima hasil submit dirender terpisah dan 409/429 SENGAJA tidak berbagi copy. Fokus dipindah terprogram: 400 memetakan fields ke input lalu fokus ke field pertama yang salah, empat hasil terminal lainnya (created/slot_taken/rate_limited/server_error) memindahkan fokus ke pesan hasil — bukan cuma 409 yang disebut eksplisit di dev-rules.md, karena visitor rate-limited yang tidak diberi tahu apa pun akan melihat form yang tampak diam tanpa alasan.

[2026-08-11] [decision] TIDAK PAKAI @hookform/resolvers — DIVERIFIKASI DULU, BUKAN DIASUMSIKAN. package.json diperiksa sebelum menulis form: paket itu tidak terpasang dan stack terkunci, jadi menambahnya perlu persetujuan user yang tidak diminta di giliran ini. bookingFormSchema.safeParse dipanggil tangan di dalam submit handler; react-hook-form hanya melacak value dan menjalankan setError — bukan resolver. Efek sampingnya satu warning ESLint yang diterima sadar: react-hooks/incompatible-library pada watch(), karena React Compiler tidak bisa memo-kan fungsi yang dikembalikan useForm(). Severity-nya warning (exit code 0), bukan bug di kode ini — ini keterbatasan react-hook-form yang dikenal luas terhadap React Compiler, dicatat di sini supaya code-reviewer tidak menganggapnya kelalaian. Diverifikasi: pnpm lint 0 errors/1 warning, pnpm typecheck bersih, pnpm format:check bersih, check:docs 14/14, check:unit 105/105 tidak terganggu.

[2026-08-11] [milestone] PHASE 3 FORM + ROUTE SELESAI, DIBANGUN DUA SUBAGENT PARALEL ATAS PERMINTAAN USER. Seam-nya didefinisikan dulu sebelum keduanya jalan — satu interface publik, `BookingForm({date, slot})` — supaya keduanya bisa menulis serentak tanpa menabrak file yang sama. Agent A: booking-form.queries.ts + booking-form.tsx. Agent B: src/app/booking/page.tsx + booking-entry.tsx. Keduanya melapor sudah verifikasi sendiri, TAPI LAPORAN AGENT BUKAN BUKTI: hasilnya saya uji ulang sendiri di browser sungguhan.

[2026-08-11] [build] VERIFIKASI INTEGRASI, DIJALANKAN SENDIRI BUKAN DIPERCAYA DARI LAPORAN. Tiga keadaan masuk /booking diuji di dev server: tanpa param -> "Tautan Booking Tidak Valid" tanpa form dan dengan tautan balik ke #order; link kedaluwarsa -> "Tautan Booking Ini Sudah Kedaluwarsa" dengan copy YANG BERBEDA, karena visitor yang linknya kedaluwarsa perlu tahu link itu pernah berfungsi; link valid -> form render dengan 5 input dan 5 label (satu-satu, jadi tidak ada input tanpa label), honeypot ada, dan TIDAK ADA atribut `disabled` native di tombol submit. Lalu jalur 409 diuji penuh: form diisi lewat DataTransfer supaya input file benar-benar terisi, nama tim "TEST409" memicu handler MSW, dan hasilnya "baru saja diambil orang lain" muncul sementara copy rate-limit dan copy sukses TIDAK muncul. Itu yang membuktikan 409 dan 429 tidak berbagi copy — persyaratan yang architecture.md sebut eksplisit karena menampilkan copy 409 ke visitor yang kena rate limit memberitahu mereka slotnya diambil padahal tidak.

[2026-08-11] [gotcha] @hookform/resolvers TIDAK TERPASANG, DAN ITU DIBIARKAN BEGITU. Stack terkunci di hard rule 9; menambah dependency butuh persetujuan dan harus lolos budget. Jadi zod di-wire manual lewat safeParse di dalam submit handler, dan react-hook-form cuma melacak nilai serta menjalankan setError/clearErrors. Nol kilobyte tambahan. Satu warning ESLint tersisa dan sengaja tidak dibungkam: react-hooks/incompatible-library pada watch() milik react-hook-form, keterbatasan React Compiler yang diketahui, bukan cacat di kode ini — lint tetap keluar 0 error.

[2026-08-11] [build] TIMEZONE WIB -> WITA, on the user's go the same hour the Lombok narrowing exposed it. src/domain/dates.ts: JAKARTA -> FIELD_TZ = tz("Asia/Makassar"), todayInJakarta -> todayAtField, currentHourInJakarta -> currentHourAtField — names went field-neutral on purpose, a function named after a city it no longer computes is the stale-name trap this repo has now caught seven times. Consumers swept: order-section.tsx, handlers.test.ts, home.service.test.ts, plus the WIB fixture in slots.test.ts. THE TEST INSTANTS MOVED WITH IT and got sharper: the old late-evening instant (18:30Z) only distinguished Jakarta from UTC; the new one (16:30Z) is 23:30 WIB but 00:30 WITA, so a helper quietly reverted to Asia/Jakarta now FAILS instead of passing by luck. order.utils.test.ts had six failures on first run — correct failures, its instants were authored in WIB — shifted one hour, all 105 tests green. Docs swept where they state product truth (architecture.md tree+table+tz-option example, database.md rule line, PRD.md task 6 + slot rules, PRODUCT.md settled bullet); "on an Asia/Jakarta machine" mentions kept, those describe the developer machine, and history stayed verbatim per convention. ADMIN RE-COPY FAILED AND IS OWED: the copy landed, verified byte-identical with cmp, then the concurrent step 08 session in that worktree restored its tree and silently took dates.ts/dates.test.ts back to WIB along with three doc sweeps — hard rule 10's exact defect shape, live. Not re-attempted; the debt is written into admin's PROGRESS.md and its check:domain will correctly fail until its owning session re-copies.

[2026-08-11] [milestone] SRC/DOMAIN/ DISINKRONKAN KE REPO ADMIN — bahaya lintas-repo yang paling nyata hari ini ditutup. Session sebelah memigrasikan repo ini ke WITA di 96817dd karena lapangannya di Lombok; salinan admin masih WIB, dan tiga file melenceng: dates.ts, dates.test.ts, slots.test.ts. Kalau dibiarkan, admin menghitung "hari ini" beda satu jam dari situs publik. TIDAK ADA ERROR YANG MUNCUL — uniq_active_slot membandingkan time_slot sebagai TEXT dan bergantung pada kedua sisi sepakat, jadi mode kegagalannya adalah dua aplikasi yang diam-diam memegang keyakinan berbeda tentang hari pemesanan yang sama. Delapan file disalin dari repo ini sebagai sumber, karena repo inilah yang memegang migrasinya. Terverifikasi: "check:domain — 8 file(s) identical, 2 dep(s) aligned" — dependency-nya ikut sejajar, jadi tidak ada temuan tersembunyi di baliknya. Di-commit di repo admin sebagai d8893ad, menyebut commit sumbernya supaya kedua histori bisa disejajarkan nanti.

[2026-08-11] [gotcha] GATE PERFORMA MASIH BELUM TERUKUR, DAN SAYA TIDAK MENCENTANGNYA. Build produksi dijalankan dan disajikan di port 3010 — dikonfirmasi lewat KONTEN ("Pilih Jam. Kirim. Main."), bukan status code, karena port 3000 di mesin ini milik proyek lain dan repo ini sudah dua kali tertipu. Yang terukur di localhost tanpa throttle, viewport 375: TTFB 10ms, FCP 116ms, domReady 69ms, load 301ms. YANG ITU BUKAN GATE-NYA. DoD meminta Lighthouse mobile >= 85 dan LCP < 2.5s di Android mid-range yang di-throttle, dan localhost tanpa throttle tidak mengukur satu pun dari keduanya — biaya yang mengikat untuk shader hero dan stagger GSAP adalah CPU per frame, dan itulah yang justru dihapus oleh mesin cepat tanpa throttle. Entri largest-contentful-paint juga kembali kosong: observer-nya harus terpasang sebelum paint, jadi angkanya tidak bisa diambil setelah fakta. Dicatat sebagai indikator, bukan sebagai kelulusan. Kotak DoD-nya tetap kosong.

[2026-08-11] [build] PHASE 3 DoD — TIGA KOTAK DICENTANG, DIUKUR DI BROWSER BUKAN DIBACA DARI KODE. Aksesibilitas: 5 input terlihat, 5 label, satu-satu; aria-describedby ada di setiap field ber-aria-invalid; inputmode="tel" terkonfirmasi di field telepon. Summary card memantulkan query param. Validasi memblokir submit dan memunculkan ketiganya: "Nama tim minimal 2 karakter", "Nomor tidak valid", "Format harus JPG, PNG, atau WEBP". Catatan lingkungan: submission HANYA bisa diuji di dev, karena MSW sengaja dikeluarkan dari build produksi — itu desain, bukan keterbatasan.

[2026-08-11] [gotcha] SAYA MENGUMUMKAN DEFECT YANG BUKAN DEFECT, DAN TES SAYA SENDIRI PENYEBABNYA. Run pertama saya isi tiga nilai salah sekaligus — nama tim "A", telepon "12345", file PDF — dan hanya error file yang muncul; aria-invalid pada dua field lain tetap "false" padahal nilainya jelas ada di DOM. Saya sempat menyebutnya cacat validasi. Bukan. Run pertama gagal di proof, lalu perbaikan file saya kirim lewat event sintetis ke form yang state react-hook-form-nya sudah basi, jadi yang saya ukur adalah sisa dari percobaan sebelumnya. Di run BERSIH — muat ulang halaman, isi sekali, submit sekali — ketiga pesan muncul dan submit-nya diblokir. Pelajarannya bukan "hati-hati dengan DataTransfer": melainkan bahwa menguji form berstate lewat event sintetis menumpuk state antar percobaan, jadi setiap percobaan harus mulai dari halaman yang baru dimuat. Ini kedua kalinya hari ini bukti yang saya kira konklusif ternyata melihat hal lain.

[2026-08-11] [build] AFFORDANCE RENTANG JAM BERTURUT-TURUT — satu-satunya tempat desain ini melayani metrik bisnis kliennya. PRODUCT.md menyebutnya dua kali sebagai "gap terjelas antara tujuan dan build": klien mengukur produk ini dari terisinya JAM MATI, dan grid-nya netral soal itu — 07.00 dan 20.00 dirender identik padahal yang satu hampir selalu kosong dan yang satu rebutan. Sinyalnya diturunkan dari sembilan status yang SUDAH dikembalikan per tanggal: nol endpoint baru, nol harga, nol klaim yang tidak bisa dihitung. Copy: "Bisa main {n} jam berturut-turut", --text-xs di --color-interactive, baris sendiri di dalam sel.

[2026-08-11] [decision] REFRAMING-NYA YANG BIKIN INI JUJUR, BUKAN MEKANISMENYA. Badge-nya TIDAK pernah berkata "ambil jam mati ini" — itu kepentingan klien, bukan kepentingan pengunjung, dan grid pemesanan adalah tempat yang salah untuk mendorong. Dia menjawab pertanyaan yang kapten tim memang punya: BISA MAIN LEBIH LAMA NGGAK? Kedua kepentingan kebetulan sejajar di jam sepi, dan kesejajaran itulah yang membuatnya afordans dan bukan manipulasi. Tidak menyebut harga sama sekali, karena tarifnya belum ada dan harga karangan adalah satu-satunya placeholder yang akan ditindaklanjuti pengunjung.

[2026-08-11] [decision] ATURAN ANTI-ENCER ADALAH DESAINNYA, BUKAN CATATAN KAKI. Minimum TIGA slot berurutan (6 jam), bukan dua: rentang dua itu biasa — kebanyakan hari punya beberapa — jadi mem-badge-nya akan menandai separuh grid dan sinyalnya terbaca sebagai hiasan. Satu badge per tanggal, di slot PERTAMA rentang terpanjang saja; rentang yang lebih awal menang saat seri karena dia menyisakan malam di belakangnya. Tiga badge untuk satu fakta adalah tiga klaim. pending, booked DAN elapsed sama-sama memutus rentang, dan fungsinya menerima array `live` dari partitionSlots, bukan respons mentah — pagi yang sudah lewat bukan enam jam yang bisa dipesan. Terverifikasi di browser: satu badge di muat awal ("Bisa main 8 jam"), dan lintas empat tanggal jumlahnya tidak pernah lebih dari satu. Nol overflow di 375px.

[2026-08-11] [gotcha] EKSPEKTASI TES SAYA SALAH UNTUK KEEMPAT KALINYA, DAN LAGI-LAGI SOAL JAM. Saya assert 3 slot live di at(10), padahal helper at() di file itu sekarang WITA — jadi at(10) itu 18:00, bukan 17:00, dan jawabannya 2. Kodenya benar. Pola yang sama sudah muncul di isPastSlot, di normalisePhone, dan di split elapsed: yang salah asumsi batas saya, bukan implementasinya. Yang berubah kali ini cuma penyebabnya — migrasi WITA menggeser arti setiap angka di helper itu, dan tes yang saya tulis sebelum migrasi tidak otomatis ikut bergeser.

[2026-08-11] [milestone] LOGO KLIEN TERPASANG — placeholder monogram AP dicabut, dan itu placeholder KEDUA dari tujuh yang lunas. Mark aslinya navy, tajam, atletis; dia justru membenarkan arah desain yang sudah dipilih, bukan menabraknya. Dipasang lewat next/image dengan width/height eksplisit, dan SENGAJA TANPA `priority`: elemen LCP-nya adalah headline — teks, server-rendered, font self-hosted — dan logo 1042x502 yang di-preload akan bersaing untuk slot itu dan bisa merebutnya, menukar LCP termurah dan paling andal yang ada dengan sebuah gambar. Terverifikasi hidup di browser: termuat 83x40, dilayani lewat pipeline optimasi Next, nol overflow.

[2026-08-11] [decision] OG DAN TITLE DIPERSEMPIT KE LOMBOK, BUKAN INDONESIA. PRODUCT.md tegas bahwa ini SATU lapangan fisik dan tidak boleh ada yang terbaca sebagai platform nasional; copy-nya ditulis untuk orang yang mencari "lapangan mini soccer Lombok / Mataram". Deskripsi yang bilang "Indonesia" bersaing di kueri yang tidak mungkin dimenangkan lapangan ini dan melewatkan kueri yang justru penting. Kartu Twitter pakai summary_large_image, bukan summary: PRODUCT.md menandai link yang ditempel ke grup chat sebagai jalur trafik nyata, dan kartu besar itulah yang membuat link tertempel terbaca di dalam thread alih-alih jadi remah seukuran favicon. Favicon dan apple-icon SENGAJA tidak dideklarasikan manual — src/app/icon.png dan apple-icon.png itu konvensi file Next, framework yang menerbitkan tag-nya dan mem-hash namanya, dan mendeklarasikan ulang dengan tangan akan jadi sumber kedua yang melenceng begitu salah satunya diganti.

[2026-08-11] [milestone] PHASE 2 DoD 7 DARI 8. Kotak art direction dicentang atas DUA PENILAIAN INDEPENDEN, tidak satu pun milik yang membangunnya: /design-review memberi B+ dengan AI-slop A, dan klien melihat / lalu menyetujui. Itu bedanya dengan mencentang sendiri, yang sempat saya tolak lakukan justru karena penilaian atas pekerjaan sendiri bukan bukti. Satu kotak tersisa: gate per-section — Lighthouse mobile >= 85, prefers-reduced-motion, keyboard, tanpa CLS — dan Lighthouse-nya ditunda user sampai semuanya selesai.

[2026-08-11] [build] TIGA DARI EMPAT ITEM GATE PER-SECTION DIUKUR; SISANYA LIGHTHOUSE YANG DITUNDA USER. CLS 0.0000 dengan NOL layout-shift entry sama sekali — bukan "di bawah 0.1", tapi tidak ada pergeseran yang tercatat. 28 elemen fokusable dan NOL tabindex positif, jadi tidak ada perangkap urutan tab. prefers-reduced-motion dijaga TIGA LAPIS independen: globals.css baris 202 mematikan durasi animasi dan transisi; src/lib/motion.ts baris 189 memeriksa matchMedia dan RETURN SEBELUM GSAP DIUNDUH SAMA SEKALI, lalu gsap.matchMedia() di baris 199 menjaga cabang animate; dan hero-canvas.tsx baris 104 dan 106 mematikan WebGL di bawah reduced-motion DAN di bawah saveData.

[2026-08-11] [gotcha] GREP PERTAMA SAYA CUMA MENANGKAP KOMENTAR, BUKAN PANGGILANNYA. Mencari "prefers-reduced-motion" di motion.ts mengembalikan baris 17 dan 82 — dua-duanya prosa yang MENJELASKAN gerbangnya, bukan gerbangnya. Kalau saya berhenti di situ, saya akan melaporkan gerbang yang ada berdasarkan bukti yang tidak pernah melihatnya, yang persis mode kegagalan yang dikejar sepanjang sesi ini. Dicari ulang untuk matchMedia dan saveData secara spesifik, dan panggilan aslinya memang ada. Sekalian dipastikan hard rule 6 masih utuh: gsap.from di hero.tsx dan order-section.tsx berada DI DALAM callback animate: ({ gsap }) => {...}, jadi keduanya menerima instance dari wrapper alih-alih mengimpor GSAP langsung — itu pola yang diizinkan, bukan pelanggaran.

[2026-08-11] [milestone] PHASE 3 DoD 5 DARI 6, DAN KEDUA FASE SEKARANG MENGGANTUNG PADA SATU PENGUKURAN YANG SAMA. Keempat state submission diverifikasi di browser lewat pemicu teamName milik mock, bukan dibaca dari kode: 201 sukses dengan fokus berpindah ke hasil, 409 "baru saja diambil orang lain", 429 "Tunggu sebentar... tidak ada yang salah dengan pemesananmu" di triple AMBER, dan 400 memetakan fields kembali ke input lewat aria-invalid. 409 DAN 429 TIDAK BERBAGI COPY MAUPUN KELUARGA WARNA — itu yang architecture.md tuntut eksplisit, karena menampilkan copy 409 ke pengunjung yang kena rate limit memberitahu mereka slotnya diambil padahal tidak. E2E terverifikasi dua kaki terpisah, dan itu BENTUK YANG BENAR: WhatsApp adalah putus yang disengaja di tengah rantai, bukan alur yang bersambung.

[2026-08-11] [gotcha] REKENING DAN DAFTAR TARIF TERNYATA TIDAK MENAHAN SATU KOTAK DoD PUN, DAN SAYA SUDAH DUA KALI MELAPORKANNYA SEBAGAI PENGHALANG. Saya bilang keduanya "nahan Phase 3 tutup penuh". Salah: enam kotak DoD Phase 3 semuanya tentang validasi, submission, summary, aksesibilitas, gate, dan E2E — tidak satu pun menyebut angka. Keduanya TODO(content), dan formulirnya berfungsi penuh tanpa mereka; itu justru gunanya placeholder yang terlihat. Yang benar-benar menahan cuma Lighthouse mobile, satu pengukuran, dan itu menahan kotak terakhir DI KEDUA FASE sekaligus. Pola yang berulang sepanjang sesi ini: penghalang yang saya laporkan lebih besar daripada penghalang yang sebenarnya, sampai daftarnya dibaca ulang baris per baris.

[2026-08-12] [decision] ARAH SENI DIGANTI TOTAL — DAN PERSETUJUAN KLIEN TIDAK IKUT PINDAH. docs/DESIGN.md ditulis ulang untuk konsep baru "velocity": halaman miring pada satu sumbu -8deg yang diambil dari kemiringan mark logo klien, tipe display naik dua kali lipat jadi 48->152px uppercase dengan satu kata di-outline (`KIRIM.`), header sticky, marquee di kaki hero, dan — perubahan terbesar — PITA NAVY FULL-BLEED yang berselang-seling dengan seksi terang. Yang digantikan: north star "buku instruksi perakitan", angka grey terisi yang menembus tepi kiri plus keyline navy, dan dunia terang tanpa pengecualian. Sumbernya satu file prototipe di scratchpad, dibaca dari CSS custom property dan struktur seksinya, bukan dari tangkapan layar.

[2026-08-12] [decision] KLIEN MENYETUJUI ARAH YANG LAMA, BUKAN YANG INI — DAN ITU ALASAN CATATAN INI ADA. Persetujuan 2026-08-11 ("klien melihat / lalu menyetujui", Phase 2 DoD kotak art direction) diberikan untuk arah TERANG SAJA yang sekarang sudah usang. RE-APPROVAL TERUTANG DAN BELUM TERJADI. Ditulis eksplisit di kepala docs/DESIGN.md sebagai blockquote supaya sesi berikutnya tidak salah baca ini sebagai keadaan yang sudah disetujui, dan supaya tidak ada yang mencentang ulang kotak DoD art direction atas dasar persetujuan yang menilai halaman lain. Kode TIDAK disentuh sama sekali giliran ini — urutan dokumen-dulu dipilih user secara sadar, karena membangun sebelum spesifikasinya ditulis persis yang menghasilkan drift dokumen-vs-kode yang baru saja dibereskan seharian.

[2026-08-12] [decision] DUA ATURAN DI DESIGN.md DULUNYA SALAH DAN DITULIS ULANG, BUKAN DIBIARKAN BERTABRAKAN DENGAN KODE. (1) "Terang dan biru-putih, tidak pernah gelap — anti-reference mengikat" adalah PARAFRASE YANG LEBIH KETAT DARIPADA UCAPAN KLIEN: PRODUCT.md merekam kata mereka sendiri, "never dark neon". Yang dilarang adalah DUNIA dark-neon, bukan navy merek sendiri sebagai permukaan. Diganti aturan lima syarat yang bisa difalsifikasi — ground tetap blue-50, seksi terang lebih banyak daripada navy, hero dan seksi order TIDAK PERNAH navy, teks pita adalah ground halaman itu sendiri (15.69:1), nol glow/neon/hue jenuh, dan navy-nya navy logo. Ditambah tes tangkapan layar 375px supaya bisa diperiksa, bukan diperdebatkan. (2) Larangan kicker/eyebrow DIPERSEMPIT, tidak dihapus: tepat SATU eyebrow, di hero, di atas h1, karena isinya fakta yang headline tidak bawa (Mini Soccer / WITA / 06.00-24.00). Di atas h2 tetap haram selamanya — itu pola yang aturan aslinya memang tuju.

[2026-08-12] [gotcha] KONSEPNYA MENABRAK KEBENARAN PRODUK DI SEMBILAN TITIK, DAN TIGA DI ANTARANYA CACAT AKSESIBILITAS TERUKUR. Eyebrow-nya berbunyi "Asia/Jakarta" dan blok lokasinya "06.00-24.00 WIB" — keduanya zona pra-migrasi dan SALAH; lapangannya di Lombok dan seluruh lapisan tanggal pin Asia/Makassar sejak 2026-08-11. Tag footernya mengklaim "produksi memakai three.js" — three.js ~150 KB gzip melawan cap chunk lazy 40 KB, jadi yang dikirim tetap shader GLSL tulis tangan. Dan yang paling mahal: di pita navy, konsepnya memakai blue-600 untuk kata aksen serta angka aturan, dan navy-400 untuk catatan di bawah daftar. Diukur: 3.30:1 dan 2.46:1 di atas navy-900. Yang kedua GAGAL AA di ukuran apa pun, dan itu terjadi persis di seksi tempat sepuluh aturan yang disetujui pengunjung dibaca. Dua token baru ditambahkan untuk menutupnya — blue-400 (6.72:1) dan navy-200 (7.91:1), keduanya KHUSUS di atas gelap: blue-400 di atas putih cuma 2.54:1. Enam baris semantik on-band ditambahkan supaya komponen tidak meraih primitif terang. Sisanya: tombol header ~37px (di bawah lantai tap 44px), body pakai stack sans sistem alih-alih Inter (Inter komitmen klien; prototipe cuma menanam satu font), sel elapsed dirender inline alih-alih grup "Sudah lewat (N)" yang runtuh, loop rAF hero jalan selamanya tanpa dihentikan saat keluar viewport, dan radius 8px yang jadi radius ketiga.

[2026-08-12] [gotcha] YANG SELAMAT DARI REDESAIN ADALAH PERILAKU, DAN ITU DIPISAHKAN SECARA EKSPLISIT DARI TAMPILAN. Empat state slot termasuk elapsed, pola aria-pressed/aria-disabled, aturan never-native-disabled, tiap lantai kontras, afordans rentang jam berturut-turut, aturan dua-scroll, larangan harga di /, Ketentuan verbatim, budget performa, dan aturan semua animasi lewat src/lib/motion.ts — semuanya batasan, bukan gaya, jadi tidak satu pun dinegosiasikan ulang. Ditulis sebagai daftar tersendiri di bagian "What changed on 2026-08-12" supaya sesi berikutnya tidak menyangka redesain membuka kembali sesuatu yang tidak dibukanya. TERUTANG DAN TIDAK DIKERJAKAN GILIRAN INI, dicatat di file yang sama: docs/DESIGN.html masih merender arah usang; .impeccable/design.json masih memegang dua entri shadow lama, container-max 1100px, typographyMeta tanpa numeral/eyebrow, dan semanticLayer.map tanpa satu pun baris on-band. Terverifikasi: node scripts/check-docs.mjs 14/14 lolos, pnpm format bersih.

[2026-08-12] [engineer] src/modules/home/components/section.tsx migrated to the velocity token layer, one-file scope. Fixed the immediate break — `var(--text-step)` renamed to `--text-numeral` at both usages, which is what had sized the numeral to nothing. Rebuilt the numeral per DESIGN.md's Numbered-Step Rule: outlined + leaning (`skewX(var(--skew))`, `origin-bottom` so the baseline stays anchored and only the top leans), stroke gated behind `@supports (-webkit-text-stroke: 1px currentColor)` with the un-stroked `grey-200` fill as its own fallback, and a `forced-colors:` rule (with `!`) that drops the stroke and fills `CanvasText` — the Outline-Needs-A-Floor Rule's three failure modes, all covered. No runtime floor guard needed for the ≥24px rule since `--text-numeral` clamps 56px→144px. REMOVED, per DESIGN.md: the old filled-grey bleed-off-edge treatment (`xl:-ml-24`) and the navy keyline (`border-t` / `first:border-t-0`) — the light/navy band alternation living elsewhere now does that job. Container widened to `--container-max` (1280px) and section rhythm moved onto the three fluid tokens (`--space-section-y`, `--space-section-x`, `--space-head-gap`), replacing the hardcoded `max-w-[1100px] px-4` / `py-12 md:py-24` / `mt-8 md:mt-12`. All three measured constraints called out in the file's own comments survived untouched: the `minmax(0,1fr)` fixed numeral track, phone-width `col-span-2` children indenting only from `md`, and the `aria-hidden` non-eyebrow numeral. VERIFIED IN A REAL BROWSER, NOT INFERRED: `pnpm lint`/`typecheck`/`format:check` all clean; zero horizontal overflow (`scrollWidth - clientWidth`) at 375/414/768/1280/1440; the numeral's own `getBoundingClientRect()` never leaves its column even at 320px (right edge 127px of a 320px viewport); computed style confirms `-webkit-text-stroke-width: 1.5px`, stroke colour `#E5E7EB` (`--color-border`), and the skew matrix. FOUND BUT OUT OF SCOPE, REPORTED RATHER THAN FIXED: at 320px only, `document.documentElement.scrollWidth` runs 19px over — traced element-by-element to OrderSection's date-row pills and slot-grid cells (`order-section.tsx`), confirmed by hiding the date row and re-measuring, not to anything in section.tsx. ALSO FLAGGED, NOT BUILT: DESIGN.md's Numbered-Step Rule wants the numeral stroke to read `navy-700` on a navy band, but Section has no on-band variant today and no current caller (`home-page.tsx`) passes one — every usage is still light-section only, so the on-band mode is a `home-page.tsx`-side decision for whoever wires the navy bands in, not built here.

[2026-08-12] [engineer] src/modules/home/components/hero.tsx rebuilt for the velocity redesign, hero-only scope (hero.tsx + hero-canvas.tsx, per the launching agent's boundary — globals.css, section.tsx, home-page.tsx untouched, other sessions own those). THE ONE EYEBROW IN THE SYSTEM: `Mini Soccer · WITA · 06.00–24.00` above the h1, preceded by an `aria-hidden` 34×2px `--color-interactive` rule skewed via the shared `[transform:skewX(var(--skew))]` pattern already established in section.tsx (confirmed live-editing concurrently — matched its convention rather than inventing a second one). Headline split into three block-level lines (`Pilih Jam.` / `Kirim.` / `Main.`) so `Kirim.` alone carries the Outline-Needs-A-Floor treatment: solid `--color-interactive` fill as the no-stroke fallback, `supports-[-webkit-text-stroke:1px_currentColor]:` for the transparent+2px-stroke branch, `forced-colors:!` for both properties. Two CTAs replace the old single one — filled "Pesan Lapangan →" and outlined "Lihat Lokasi" → `#lokasi`, both `--radius-control` (12px, not the old 10px) and 56px tall, stacking below `min-[420px]` per the Hero component spec. Scramble hover moved from the whole anchor to a dedicated label `<span>` so the arrow glyph (its own hover-only `translate-x-[5px]`) never gets swept into the character-swap tween. hero-canvas.tsx gained the Off-Screen-Is-Off Rule: an `IntersectionObserver` (`threshold: 0`) plus the pre-existing `visibilitychange` handler now jointly gate the rAF loop through idempotent `startLoop`/`stopLoop`, and `running` now starts `false` instead of `true` — a deep link that mounts the hero scrolled out of view no longer spends a frame budget on nobody. VERIFIED: `pnpm lint`/`typecheck`/`format:check` clean (one pre-existing warning, unrelated file). Rendered against a real (not curl-only) browser: zero horizontal overflow on the hero `<section>` at 320/375/414/768/1280/1440 — the 19px overflow present at 320px page-wide is the OrderSection date-row/slot-grid defect the section.tsx entry above already named, reconfirmed by scoping the measurement to `<section>` alone rather than `document.documentElement`. Compiled CSS inspected directly (not just computed style) to confirm both outline fallbacks actually generated: `@supports (-webkit-text-stroke: 1px currentColor) { .supports-\[-webkit-text-stroke\:1px_currentColor\]\:text-transparent { color: rgba(0,0,0,0); } }` and `@media (forced-colors: active) { .forced-colors\:\!text-\[color\:CanvasText\] { color: canvastext !important; } }`. Canvas confirmed live (`aria-hidden="true"`, `opacity: 1`, WebGL2 context created) and its rAF loop confirmed running while mounted-and-visible (86 fps sampled via a monkey-patched `requestAnimationFrame` counter). NOT LIVE-VERIFIED, CODE-REVIEWED ONLY: the stop-on-scroll-away and resume-on-scroll-back halves of the Off-Screen-Is-Off Rule — the shared `browse` daemon in this session restarted silently on roughly half of all commands issued against it (confirmed via repeated `about:blank`/PID changes mid-sequence, consistent with contention from a concurrent session also driving it), and every multi-step timed sequence needed to sample frame counts before/during/after a scroll lost its patched state before completing. The `startLoop`/`stopLoop` pair is the same idempotent shape as the pre-existing `visibilitychange` handler this file already shipped and which an earlier entry in this log measured as working, so confidence is high, but this is disclosed rather than claimed as measured. OUT OF MY SCOPE, REPORTED NOT BUILT: the decorative 7%-opacity parallax mark and the hero-foot marquee DESIGN.md's Hero component section describes — neither was in the launching agent's explicit ask list, the marquee needs its own file, and the parallax's "slow" speed is unspecified enough to need an AskUserQuestion round before any GSAP goes in, which the launching agent's task did not include. Also left alone: the full skewed button-wipe hover treatment DESIGN.md's Buttons section specifies — it is a cross-page component pattern (header CTA, closing CTA, both hero CTAs) that does not yet have a shared home, so building one ad hoc inside hero.tsx risked shipping a treatment the rest of the page would then have to match retroactively; kept the existing simple colour-swap hover instead, plus the arrow-advance which is cheap and hero-local.

[2026-08-12] [milestone] LAPISAN VELOCITY PERTAMA MENDARAT — token, Section, hero, DESIGN.html, dan sidecar, dikerjakan empat agent paralel di file yang tidak beririsan. Terverifikasi sendiri setelahnya, bukan dari laporan: check hijau penuh termasuk check:domain, 115 tes, dan hero merender arah barunya di 375px dengan eyebrow berbunyi WITA, "KIRIM." ber-outline, dua CTA, dan overflow nol.

[2026-08-12] [gotcha] DESIGN.md BERTENTANGAN DENGAN DIRINYA SENDIRI, DAN SAYA MENGUTIP ANGKA BASI SEPANJANG SESI. Dua cacat di spek, keduanya ditemukan agent yang membangun DARI spek itu. (1) Baris 260 menyatakan "satu nilai skew, yang kedua adalah cacat"; baris 791 menulis skewX(-12deg) — nilai kedua, di dokumen yang sama, empat ratus baris di bawah aturannya sendiri. Itu transkripsi dari prototipe, bukan keputusan; diganti var(--skew). (2) "Menunggu Konfirmasi butuh 146px" saya kutip berulang kali — di brief agent, di komentar kode, di justifikasi breakpoint. Diukur ulang DENGAN ORBITRON BENAR-BENAR TERMUAT: 133px, karena redesign memindahkan label dari 14px ke 13px. Kesimpulannya tidak berubah — 133 lebih kecil, jadi setiap kolom yang lolos 146 pasti lolos 133 — tapi angka yang jadi dasar keputusan berikutnya sekarang yang terukur.

[2026-08-12] [gotcha] OVERFLOW 19px DI 320px BUKAN REGRESI, DAN SAYA SEMPAT MENYEBUTNYA BEGITU. Slot cell butuh 321px sementara kolomnya 288px setelah padding. Saya menduga pass token yang memperkenalkannya; tidak terbukti — --space-section-x di 320px adalah 16px, sama persis dengan px-4 yang lama. Yang sebenarnya: DESIGN.md mengukur 343px PADA 375px, dan di 320px baris 343px itu tidak ada. DESIGN.md menyatakan target desainnya 375px, jadi 320px di bawah target, bukan lebar yang rusak. Tetap harus diputuskan, dan tempatnya di giliran order-section, bukan ditambal di section.tsx. Dua agent menemukannya secara independen dan tidak satu pun menambalnya di file yang salah.

[2026-08-12] [engineer] ORDER SECTION PORTED to the velocity redesign — order-section.tsx, slot-cell.tsx, date-pills.tsx, the three files this turn owned. THE 320px OVERFLOW THE PRIOR TWO ENTRIES NAMED IS SETTLED BY COMPLETING THE PORT, NOT BY A NEW RULE: SlotCell's row layout (time beside label, needing ~321px) is gone, replaced with DESIGN.md's "stacked at every width" layout — time above, label below, always, not just from `md`. The cell's own width need drops to `max(133px label, ~127px time) + 32px padding ≈ 165px`, which is comfortably under the ~240-288px a single grid column has even at 320px. No narrower breakpoint, no truncation, no special-cased width — the redesign's own rule already closes the gap once it is actually applied. Wrote down WHY explicitly in slot-cell.tsx rather than leaving it to be re-discovered. THE ORDER PANEL is new: a white `rounded.panel`/`shadow-lg` card (`@container`, `container-type: inline-size`) wrapping the date row, scarcity line, elapsed disclosure, slot grid and hand-off bar — DESIGN.md: "the grid no longer measures the viewport, it measures the panel," which is only true if the query container IS the panel, so the slot grid's 640px/1180px breakpoints are now Tailwind v4 `@min-[640px]:`/`@min-[1180px]:` container-query variants on the panel, not `md:`/`lg:` viewport ones. VERIFIED IN A REAL BROWSER: at 700px viewport the panel (container-box ≈600-640px there) sits at 1 column, at 2000px viewport it holds at 2 columns because the panel's own width caps around 915px under the CURRENT page composition (container-max 1280 minus Section's numeral track) — 3 columns genuinely cannot engage until a future two-column order-section layout (copy + legend beside a narrower panel, per DESIGN.md's Order Section spec) widens what's available, which is NOT built here and belongs to home-page.tsx. That is the container-query mechanism working as designed, not a defect: the breakpoint stays correct automatically once that composition lands, with nothing here needing to change. THE LEGEND (three chip rows) and the copy+legend two-column composition are explicitly NOT built — out of the three files in scope, reported rather than invented. OTHER PORT WORK: 1.5px borders (was 1px) on every state; `--radius-control`/`--radius-panel` everywhere a literal 10px/14px used to be; hover on an available cell now takes `--glow-interactive` gated `pointer-fine:` (was an ungated `shadow-md`); selected-slot label now reads "Dipilih" at 85% white instead of leaving the status label ("Tersedia") showing under the fill; the hand-off bar is now a real `aria-live="polite"`-wrapped bar showing the slot on the left and "Lanjut ke WhatsApp →" on the right (was one merged sentence), animated in via a fourth `useMotion` call (rise 12px, fade, 350ms) keyed on whether a slot IS selected rather than on which one, so switching between two already-selected slots does not replay the entrance — the 0ms fill it follows is unchanged; date pills gained the 2px hover lift and lost their visible scrollbar (`scrollbar-width:none` plus the WebKit pseudo-element, cross-engine, DESIGN.md: "scrolls with its scrollbar hidden" — the pill shape already says "this scrolls").

[2026-08-12] [gotcha] AN 8px PAGE OVERFLOW AT 320px SURVIVES THIS PORT, AND IT IS THE SECTION HEADINGS, NOT THE KETENTUAN LIST. The port's own numbers are clean: `#order` reads `scrollWidth === clientWidth` at 320/375/414/768/1180/1280. The residual 8px lives in the Ketentuan `<section>` (328 vs 320), and the first hypothesis — the `<ol>`'s `list-decimal` marker box against `pl-5` — is WRONG. Measured directly: that `<ol>` reads `scrollWidth 254, clientWidth 254`, perfectly clean. The overflowing element is the `<h2>` itself.

The mechanism is `section.tsx`'s fixed numeral track, not any file this port touched. At 320px the section's content box is 288px; the numeral track computes to 97.9px (`--text-numeral` resolves to 57.6px there via `2rem + 8vw`, times the 1.7 two-digit allowance) and the `gap-x-4` takes 16px, leaving the heading column exactly 174.1px. Two uppercase Orbitron headings do not fit that: "KETENTUAN" needs 198px and "PESAN LAPANGAN" needs 182px. Neither is a single-line failure that wrapping could absorb — "KETENTUAN" is one unbreakable word. The 24px the widest heading overruns is what surfaces as 8px of page scroll after the section's own padding absorbs the rest.

It resolves on its own by 360px (every heading measures `scrollWidth === clientWidth` at 360 and 375), which is why 375px — the stated design target — reads zero. So this is a below-target gap in the Numbered-Step Rule's geometry, not a regression: the track is a fixed fraction of a numeral that stops shrinking at its clamp floor, while the heading beside it keeps needing the same width. Fixing it means changing either the numeral's floor, the 1.7 multiplier, or `--text-h2`'s floor — all three are art-direction decisions in DESIGN.md, not component bugs, and none should be picked silently.

VERIFIED: `pnpm lint` (0 errors, 1 pre-existing unrelated warning in booking-form.tsx), `pnpm typecheck` clean, `pnpm format:check` clean, `pnpm check:domain` 8/8 + 2 deps aligned, `pnpm check:docs` 14/14, `pnpm check:unit` 115/115 (unchanged — no test files in this turn's scope). Real-browser measurements at 320/375/414/768/1280/1440: `#order`'s own `scrollWidth - clientWidth` is 0 at every width (the page-level number is 0 at every width except 320px, where the Ketentuan defect above adds 8px unrelated to this turn). All four slot states confirmed live with correct triples via `getComputedStyle` — available (no `aria-disabled`, `pointer-fine:hover` glow), pending (`amber-100`/`amber-300`/`amber-800`, `aria-disabled="true"`, `disabled === false`), booked (`red-100`/`red-300`/`red-800`, same never-native-disabled pattern), elapsed (`grey-200` fill AND border, same colour, the one borderless-reading cell) — on real mock data reached by clicking through the date row, not assumed. Selecting an available slot flips its background to `rgb(37,99,235)` (`blue-600`) and `aria-pressed="true"` in the SAME synchronous click handler, before any animation frame — the 0ms-fill rule, checked by reading computed style immediately after a direct `.click()`, not after a `requestAnimationFrame`. Free-run badge: exactly one on a date with a 4-slot run, text "Bisa main 8 jam berturut-turut", matching `4 slots × 2h`. One `.impeccable` hook finding on `slot-cell.tsx` — `text-[13px]` off the documented type ramp — left as-is and disclosed rather than suppressed: it is DESIGN.md's own stated value ("State: Inter at 13px", moved from 14px in the same redesign), and the finding fires because `.impeccable/design.json`'s sidecar is the artifact already logged as owed and out of date, not because the value is wrong. Not committed, per the launching agent's instruction.

[2026-08-12] [engineer] REGRESSION FIXED, AND THE CAUSE UNDER IT. `/booking`'s three `<h1>`s ("Lengkapi Pemesanan", plus the expired and unusable notices) overflowed the viewport — measured 61px over at 320px, 49px at 375px, 41px at 414px — because the velocity token pass made the base `h1` rule uppercase at `--text-display` (48px->152px). That scale is tuned for the hero: one headline, three lines, one word each, longest word `PILIH JAM.` at five characters. "LENGKAPI PEMESANAN" is two long Indonesian words with no line of its own to control. DESIGN.md defines no scale for a lone form-page heading, so this borrows `--text-h2`'s size (28px->56px, the "section headings" role — a single-purpose route's h1 behaves like one section) and keeps the h1 tag's own weight, leading, tracking and uppercase, since only the size caused the overflow. The gap in DESIGN.md is flagged, not papered over with an invented number.

Under it sat a cascade bug that had already broken four headings silently. globals.css's `body` and `h1/h2/h3` rules were UNLAYERED, and an unlayered rule beats every rule in a layer regardless of specificity — including all of Tailwind's `@layer utilities`. So a `text-[length:var(--text-h3)]` class on a heading was emitted, matched, and lost, with no error anywhere. Two places were doing exactly that and rendering at a size nobody chose: the payment card's `<h2 id="payment-heading">` (asking for `--text-h3`) and three `<h3>` labels in home-page.tsx (asking for `--text-sm`). Those rules now live in `@layer base`. `:focus-visible` and the cursor-pointer restoration stay unlayered on purpose — outranking utilities is the point of those two.

Verified in a real browser at a confirmed port (3012, confirmed by content: the `/booking` h1 reads "Lengkapi Pemesanan"). `document.scrollWidth - clientWidth` is 0 at 320/375/414/768/1280 on the form, and the two notice states measure 0 at the same five widths. The h1 now computes 28px -> 56px across that range, and the payment `<h2>` 20px -> 30.3px, which is the `--text-h3` it had been asking for all along; the landing page's three `<h3>` labels now compute 14px (`--text-sm`) instead of `--text-h3`. `pnpm typecheck` clean, `pnpm format:check` "All matched files use Prettier code style!", `pnpm lint` 0 errors with one pre-existing `react-hooks/incompatible-library` warning. `/` still measures 8px of horizontal overflow at 320px, which belongs to the concurrent order-section work and is not addressed here.

[2026-08-12] [design] THE SUB-360 FLOOR, AND TWO CONTRAST FAILURES THE PROTOTYPE HAD ALREADY CAUGHT.

The 8px overflow at 320px is closed, by the user's choice among three options: `--text-h2` is pinned to 24px below 360px and untouched at 360 and above. A lower `clamp()` floor was the obvious move and does not work — at 320px the clamp's middle term governs, and `1rem + 3.4vw` still computes 26.88px there; steepening it to reach 24px at 320px would drag every width from 375px to 738px off the approved scale. The floor is the only part of the curve that moves without moving 375px. This narrows DESIGN.md's Fluid-Not-Stepped Rule, which is now written down as a named exception rather than left as a quiet contradiction. Measured after: every `h2` reads `scrollWidth === clientWidth` at 320/340/359/360/375/768, and page overflow is 0 at 320/360/375/414/768/1280 on `/` and at 320/375/768 on `/booking`.

Then, reading DESIGN.html to add the same floor to the prototype, found two live contrast failures — both of which DESIGN.html had ALREADY caught and recorded, and neither of which had reached the components. This is hard rule 10's blind spot working exactly as the rule predicts, in the direction nobody watches: not a gitignored critique this time, but a prototype whose comments carry a correction the spec it implements never received.

1. The slot cell's selected state label was `opacity-85`, straight from DESIGN.md's Slot Cell line. White at 85% over `blue-600` composites to `rgb(222,232,252)` and computes 4.19:1 against the fill — under AA, at 13px, so no large-text exemption. Now full white, 5.17:1. DESIGN.md's line is corrected at the source so the next reader does not re-implement it.
2. The date pill's day label was `opacity-80` on the selected pill, added by an earlier fix whose own comment says the muted navy "would drop under AA" — and then replaced it with 3.89:1, trading one contrast failure for a quieter one. Now full white. DESIGN.html names the rule this follows: the day separates from the date by SIZE, never by transparency.

Verified live at 375px: the selected pill's two spans both read `opacity: 1`, `rgb(255,255,255)`; the selected slot reads fill `rgb(37,99,235)` with "20.00 - 22.00" and "Dipilih" both at `opacity: 1`, `rgb(255,255,255)`. Swept `src/modules/` for any remaining transparency-as-mute — no matches. `pnpm typecheck` clean, `check:docs` 14/14, `check:domain` 8 identical + 2 deps aligned, `check:unit` 115/115, `lint` 0 errors (one pre-existing warning).

Still open, unchanged: the `.impeccable` `design-system-font-size` finding on `slot-cell.tsx`'s 13px is DESIGN.md's own value and remains unsuppressed, because suppression needs the user's word. The sidecar `.impeccable/design.json` is older than DESIGN.md and is owed a refresh.

[2026-08-12] [engineer] COMPONENT FILENAMES ARE PascalCase, AND THE SLOT STATE LABEL IS BACK ON THE TYPE RAMP. Two unrelated user requests in one turn.

Nine component files renamed to match their exports; `src/app/` untouched, because `layout.tsx`/`page.tsx`/`route.ts` are framework filenames and `providers.tsx` keeps their casing rather than being the single PascalCase file in a lowercase folder (user's call). Non-component module files keep `<module>.<role>.ts`. `docs/dev-rules.md`'s Naming table stated the OPPOSITE rule as a row and was rewritten; `docs/architecture.md`'s folder tree listed kebab component names as examples and now names the real files plus the convention outright. CLAUDE.md deliberately unchanged — its own "when to update" section routes naming to dev-rules.md and it never carried this rule.

TWO MECHANICAL TRAPS, BOTH REAL. `git config core.ignorecase` is true on this NTFS checkout, so a direct case-only `git mv` either errors or renames on disk while the index keeps the old casing — a commit that looks clean locally and breaks every case-sensitive build. Every rename went through a temporary name in two steps and was verified with `git ls-files`, which reads the index; `ls` cannot see this and would have lied. Second: of the nine import sites, eight are static imports `tsc` would catch, and the ninth is `dynamic(() => import("./hero-canvas"))` in `Hero.tsx` — invisible to typecheck, and invisible to `check:docs`, whose specifier scan never verifies that an import target exists. Caught by `pnpm build` and by the canvas actually mounting on a live server with zero console errors.

THE SLOT STATE LABEL: `text-[13px]` -> `--text-sm`. The user asked whether 12px would work. It would silence the `.impeccable` finding and it is the wrong answer: the free-run badge sits directly under this label and is already `--text-xs`, so 12px would make the cell's primary content the same size as its secondary affordance and leave colour as the only separator — against DESIGN.md's own standing rule that labels separate by size and face, never by transparency. DESIGN.md's Hierarchy assigns "state labels" to `sm`, which is where it sat before the redesign. 14px chosen by the user. The finding is now gone because the value is on the ramp, not because anything was suppressed — no `ignore-value` was run.

RE-MEASURED RATHER THAN SCALED, because DESIGN.md's own note demanded it: "Menunggu Konfirmasi" is **145.5px** at Inter 14px with the font loaded (`getBoundingClientRect` on the live string), against 133px at 13px and the 146px estimate before that. Three values in one day for one string is the argument for measuring it. The tightest column in the system leaves 222px of content at a 320px viewport, so every breakpoint justification still holds. DESIGN.md, DESIGN.html and the component comment all carry the measured number now.

AND ONE CONTRADICTION SETTLED WHILE THERE: the Fixed-Small Rule was written three times with two different floors — DESIGN.md's named rule said "below 12px", while DESIGN.md's five-parts paragraph and `globals.css`'s token comment said "below 14px", a floor `--text-xs` itself sits under, making the rule unsatisfiable by its own tokens. Both stale copies corrected to 12px.

VERIFIED: `pnpm build` compiled and generated all six routes; `typecheck` clean; `check:docs` 14/14; `check:domain` 8 identical + 2 deps aligned; `check:unit` 115/115; `lint` 0 errors (one pre-existing warning); `format:check` clean. Live at 320/375/414/768/1280 on `/` and 320/375/768 on `/booking`: `document.documentElement.scrollWidth - clientWidth` is 0 everywhere, which is the check that matters because the label just got wider. State label computes 14px on real cells. The badge's 12px was measured earlier this session and its token is untouched, so the 2px separation follows from two verified token values — the mock produced no date with a 3+ free run during this pass, so the two were not observed in the same cell, and the browse daemon restarted three times trying, at which point I stopped rather than keep clicking.

Still owed: `.impeccable/design.json` is a generated sidecar, now older than DESIGN.md and carrying a `componentLiterals` entry that declares 13px intentional. Refresh with `/impeccable document` as its own change — do not hand-edit a generated file.

[2026-08-12] [engineer] THE PAGE THAT COMPOSES `/` IS FINALLY VELOCITY TOO — turn 1 of 3, the frame rather than the furniture.

`HomePage.tsx` was the last pre-redesign file on the landing page, which is why `/` spent a day looking half-ported: `Hero`, `Section`, `OrderSection`, `SlotCell` and `DatePills` were all velocity and the thing arranging them was not.

THE STICKY HEADER IS `position: fixed`, AND THE SPEC WORD DOES NOT SURVIVE CONTACT WITH THE HERO. A `sticky top-0` header occupies flow space before it sticks, so the hero would begin below it and the two together would exceed one viewport — while DESIGN.md's Hero section opens with "Full `100svh`". `fixed` is the layout that produces the specified behaviour. New file `SiteHeader.tsx`, in `modules/home/components/` rather than `src/components/` because it has one consumer. Measured live: `position: fixed`, transparent with no border at scroll 0; past 40px it settles at exactly `rgba(255,255,255,0.82)` and `blur(14px)` with a `grey-200` bottom edge — the first two reads after scrolling caught it mid-transition at 0.776/13.28, which is worth recording because a single read would have reported the wrong number as the final one. The CTA measures **44px exactly**, which is DESIGN.md's correction to the source prototype's ~37px pill. The blur is gated behind `@supports` while the 82% fill is not, because `backdrop-filter` is missing on a real share of in-app webviews and the fill has to separate the header from a moving generative field on its own.

THE FIRST NAVY BAND, AND THE CONTRAST WAS COMPUTED RATHER THAN LOOKED AT. DESIGN.md calls the On-Band Rule "the redesign's single largest new source of defects" precisely because the failure is invisible in a screenshot, so `Section` gained a `band` prop that switches the SEMANTIC LAYER — surface, foreground, muted foreground and the numeral's outline stroke move together — instead of a list of colours a later component could half-adopt. Measured on the live band, every text node against `rgb(1,26,67)`: heading `blue-50` **15.69:1** (DESIGN.md's own stated figure, matched), accent word `blue-400` **6.71:1**, lede and sub-heading `navy-200` **7.91:1**, rule text `blue-50` **15.69:1**. Swept the whole document for band tokens leaking onto light surfaces — `blue-400` is 2.54:1 on white, so a leak is a real failure — and found **zero**. The falsifiable band test from DESIGN.md:301 at 375px: navy is **30% of page height** against a "more than half" bar, one banded element, hero and order section both unbanded.

THE HERO'S SECONDARY CTA WAS A DEAD LINK AND NOBODY HAD NOTICED. `Hero.tsx` has always rendered `href="#lokasi"` and no element on the page carried that id. Adding `#ketentuan` and `#lokasi` fixes it. The fixed header then owes every anchor its own height, so `scroll-padding-top: 80px` is declared once on `html` rather than as a `scroll-mt` per section — per-section is exactly how three of four anchors end up correct and the fourth does not. Verified: a `#lokasi` jump lands the section at y=96 against an 81px header.

A DEFECT MY OWN CHANGE CREATED, CAUGHT IN A SCREENSHOT AND NOT IN CODE REVIEW: with the header carrying the mark, the hero's own solid logo rendered ~100px below it and the page showed the logo twice, stacked. That inline mark was correct while there was no header and is now a leftover. Removed. DESIGN.md wants the mark in the hero in one form only — "the client mark at 7% opacity with a slow scroll parallax", `aria-hidden` — and that is a motion piece deliberately not built yet. So the hero shows no mark this turn and the brand appears once, above the fold, in the header.

FABRICATED COPY FOUND AND REMOVED, unrelated to the redesign: section 03's lede read "Mudah dijangkau, parkir luas." Nobody supplied either claim, and they are checkable facts about a place whose street address is still an outstanding placeholder — the same class of invention as a made-up street name, only quieter because it sounds like copy rather than data. Now "Satu lapangan, satu nomor WhatsApp", which is true and already on the page.

THE FOUR SECTION HEADINGS now match DESIGN.md's Section head table, with one accent word each through a local `Accent` helper that takes an explicit `band` prop rather than inheriting: `blue-600` is 5.17:1 on light and 3.30:1 on navy while `blue-400` is 6.72:1 on navy and 2.54:1 on white, so getting it backwards fails silently in BOTH directions and the surface is stated at every call site instead of remembered.

ONE AMBIGUITY FLAGGED RATHER THAN GUESSED: DESIGN.md's table calls 01's surface "White" and 03's "Ground". Section 01 already contains a white panel, so "White" most likely describes the panel rather than a section fill, and inventing a white section fill the document never otherwise describes would be reading a table as a spec. Both are on the page ground; if that is wrong it is a one-line change.

ALSO CORRECTED: DESIGN.md's "Still owed" list had four bullets and **three were false** — DESIGN.html was regenerated for velocity hours after that line was written, the sidecar carries 16 on-band references and no `1100px`, and eight components under `src/` were ported. Only "client re-approval" is still true. This is not tidying: DESIGN.html is what the client would be sent for that re-approval, and the list said it was stale.

VERIFIED: `pnpm build` compiled and generated all six routes; `typecheck` clean; `lint` 0 errors (one pre-existing warning); `check:docs` 14/14 — it caught a real mistake first, a `TODO(content)` written inside prose with no category, which is the allowlist working as designed; `check:domain` 8 identical; `check:unit` 115/115. Zero horizontal overflow at 320/375/414/768/1280.

NOT BUILT THIS TURN, named so their absence is not read as a decision: the Ketentuan ten-row band treatment (its list is transparent with on-band hairlines for now, so it is not a white card sitting on the band, but it is not the `84px 1fr` row grid either), the location section's map placeholder, the closing CTA band — turn 2; the slot legend and the two-column order composition — turn 3; the marquee, the parallax mark and the progress bar — turn 3, and all three need the user's motion choice before any of them is written. The footer's stale `max-w-[1100px]`, `rounded-[10px]` and its surviving `AP` monogram belong to turn 2's closing-CTA rewrite. `Hero.tsx` also still carries `max-w-[1100px]` against the 1280px container — noted, not fixed here.

Two `.impeccable` `design-system-font-size` findings on `Hero.tsx` (15px, twice) are the hero CTA labels and are DESIGN.md's own Buttons value; left unsuppressed like the others, because the sidecar refresh is the fix and suppression needs the user's word.

[2026-08-12] [decision] BOTH TYPEFACES REPLACED. Orbitron -> Saira at `wdth` 125, Inter -> Plus Jakarta Sans.

THE FACE THE USER ASKED FOR WAS NOT SHIPPED, AND THE REASON IS A LICENCE. They asked for NV Dune Hero from 1001fonts. Its own page says, verbatim, "This demo font is free for PERSONAL USE" and "ONLY FOR PERSONAL USE!!!". This is a paid client project, so shipping it would put THE CLIENT in breach, not the studio. Checked before writing any code rather than after. The user chose a free-commercial substitute over buying the full family; the brief behind the ask — wide, expanded, geometric, athletic — is what the substitute had to honour.

CHOSEN BY MEASUREMENT, NOT RESEMBLANCE. DESIGN.md names one figure "the binding number in the whole type system": `PILIH JAM.` has to fit the 343px content box at 375px. Probed in a browser with all three candidates loaded, at the size the clamp actually produces (57.25px, not the 48px floor): Archivo Expanded **384.6px, over the box by 42**; Saira ExtraExpanded **323.0px**; Orbitron **323.4px**. Archivo would have broken the headline outright — and it was the more obvious pick by eye. Saira lands within **0.4px** of the face it replaces on that number and is **9.7px narrower** on "KETENTUAN" (194px against 203.7px), which eases the Sub-360 Floor instead of worsening it. Body: "Menunggu Konfirmasi" measures **143.3px** in Plus Jakarta Sans at 14px against Inter's 145.5px. Plus Jakarta Sans was drawn for Indonesian city branding and has more open apertures than Inter, which is the right property for a captain reading Indonesian at speed on a mid-range Android — a contextual reason, not a taste one.

TWO CODE CONSTANTS WERE DERIVED FROM ORBITRON'S METRICS AND BOTH WERE RE-TESTED RATHER THAN CARRIED OVER.

1. `Section.tsx`'s numeral track, `calc(var(--text-numeral)*1.7)`. Saira's widest digit is "8" at 0.788em and its widest two-digit pair "08" at 1.55em, both narrower than Orbitron — but the numeral is SKEWED, and `skewX` widens the box. Measured at 1280px: numeral 134.4px, worst-case pair 208.3px of ink plus ~15px of skew against a 228.5px track. It fits with about 5px to spare and 1.65 would clip. **The constant stays, on new evidence.**
2. The Sub-360 Floor. Saira is narrower, so the honest question was whether the rule had stopped having a reason. It has not: at 320px the clamp's middle term computes 26.88px where "KETENTUAN" measures **181.7px** against a **174.1px** column, still over by 7.6px; at the 24px floor it measures 162px and fits. **The numbers moved, the conclusion did not.**

TWO DEFECTS THE SWAP FIXED THAT WERE NOT THE POINT OF IT.

- **`tabular-nums` does something now.** Nine slot times share one grid and their width must not depend on their digits — under Orbitron "18.00 - 20.00" came to 127px and "10.00 - 12.00" to 120px. The fix was tried in Phase 1b, measured as a NO-OP because Orbitron ships no `tnum` feature, and reverted rather than left in with a comment claiming it worked. Saira ships it: at 100px "111" and "888" measure 136.2px and 236.4px proportionally and both 214.8px with the feature on. **All nine times now measure 124.7px.**
- **Weights stopped being synthesised.** Orbitron was loaded at 500/700/900 while the system asks for 600 (hero eyebrow) and 800 (h2, buttons) — the browser was faking both. Both new faces are variable and neither declares `weight`, so the full ranges ship: `document.fonts` reports `Saira:100 900:50% 125%` and `Plus Jakarta Sans:200 800`.

THE WIDTH AXIS NEEDED A HOME, AND THIS IS THE ONE THING A LATER SESSION COULD GET SILENTLY WRONG. `--font-display` names a family; Saira's expanded look lives on `font-stretch`, a separate property. A display usage that sets the family and forgets the width renders an ordinary grotesque with nothing visibly broken. So `.type-display` in `globals.css` pairs them and all seven call sites route through it — deliberately NOT named `.font-display`, because Tailwind v4 generates utilities from `--font-*` theme keys and that collision fails silently.

THE RAMP GAINED A `label` ROLE AT 15px, which is what the design detector had been reporting for two sessions and I twice called a false positive. It was not: DESIGN.md's Buttons section has specified 15px since the redesign while the Hierarchy had no 15px step, so the document disagreed with itself and every button carried a hard-coded number. Four call sites now share `--text-label`. Two were wrong in different directions and are fixed by the same token: the header CTA was on `--text-eyebrow` (12px, and the One-Eyebrow Rule says that token belongs to the hero alone), and DESIGN.html's hand-off button was 13px. Detector findings on `src/` went from four to **zero** with no suppression.

A SUPPRESSION WAS REMOVED, NOT RE-WORDED. `.impeccable/config.json` carried an `overused-font: inter` ignore whose stated reason was the client commitment and the Inter-derived measurements. Both are gone, so the record goes with them.

AND THE SAME RULE NOW FIRES ON THE REPLACEMENT: `overused-font: plus jakarta sans`. Reported, not suppressed — suppression needs the user's word, and this one has a real answer if they want it (the face was chosen for Indonesian, not for neutrality).

PRODUCT.md:84 RECORDED THE OLD PAIR AS A CLIENT COMMITMENT, and both DESIGN.md and the suppression cited it as why the decision was closed. That line is now a dated supersession rather than a swap, and it says explicitly that the client has not seen this — it joins the velocity direction in the re-approval already owed.

VERIFIED: `pnpm build` compiled and generated all six routes; `typecheck` clean; `lint` 0 errors (one pre-existing warning); `check:docs` 14/14; `check:domain` 8 identical; `check:unit` 115/115. Live at 320/375/414/768/1280 on `/` and 320/375 on `/booking`: `scrollWidth - clientWidth` is **0 everywhere**, which is the check that matters when every heading on the page changes width. Header CTA still 44px tall at 15px. `document.fonts.check` confirms the arrow glyph, the middot and `×` are all covered by the new faces rather than falling back.

STILL OPEN: DESIGN.html's 24px Ketentuan numeral and its `clamp(2.5rem, …)` closing heading are off the ramp — both belong to components being built next turn, so they are reported rather than papered over. The map-placeholder grid finding is a false positive by the rule's own text, which exempts map surfaces.

[2026-08-12] [engineer] Two new components, `src/modules/home/components/SlotLegend.tsx` and `ProgressBar.tsx` — owned files only, nothing else touched (confirmed via `git status` before finishing). TWO CONFLICTS FOUND AND RESOLVED, both written into code comments rather than papered over. (1) THE RADIUS CONFLICT: DESIGN.md's Order section asks for a "40×22px chip at rounded.control" (12px), but 12px on a 22px-tall box is at least half the shorter side, so CSS clamps the corners to a capsule — indistinguishable from the pill DESIGN.md's own One-Round-Shape Rule reserves for the date pill alone. Resolved by growing the chip to 28px tall (`h-7`, spec kept at `w-10`/40px): half of 28 is 14, above the 12px radius, so the corners stay genuinely rounded, `--radius-control` is kept intact (no fourth radius invented, "No third radius" honoured), and the swatch still shares its rounding language with the `SlotCell` it explains. (2) THE MOTION-ROUTING CONFLICT: the brief says "ALL ANIMATION GOES THROUGH src/lib/motion.ts", but `ProgressBar` has no GSAP tween to gate — a scroll-position write with zero easing, same category as `HeroCanvas.tsx`'s existing raf loop, which also bypasses `useMotion` and checks `matchMedia` directly for the identical reason. Routing a bare `transform: scaleX()` write through `useMotion` would mean fetching 43.6KB of GSAP to call `gsap.set()` where one line of vanilla JS already does the job, and would also misuse `settle()` — documented as a one-shot idempotent call with no per-call cleanup — as a long-lived listener host. Built as a plain `useEffect` with a passive `scroll`+`resize` listener instead, no `requestAnimationFrame` loop. REDUCED-MOTION DECISION: the bar keeps updating under `prefers-reduced-motion`, not hidden — it carries wayfinding information (how much of the page, specifically the ten-rule Ketentuan band, is left) and it is a direct 1:1 function of the visitor's own scroll offset with no autonomous motion to suppress, so hiding it would cost reduced-motion visitors a real aid for no accessibility gain. LABEL TEXT: legend prints "Menunggu Konfirmasi" (capital K), matching `SlotCell.tsx`'s rendered `STATE_LABEL`, not DESIGN.md's own prose which lowercases it — the legend has to agree with what a visitor actually sees on a pending cell. MEASURED, NOT ASSUMED, since neither file is imported by any page yet and Tailwind's JIT could not be trusted output without one: `header` on the real running app is 81px tall, z-index 50 (both exactly as briefed). The swatch and bar classes were cross-checked against an isolated harness in the scratchpad (hand-expanded CSS matching the exact Tailwind arbitrary-value classes, tokens copied from `globals.css`) after a first live-DOM injection attempt gave inconsistent readings once already-compiled vs not-yet-compiled utility classes got mixed up mid-session. Isolated harness, three swatches: 40×28px, `border-radius: 12px` (unclamped — confirmed against the CSS spec's corner-radius-overlap formula, not just the browser), border renders at 1px (Chromium's DPR-1 snap of the declared 1.5px — same behaviour `SlotCell`'s existing 1.5px border already gets, not a regression). Bar: height 3px, top 0, z-index 60 — strictly above the header's 50, so it paints as a permanent hairline over the materialised header rather than underneath it. Legend total width (chip + gap + label) at 375px: 228.97px in a fallback system font; cross-checked against DESIGN.md's own authoritative Plus Jakarta Sans figure for "Menunggu Konfirmasi" (143.3px) giving 195.3px — both comfortably inside the 343px content box. `pnpm typecheck` clean, `pnpm lint`/`format:check` clean on both new files (pre-existing failures in `BookingForm.tsx`/`Marquee.tsx`/`Hero.tsx` belong to other agents this turn, confirmed not mine), `check:unit` 115/115 (no colocated test added, matching the existing convention that presentational/effect components in this directory — `SlotCell.tsx`, `SiteHeader.tsx` — carry none), `check:docs` 14/14. NOT MOUNTED — engineering-lead wires both into the page.

[2026-08-12] [engineer] `Button.tsx` (new, shared) and `Marquee.tsx` (new, hero-foot), `Hero.tsx` and `SiteHeader.tsx` wired to both — owned files only, confirmed via `git status` before finishing.

`Button.tsx`: three variants (`primary`/`secondary`/`on-band`) and two sizes. `size="sm"` (44px) exists ONLY for the header CTA, DESIGN.md's own stated tap-floor correction to the 56px spec height — a `PADDING_X` table splits horizontal padding by fill kind (`filled` vs `outlined`) rather than by variant name, because a 2px border eats into its own box and the original hand-rolled code had already tuned `secondary` 4px narrower than `primary` at the same size; that nuance is preserved rather than flattened. THE WIPE READS `var(--skew)`, not a literal angle — the exact defect DESIGN.md itself records happening once already (`skewX(-12deg)`, caught 2026-08-12, four hundred lines below its own One-Axis Rule). `secondary` gets NO wipe, on a reading of DESIGN.md's own verb choice: primary and on-band "wipe", secondary "fills [...] and inverts" — a plain `transition-colors`. `aria-disabled` only, never native `disabled`, on both the `<a>` and `<button>` branches (discriminated union on `href`). The wipe is pure CSS (`transition-transform`), not GSAP, so it needs no `src/lib/motion.ts` routing — same category as the pre-existing arrow-advance in the old `Hero.tsx`, and it inherits `prefers-reduced-motion` for free from `globals.css`'s global `transition-duration: 0.01ms` rule.

`Marquee.tsx`: full-width navy band, `skewY(-1.2deg)` (not `--skew`, per DESIGN.md's own distinction — a full-width element skewed at the button/numeral axis value would drop its corner 100px+ below the fold, confirmed by the arithmetic: `skewY` displacement scales with element WIDTH, so 1280px × tan(8deg) ≈ 179px, against 1280px × tan(1.2deg) ≈ 27px). The inner track carries the exact counter `skewY(1.2deg)` — two skewY matrices around the same axis compose to net zero, verified live: computed transform read `matrix(1, 0.02095, ...)` on the track (matches `tan(1.2°)`) while the band's own box renders visibly skewed. Content is the six facts from `DESIGN.html`'s own prototype marquee, copied verbatim rather than invented, cross-checked against real values rather than assumed: nine slots (`TIME_SLOTS.length`) and 14 days (`BOOKING_WINDOW_DAYS`).

ONE VALUE FLAGGED, NOT SILENTLY CHANGED: DESIGN.md's Marquee section literally says `blue-600` `///` separators. Not used — `blue-600` on `navy-900` computes 3.30:1, the exact on-band failure DESIGN.md's own token table exists to fix, and `Section.tsx`/`KetentuanRows.tsx` already read `--color-interactive-on-band` (`blue-400`, 6.72:1) in the identical position. Built with the semantic on-band token instead; DESIGN.md's prose is stale on this one line and should get the same correction its skew and opacity lines already received.

SPEED AND DIRECTION ARE NOT IN DESIGN.md, and design-process.md names exactly this ("marquee speed and direction") as needing `AskUserQuestion` before code exists. That tool is not in this session's toolset — no such function was available to call. Shipped a disclosed default (45px/s, right-to-left) rather than skip the file or silently invent without flagging it: documented in the file's own header comment, and named here as needing the user's confirmation before the client checkpoint, not as a settled decision.

A REAL BUG, FOUND BY MEASURING RATHER THAN ASSUMING: first build never actually moved. `getComputedStyle` sampled every second for 5s on a live, fully-visible hero read the identical matrix each time (`x` stuck at 0) — the IntersectionObserver's first callback fires before GSAP's dynamic import resolves, so `apply()` ran against a still-null `tweenRef.current` and nothing ever re-fired it once the tween existed, since the element's intersection state had not actually changed again. Fixed by lifting `visible` into a ref shared with `animate()`, which now checks it the moment the tween is created instead of unconditionally starting paused. Re-measured after the fix: `x` advanced -58 / -103 / -148 / -193 across four 1s samples (≈45px/s, matching `SPEED_PX_PER_SECOND` almost exactly), froze solid across 3 more samples after `scrollTo(0, 3000)`, and resumed from the SAME frozen value (no restart jump) after scrolling back — the full off-screen-halt round trip, live, not inferred.

VERIFIED: `pnpm typecheck` clean; `pnpm lint` 0 errors (1 pre-existing warning, `BookingForm.tsx`, not mine); `pnpm format:check` clean; `pnpm check:unit` 115/115 (no colocated test added — both files are presentational/effect components, the existing convention `SlotCell.tsx`/`SiteHeader.tsx` already carry none); `pnpm check:docs` 14/14. Live in a real browser (not curl-only): `scrollWidth - clientWidth` is 0 at 320/375/414/768/1280. Header CTA measures exactly 44px, both hero CTAs exactly 56px, at both 375px and 1280px. The wipe's resting computed transform read `matrix(1, 0, -0.140541, 1, -346.43, 0)` — confirmed against the compiled CSS output directly (fetched the built stylesheet, found `.group-hover:\[transform:translateX\(0\)_skewX\(var\(--skew\)\)\]:is(:where(.group):hover *) { transform: translateX(0) skewX(var(--skew)); }` inside `@media (hover: hover)`, Tailwind v4's own default guard against sticky touch-hover). A genuine hover DID register mid-session (interpolated matrix value `-261.195` caught between rest and full), but the shared `browse` daemon in this session restarted silently on the majority of commands issued against it — same instability an earlier entry in this file already named — so a clean settled-hover-state screenshot could not be captured; the compiled-CSS proof plus the mid-transition sample together are the evidence offered, not a live full-hover screenshot. `prefers-reduced-motion` for the marquee tween (no `animate()` call, so `tweenRef` stays null and the observer's play/pause calls become harmless no-ops) is code-reviewed against the already-verified `useMotion` mechanism, not independently re-measured this session — `Emulation.setEmulatedMedia` is not on this project's CDP allowlist.

Hero fits one `100svh` at 375px WITH the marquee, measured live: section height 812px = `window.innerHeight` 812px, not exceeded. The marquee band (54.25px tall) sits inside it; its skewed bounding box extends ~3px past the section's geometric bottom edge, clipped by the section's own `overflow-hidden` — this is the "pulled up over the hero's lower edge" DESIGN.md describes, not an overflow defect (page-level `scrollWidth - clientWidth` stays 0 throughout).

[2026-08-12] [engineer] THE LANDING PAGE IS COMPOSED. Every component DESIGN.md specifies for `/` now exists and is mounted: the ten-row Ketentuan band, the location block with its designed map placeholder, the navy Closing CTA, the three-chip slot legend, the scroll progress bar, the hero-foot marquee, and a shared `Button` carrying the skewed wipe at all four call sites. Four agents on non-overlapping files, composition wired by hand.

MEASURED AFTER MOUNTING, not assumed: `scrollWidth - clientWidth` is 0 at 320/375/414/768/1280 on `/` and 320/375/768 on `/booking`. Ten Ketentuan rows render with zero text under AA against the band (15.69 / 7.91 / 6.72 on the three on-band roles). The map placeholder is exactly 4:3 at every width (288x216, 343x257, 524x393, 442x331). The rule numeral computes 24px with a 1px blue-400 stroke and a -8deg skew. `/booking` still shows 5 inputs to 5 labels with `aria-disabled` and no native `disabled`.

THREE TYPE ROLES WERE ADDED, AND ALL THREE CLOSE DETECTOR FINDINGS I HAD TWICE CALLED FALSE POSITIVES. `label` at 15px (four buttons), `closing` (the closing heading), `ruleNumeral` at 24px (the Ketentuan rows). Each was a value DESIGN.md already specified in prose while its own type ramp had no step for it — the document disagreeing with itself, which the detector was reporting correctly the whole time. Findings under `src/` went from four to **zero with no suppression**. The single remaining finding in the repo is the map placeholder's grid in DESIGN.html, which the rule's own text exempts ("Reserve grid overlays for actual canvas, map, blueprint, or measurement surfaces").

ONE SPEC CONFLICT RESOLVED RATHER THAN OBEYED. DESIGN.md calls the closing heading "display-scale", which collides with the binding constraint it states forty lines earlier — the hero's longest word is five characters BECAUSE display type has 343px at 375px. "MENUNGGU." is nine. Measured at `--text-display`: 334.9px against a 288px box at 320px, and the page scrolled 7px. `--text-closing` clamps 40->112px instead, where the same word measures 272.1px and 300.9px. The scale was NOT invented: DESIGN.html's `.closing__h` had carried it since the redesign, which is exactly why 2.5rem kept being reported as off-ramp.

A REAL IMAGE BUG, WORTH 71KB. The header requested `/_next/image?url=%2Flogo-mark.png&w=3840&q=75` — the largest candidate in the device list — for a mark that paints 32px tall. `width={1042}` describes the SOURCE, not the box, and without `sizes` next/image cannot know the difference. Adding `sizes="80px"` moved it to `w=256`: **81.7KB -> 10.4KB**, verified in the network log of a production Lighthouse run. Nothing in this repo would ever have caught it — `check:budget` measures JavaScript and cannot see an image.

AND A MEASUREMENT I HAD TO RETRACT, WHICH IS THE ENTRY THAT MATTERS. I reported `/` at 77 and `/booking` at 80 earlier today. Both numbers are void, for two independent reasons I found only by checking:

1. **The first run was taken while four subagents were saturating the CPU.** Re-running the identical build on an idle machine gave 92. Same bytes, same code, 15 points apart.
2. **Neither run measured the build I thought it did.** `pnpm start` had failed with `EADDRINUSE` into a log I had not read, so the port kept serving an older build. `curl | grep "Jam operasional"` returned 0 — the LocationBlock was not in the page being scored. A status check would have shown 200 and told me nothing; the content check is what caught it.

THE HONEST NUMBER, after freeing the port properly, rebuilding, and running three times on an idle machine: **70 / 71 / 74, median 71** — FCP 2.4s, LCP 4.0s, TBT 330-430ms, CLS 0, SI 4.7s. That is **below the >=85 gate and over the <2.5s LCP rule**, and it is the current truth for both Phase 2 and Phase 3's last DoD box. The dominant costs are main-thread, not network: every request finishes by 83ms, while Script Evaluation runs 1.8s and Style & Layout 1.2s, with a 410ms document-level task at 689ms delaying first paint and a hydration cluster at 3.1-3.6s pushing LCP. DOM is 217 elements; images, compression and CSS all pass. **This is a performance project, not a design tweak, and it is not started.**

VERIFIED: `pnpm typecheck` clean, `check:docs` 14/14 over 100 files, `check:domain` 8 identical, `check:unit` 115/115, `lint` 0 errors with one pre-existing warning, `.impeccable` detector clean across `src/`.

OPEN, AND EACH NEEDS THE USER: the marquee's speed and direction (45px/s right-to-left is a disclosed default, not an approved decision — `design-process.md` requires the choice be made before the code, and no agent could ask); the location block's three display lines (ARENA / PLAYER / LOMBOK, reasoned from PRODUCT.md's confirmed market but still copy nobody approved); and the performance gate above. Also still owed: `.impeccable/design.json` needs a refresh for the three new type roles, and the client re-approval now covers the velocity direction AND the typeface change.

[2026-08-13] [lead] FIFTH AGENT ADDED — `ui-designer`, and it owns the whole visual layer rather than advising on it. The user's boundary, chosen explicitly: if a visitor can see it, the designer owns the decision AND the code. `software-engineer` narrows to route handlers, `src/domain/`, `src/server/`, `src/services/`, module logic, MSW and every test; its description, body and handoff line were rewritten to match. A form's submit path is the engineer's, the same form's markup is not.

THE GAP THIS FILLS WAS STRUCTURAL, NOT A PREFERENCE. `docs/design-process.md` named `/impeccable` as "the single named design driver" and named no owner at all — the word "agent" did not appear in that file once. Visual work therefore landed on whichever agent happened to be building, which is how a page ends up with sections designed by three different hands. The Consulting order gains a step 0 and a "Who runs this" section above it.

IT ALSO CLOSES A LIVE INCONSISTENCY. `software-engineer.md` obliged that agent to run the motion-approval `AskUserQuestion` round while its own frontmatter never granted `AskUserQuestion`. The duty moved to `ui-designer`, which does have the tool. `model: opus`, matching the repo's own split — opus for the two judgement roles, sonnet for execution.

WRITTEN AGAINST A GATE THAT TARGETS EXACTLY THIS FILE. `scripts/check-docs.mjs` check 7 scans `.claude/agents/` and fails on any hex or contrast ratio absent from DESIGN.md, and its own comment records that the agents surface is where the overstated-ratio defect came from. A design-owning agent is the file most tempted to quote tokens, so it points at DESIGN.md and copies nothing — verified: `grep -E "#[0-9a-f]{6}|[0-9.]+:1"` on the new file returns nothing.

REGISTERED IN SIX PLACES, WHICH IS EVERY ROSTER THIS REPO ACTUALLY HAS: the agent-tag line at the head of this file (`pm | lead | designer | engineer | reviewer`), the handoff rosters in `engineering-lead` and `project-manager`, `code-reviewer`'s "who fixes what" line, `design-process.md`'s new ownership section, and the folder tree in `architecture.md` — where `agents/` was the one entry with no inline contents comment while `skills/` beside it had one. That tree also gained the missing `check-docs.ps1`, a pre-existing staleness.

CLAUDE.md UPDATED, DELIBERATELY. The change crosses its own stated threshold — "a cross-cutting convention every agent must follow" — because it changes which agent writes UI code. One line, pointing rather than explaining, per that file's own budget note.

VERIFIED: `check:docs` 14/14 over 101 files, `typecheck` clean, `check:unit` 115/115, `check:domain` 8 identical, `lint` 0 errors. The new file is 33 lines, inside the 29-39 house range, with exactly the four frontmatter keys the other four use.

[2026-08-13] [designer] KETENTUAN REBUILT, AND THE OVERFLOW WAS NOT WHERE THE SECTION WAS. The band is one enamel plate now — 2px painted edges, a head field carrying the client's own title, ten hairline-divided rule fields. The plate's own overflow was 0 from the first measurement; the 41px belonged to the `<h2>` beside the ordinal, which no file inside that section can reach.

THE TRACK CONSTANT WAS BROKEN PAGE-WIDE UNDER PANCHANG, on hero, order, ketentuan and lokasi alike. Measured per glyph at 100px: this face's "0" advances 119.61px where its "1" advances 56.00px, so the widest two-digit ordinal is "04" at **2.33em** against a track sized at **1.7em** — a multiplier derived for Orbitron and re-justified for Saira. The ordinal's ink ran 13.9px past its own grid cell at 375px and 30.2px at 1280px, drawn straight through the heading. Visible in a screenshot, silent in every check.

WIDENING THE TRACK ALONE COULD NOT FIX IT, and that is the part worth recording. The track and the heading share one row, so every pixel the ordinal takes is a pixel "KETENTUAN" loses — and that word is a single unbreakable 278.6px in Panchang against 194px in the face it replaced. Raising the multiplier made the heading worse. The arithmetic left three levers and needed all three:

1. `--text-numeral` scaled by ~0.7, 56->40px floor and 144->100px ceiling, so the ordinal's INK occupies roughly what the composition was originally tuned on.
2. The track multiplier re-derived to **2.85** — 2.33em of ink plus the skew's overhang, measured together at 2.73em and rounded for margin.
3. The `--text-h2` floor stepped rather than cliffed: 18px below 360, 19px to 479, 24px from 480 to 599, and the clamp's own curve from 600 up, which is where it first fits on its own. A single floor could not work: the heading column grows with the viewport while the word stays one width, so one value is either too small on a phone or too big at 500px.

This is the third time the Sub-360 Floor has been re-derived rather than scaled, and the reason has been identical each time: the binding number is one specific word in one specific face, and nothing about it survives a typeface change.

MEASURED AFTER: `#ketentuan` `scrollWidth - clientWidth` is **0** at 320/360/375/414/480/560/600/768/1280/1440 — it was 42/41/21 at the first three. `#order` stays 0. Ten rows, zero text under AA on the band. Page-wide overflow is now entirely `#lokasi` and the closing band, both still built for the retired face and both owed their own turn.

THE AGENT'S OWN NUMBERS, VERIFIED INDEPENDENTLY: numeral track re-derived 84px -> 66px inside the rule rows (recovering 18px of reading column at 375px), rule text 15.69:1 resting and 11.94:1 hovered, numeral stroke 6.71:1 and 5.11:1, hairlines 1.31:1 and correct since they carry no state. It also declined DESIGN.md's 14px hover slide with a reason worth keeping: at 375px the slide put the row's right edge 2px from the viewport, and `transition-duration: 0.01ms` under reduced motion makes a transform INSTANT rather than absent, so a reduced-motion visitor was getting the full 14px jump.

OPEN, AND SURFACED RATHER THAN RESOLVED: `Hero.tsx` argues that on a navy plate an outlined word "reads as a hollow punched through the sign, and this direction is made of filled fields" — a direct precedent against the outlined rule numerals shipped here. DESIGN.md and the brief both specify outlined, so outlined shipped. DESIGN.md now contradicts the build in three places for this section (the `84px 1fr` grid, the 14px hover slide, the plate framing); that is expected, since it is rewritten from the built result at the end, but it should not be papered over.
