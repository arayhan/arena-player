.next/
node_modules/
pnpm-lock.yaml
public/mockServiceWorker.js

# Reference artifact, hand-authored, not production. Reformatting it would
# produce a large meaningless diff and fight the impeccable detector that
# scans it.
docs/DESIGN.html

# Tool-owned, regenerated by `/impeccable document` and the design hook.
# Formatting these rewrites all 398 lines of design.json, and the next
# regeneration rewrites them back — a diff that flip-flops forever and belongs
# to neither side.
.impeccable/

# Closed phases. History records what was true when the work ran and is never
# rewritten, whitespace included. docs/PROGRESS.md is deliberately NOT here:
# it is appended every step, so keeping it formatted costs nothing.
docs/progress-archive/
